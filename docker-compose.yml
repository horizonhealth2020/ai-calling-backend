version: "3.9"

services:
  postgres:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-ops}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ops_secret}
      POSTGRES_DB: ${POSTGRES_DB:-ops_prod}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ops}"]
      interval: 3s
      timeout: 3s
      retries: 10

  ops-api:
    build:
      context: .
      dockerfile: apps/ops-api/Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-ops}:${POSTGRES_PASSWORD:-ops_secret}@postgres:5432/${POSTGRES_DB:-ops_prod}
      AUTH_JWT_SECRET: ${AUTH_JWT_SECRET}
      PORT: "8080"
      NODE_ENV: production
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:3011,http://localhost:3012,http://localhost:3013,http://localhost:3014,http://localhost:3019,http://localhost:3026}
    ports:
      - "8080:8080"

  auth-portal:
    build:
      context: .
      dockerfile: Dockerfile.nextjs
      args:
        APP_NAME: auth-portal
        NEXT_PUBLIC_OPS_API_URL: ${OPS_API_URL:-http://localhost:8080}
    restart: unless-stopped
    depends_on:
      - ops-api
    environment:
      AUTH_JWT_SECRET: ${AUTH_JWT_SECRET}
      AUTH_PORTAL_URL: ${AUTH_PORTAL_URL:-http://localhost:3011}
      MANAGER_DASHBOARD_URL: ${MANAGER_DASHBOARD_URL:-http://localhost:3019}
      PAYROLL_DASHBOARD_URL: ${PAYROLL_DASHBOARD_URL:-http://localhost:3012}
      OWNER_DASHBOARD_URL: ${OWNER_DASHBOARD_URL:-http://localhost:3026}
      CS_DASHBOARD_URL: ${CS_DASHBOARD_URL:-http://localhost:3014}
    ports:
      - "3011:3000"

  sales-board:
    build:
      context: .
      dockerfile: Dockerfile.nextjs
      args:
        APP_NAME: sales-board
        NEXT_PUBLIC_OPS_API_URL: ${OPS_API_URL:-http://localhost:8080}
    restart: unless-stopped
    depends_on:
      - ops-api
    ports:
      - "3013:3000"

  manager-dashboard:
    build:
      context: .
      dockerfile: Dockerfile.nextjs
      args:
        APP_NAME: manager-dashboard
        NEXT_PUBLIC_OPS_API_URL: ${OPS_API_URL:-http://localhost:8080}
    restart: unless-stopped
    depends_on:
      - ops-api
    ports:
      - "3019:3000"

  owner-dashboard:
    build:
      context: .
      dockerfile: Dockerfile.nextjs
      args:
        APP_NAME: owner-dashboard
        NEXT_PUBLIC_OPS_API_URL: ${OPS_API_URL:-http://localhost:8080}
    restart: unless-stopped
    depends_on:
      - ops-api
    ports:
      - "3026:3000"

  payroll-dashboard:
    build:
      context: .
      dockerfile: Dockerfile.nextjs
      args:
        APP_NAME: payroll-dashboard
        NEXT_PUBLIC_OPS_API_URL: ${OPS_API_URL:-http://localhost:8080}
    restart: unless-stopped
    depends_on:
      - ops-api
    ports:
      - "3012:3000"

  cs-dashboard:
    build:
      context: .
      dockerfile: Dockerfile.nextjs
      args:
        APP_NAME: cs-dashboard
        NEXT_PUBLIC_OPS_API_URL: ${OPS_API_URL:-http://localhost:8080}
    restart: unless-stopped
    depends_on:
      - ops-api
    ports:
      - "3014:3000"

volumes:
  pgdata:
