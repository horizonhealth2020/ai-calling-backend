# v3.2 Chargeback Correctness — Archive

**Archived:** 2026-04-17
**Status:** Complete
**Tag:** v3.2

---

## Milestone Overview

**Goal:** Close chargeback correctness regressions surfaced after v3.1 — CS chargeback approval gate was never actually gating (POST /chargebacks mutated the paycard at submission regardless of source; alert approval was theater), plus cross-period paycard math/display holes (formatDollar strips sign, agentGross omits adjustmentAmount, Phase 71 residue in PayrollPeriods sidebar net, CLAWBACK_CROSS_PERIOD row tint orange instead of red).

**Constraints:** Forward-only. No schema changes.

**Scope note — Phase 80 (MyQueue Rep Linkage) SKIPPED from this milestone.** Discovery confirmed Phase 77 shipped a fully functional admin dropdown under OwnerUsers role edit that assigns `CsRepRoster` → `User.csRepRosterId`. The 3 active CS users (Alex, Ally, Jasmin — all `csRepRosterId = NULL` per prod SQL) simply needed the admin to link them via the existing UI — no code work required. Discovery artifacts preserved at `.paul/phases/80-myqueue-rep-linkage/` for reference; can be revived as a future phase if name-fallback or admin visibility bugs recur.

## Phases

| Phase | Name | Plans | Status | Completed |
|-------|------|-------|--------|-----------|
| 79 | Chargeback Approval Gate + Paycard Display | 1 | Complete | 2026-04-16 |

### Phase 79: Chargeback Approval Gate + Paycard Display

Focus: Five atomic correctness/display fixes:
1. Gate `Clawback` creation + `applyChargebackToEntry` on `source !== "CS"` in `POST /chargebacks` so CS-submitted chargebacks ONLY apply to the paycard upon alert approval (payroll-direct path preserved; they ARE the approver)
2. Introduce `formatDollarSigned()` in `packages/utils` that preserves leading minus (`-$76.04`); swap in `WeekSection.tsx:413` for clawback rows (existing `formatDollar` kept to avoid 40+ call-site regression)
3. Thread per-entry `adjustmentAmount` sum into agent card subtotal + liveNet + sidebar net + print net so cross-period chargeback rows deduct from on-screen totals
4. Replace Phase 71 residue (`+ fronted`) with Phase 78 (`- fronted`) in `PayrollPeriods.tsx:432,892`
5. Change CLAWBACK_CROSS_PERIOD row tint to RED (`semanticColors.statusDead` alpha-08); ZEROED_OUT_IN_PERIOD stays yellow

**Discovery:** `.paul/phases/79-chargeback-gate-and-display/DISCOVERY.md` (HIGH confidence)

**Plans:**
- [x] 79-01: Approval gate + formatDollarSigned + entryAdj threading + Phase 78 alignment + red cross-period tint (186/186 tests pass; 1 auto-fix deviation + 1 deferred item documented in 79-01-SUMMARY.md)

**Post-UAT patches (same day):**
- `f7ee226 fix(payroll): print card row highlight colors` — `.row-cross-period` CSS from orange to red (matching on-screen); `print-color-adjust: exact` added to all 4 row classes (cross-period, in-period-zero, clawback-applied, ach) on BOTH tr and td so highlights actually render when payroll prints
- `526e5f4 fix(payroll): print row net for clawback statuses` — Victoria Checkal print card showed `$0.00` instead of `-$76.04` because print template used `payoutAmount` unconditionally; now mirrors screen logic (netAmount with signed format for CLAWBACK_CROSS_PERIOD + ZEROED_OUT_IN_PERIOD); SUBTOTAL gross cell now shows `agentGross + agentEntryAdj` to match on-screen subtotal

## Deferred Items

- **79-DEFER-01:** Handler-level integration tests for POST /chargebacks approval gate. Plan required 3 new tests (CS submission → alert-only, CS approval → clawback, PAYROLL source → inline). Existing test infrastructure is unit-only (Prisma mocks via @ops/db, no supertest + test DB). Test-infra upgrade judged scope-creep. Compensated by 4 structural grep audits + UAT + unchanged 14 `applyChargebackToEntry` helper tests.
- Pre-deploy SQL query for pre-fix PENDING PayrollAlerts with associated pre-fix Clawbacks — must run against prod before shipping + brief payroll team on CLEAR-vs-APPROVE policy. Query preserved in 79-01-PLAN.md Verification section.
- OPEN-period SQL audit (Phase 78-02 carryover) — run before next production payroll lock to identify agents with mixed Phase 71/78 semantics in OPEN periods.

---
*Archived at v3.2 milestone closure — 2026-04-17*
