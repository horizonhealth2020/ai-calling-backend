# v3.1 CS + Payroll Gap Closure — Archive

**Archived:** 2026-04-16
**Status:** Complete
**Phases:** 2 (77, 78)
**Plans:** 4

---

## Phases

| Phase | Name | Plans | Completed |
|-------|------|-------|-----------|
| 77 | CS Fixes | 1 | 2026-04-16 |
| 78 | Payroll Polish + Fronted Fix | 3 | 2026-04-16 |

## What Shipped

**Phase 77 (77-01):**
- User.csRepRosterId FK + OwnerUsers dropdown + auto-sync on CUSTOMER_SERVICE user creation
- stale-summary DB lookup (not JWT) for rep identity
- Composite-key soft dedupe on POST /chargebacks + POST /pending-terms
- 184 tests at start → 184 tests at close (no regressions)

**Phase 78 (78-01):**
- Sale edit addon premium inputs (ManagerSales.tsx)
- CHANGES display fix ([object Object] → JSON.stringify)
- String error fix (numeric coercion parseFloat in saveEdit())

**Phase 78 (78-02):**
- computeNetAmount: fronted? optional param, deducts same-week
- carryover.ts D-09 (fronted→hold carry) removed; D-10 preserved
- payroll-net-formula.test.ts: 9-case suite (8 + backward compat)
- carryover tests updated (CARRY-02/07/09/11 from D-09 → D-10)
- 186 tests at close

**Phase 78 (78-03):**
- Server-side unapprove gate (OPEN periods only, logAudit on blocked)
- AgentPeriodAdjustment.notes schema + migration + POST endpoint
- WeekSection: unapprove gate, ACH print (print-color-adjust), note textarea, liveNet Phase 78
- PayrollService.tsx: Total green, agent name bold+centered
- CS print: per-agent cards, name on header line
- PayrollPeriods.tsx: CS print restructured with page breaks

## Key Technical Decisions

1. DB lookup for csRepRosterId (not JWT) — JWT won't carry new fields until session refresh
2. TOCTOU accepted for soft dedupe — human-pace paste, no concurrent client risk
3. Fronted deducted same-week, forward-only — mixed semantics in OPEN periods expected
4. print-color-adjust: exact on tr AND td — browsers strip tr backgrounds in print
5. HTML number inputs → parseFloat before Zod — e.target.value always string
