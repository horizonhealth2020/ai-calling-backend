---
phase: 54-product-type-color-coding
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Update product type color coding to visually differentiate ACA (purple), Core (blue), Add-ons (green), and AD&D (current amber) across the Products tab and payroll entry pills.

## Purpose
Currently Core and Add-ons both use teal variants, making them hard to distinguish at a glance. The new color scheme creates four distinct product type colors.

## Output
- TYPE_COLORS updated in PayrollProducts.tsx
- Badge colors updated in WeekSection.tsx
- Print CSS updated in PayrollPeriods.tsx for ACA
</objective>

<context>
@apps/ops-dashboard/app/(dashboard)/payroll/PayrollProducts.tsx (lines 38-40 — TYPE_COLORS)
@apps/ops-dashboard/app/(dashboard)/payroll/WeekSection.tsx (lines 328, 340 — Badge colors)
@apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx (lines 296-301 — chargeback type colors)
</context>

<acceptance_criteria>
## AC-1: Product types have distinct colors
```gherkin
Given any view showing product type badges (Products tab, payroll entries)
When products are displayed
Then ACA products show purple badges
And Core products show blue badges
And Add-on products show green badges
And AD&D products show amber/warning badges (unchanged)
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Update product type colors across payroll views</name>
  <files>apps/ops-dashboard/app/(dashboard)/payroll/PayrollProducts.tsx, apps/ops-dashboard/app/(dashboard)/payroll/WeekSection.tsx, apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx, apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx</files>
  <action>
    **PayrollProducts.tsx — TYPE_COLORS (line 38-40):**
    Change from:
    ```
    CORE: C.primary400, ADDON: C.accentTeal, AD_D: C.warning, ACA_PL: C.info
    ```
    To:
    ```
    CORE: semanticColors.accentBlue, ADDON: semanticColors.accentGreenMid, AD_D: C.warning, ACA_PL: semanticColors.accentPurple
    ```
    Add `semanticColors` to the @ops/ui import if not already present.

    **WeekSection.tsx — Badge colors:**
    - Line 328: Core product badge — change `C.primary400` to `semanticColors.accentBlue`
    - Line 340: Addon badge — change `C.accentTeal` to `semanticColors.accentGreenMid` (keep AD_D as `C.warning`)
    - ACA badge styling (line 329): update ACA_BADGE constant color if it exists, or the inline style, to use purple tint
    Add `semanticColors` to the @ops/ui import if not already present.

    **PayrollChargebacks.tsx — type colors (lines 296-301):**
    Update the chargeback type color mapping to match:
    - CORE → semanticColors.accentBlue
    - ADDON → semanticColors.accentGreenMid
    - AD_D → keep current
    - ACA_PL → semanticColors.accentPurple

    **PayrollPeriods.tsx — Print CSS (lines 786-787):**
    Update `.prod-aca` CSS color from cyan to purple tint for print consistency.

    Avoid: Changing AD&D color — it stays as C.warning (amber/yellow).
    Avoid: Changing any non-color properties (padding, font, layout).
  </action>
  <verify>
    - Products tab shows 4 distinct badge colors
    - Payroll entry pills show matching colors
    - TypeScript compiles without errors
  </verify>
  <done>AC-1 satisfied: Four distinct product type colors visible</done>
</task>
</tasks>

<verification>
- [ ] ACA badges are purple
- [ ] Core badges are blue
- [ ] Add-on badges are green
- [ ] AD&D badges are amber (unchanged)
- [ ] Colors consistent between Products tab and payroll entry pills
- [ ] TypeScript compiles without new errors
</verification>

<output>
After completion, create `.paul/phases/54-product-type-color-coding/54-01-SUMMARY.md`
</output>
