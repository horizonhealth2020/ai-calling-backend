3 files written to `~/.claude/skills/zod/`:

- **SKILL.md** — Quick reference: `zodErr()` helper, `.safeParse()` pattern, financial field constraint table, `.parse()` vs `.safeParse()` decision
- **references/patterns.md** — `zodErr` requirement, financial constraints (including the `adjustmentAmount` chargeback exception), enum/array patterns, nullable vs optional table, WARNING on `.parse()` bubbling as 500s
- **references/workflows.md** — New route checklist, add-a-field feedback loop, nested Prisma relations workflow, date/Decimal coercion bridge, debugging guide for common 400/500 symptoms