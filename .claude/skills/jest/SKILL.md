All 5 files written to `~/.claude/skills/jest/`:

- **SKILL.md** — quick reference with the module reset pattern, jest config, and run commands
- **references/unit.md** — inline extraction pattern, env-dependent module testing, `jest.spyOn` with restore
- **references/integration.md** — current placeholder state, how to add real tests with supertest, WARNING about missing `supertest` dep
- **references/mocking.md** — `jest.mock()` with factory, mock return values, `clearAllMocks` vs `resetModules` vs `mockRestore` table
- **references/fixtures.md** — inline-first approach, env var save/restore helper pattern, object factories, mutation anti-pattern