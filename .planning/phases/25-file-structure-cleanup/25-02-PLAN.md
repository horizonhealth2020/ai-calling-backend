---
phase: 25-file-structure-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - FS-02
  - FS-03
  - FS-04

must_haves:
  truths:
    - "apps/payroll-dashboard/ directory does not exist"
    - "No orphaned .js or .md files remain at repo root (only CLAUDE.md and README.md)"
    - "FIXES.md, ISSUES.md, TESTING.md, and docs/ no longer exist"
    - "README.md contains Morgan known issues summary, Railway deployment info, and updated monorepo layout"
    - "CLAUDE.md reflects current app structure (ops-api, ops-dashboard, sales-board, morgan)"
    - "No stale references to payroll-dashboard remain in tracked documentation files"
  artifacts:
    - path: "README.md"
      provides: "Updated project docs with consolidated content from deleted files"
    - path: "CLAUDE.md"
      provides: "Updated AI instructions reflecting current app structure"
  key_links:
    - from: "README.md"
      to: "apps/"
      via: "monorepo layout section"
      pattern: "apps/morgan"
    - from: "CLAUDE.md"
      to: "apps/morgan/"
      via: "architecture and commands sections"
      pattern: "apps/morgan"
---

<objective>
Delete stale apps and docs, consolidate useful documentation into README.md, and update all documentation references to reflect the current repository structure.

Purpose: After Morgan relocation (Plan 01), the repo still has an empty payroll-dashboard directory, orphaned doc files (FIXES.md, ISSUES.md, TESTING.md, docs/), and stale references throughout CLAUDE.md and README.md. This plan cleans all of that up so the repo looks like a professional dev team created it.

Output: Clean repo root with no orphaned files. README.md with consolidated useful content. CLAUDE.md reflecting actual current app structure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-file-structure-cleanup/25-CONTEXT.md
@.planning/phases/25-file-structure-cleanup/25-RESEARCH.md
@.planning/phases/25-file-structure-cleanup/25-01-SUMMARY.md

@README.md
@CLAUDE.md
@FIXES.md
@ISSUES.md
@TESTING.md
@docs/railway-deploy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete stale directory and files, consolidate docs into README.md</name>
  <files>README.md, FIXES.md, ISSUES.md, TESTING.md, docs/railway-deploy.md, apps/payroll-dashboard/</files>
  <read_first>
    README.md, FIXES.md, ISSUES.md, TESTING.md, docs/railway-deploy.md
  </read_first>
  <action>
    **Step 1: Delete stale directory and files**

    ```bash
    rm -rf apps/payroll-dashboard
    git rm -f FIXES.md ISSUES.md TESTING.md
    git rm -rf docs/
    ```

    **Step 2: Rewrite README.md**

    The README.md needs a complete rewrite to reflect the current state of the repo. The current README references 6 standalone apps (auth-portal, manager-dashboard, payroll-dashboard, owner-dashboard, sales-board, ops-api) that were consolidated into ops-dashboard in v1.3. It also references `index.js` at root (now `apps/morgan/index.js`), payroll-dashboard (deleted), and docs/ (deleted).

    The new README.md must contain these sections (use clean, professional formatting):

    1. **Title and overview** -- "INS Internal Operations Platform (Railway Monorepo)" with two workloads: Morgan voice service (apps/morgan/) and Ops Platform (apps/ + packages/)

    2. **System Architecture** -- Keep the existing Mermaid flowchart but update:
       - Remove AP (Auth Portal), MD (Manager Dashboard), PD (Payroll Dashboard), OD (Owner Dashboard) as separate nodes
       - Replace with single OD node for "Ops Dashboard :3000" (the consolidated dashboard)
       - Keep SB (Sales Board :3013) as separate
       - Update MORGAN node to say "apps/morgan/" instead of "root index.js"
       - Update auth flow arrows to show ops-dashboard instead of individual dashboards

    3. **Role-Based Access** -- Keep existing table as-is (roles haven't changed)

    4. **Data Flow -- Sale Lifecycle** -- Keep existing Mermaid diagrams (sale flow and ER diagram unchanged)

    5. **Monorepo Layout** -- Update to reflect current structure:
       ```
       .
       ├── apps/
       │   ├── morgan/          # Voice service (Node.js/Express)
       │   ├── ops-api/         # REST API (Express + Prisma)
       │   ├── ops-dashboard/   # Unified dashboard (Next.js 15)
       │   └── sales-board/     # Public leaderboard (Next.js 15)
       ├── packages/
       │   ├── auth/     # JWT + sessions
       │   ├── db/       # Prisma client
       │   ├── types/    # Roles + shared types
       │   ├── ui/       # PageShell + theme
       │   └── utils/    # Structured logging
       ├── prisma/
       │   ├── schema.prisma
       │   └── seed.ts
       └── package.json
       ```

    6. **Service Responsibilities** -- Update to current 4 apps:
       - **ops-api**: auth, session endpoints, RBAC, business logic, payroll/clawbacks, exports API
       - **ops-dashboard**: unified dashboard with role-based views (manager, payroll, owner, CS, admin)
       - **sales-board**: read-only public sales leaderboard
       - **morgan**: AI voice calling service (Convoso + Vapi integration)

    7. **Shared Auth/Session** -- Keep existing content (unchanged)

    8. **Environment Variables** -- Update sections:
       - ops-api: keep as-is
       - Replace "auth-portal" section with "ops-dashboard": `NEXT_PUBLIC_OPS_API_URL`
       - Replace "manager/payroll/owner/sales-board" with "sales-board": `NEXT_PUBLIC_OPS_API_URL`
       - Add "morgan" section listing: `CONVOSO_AUTH_TOKEN`, `VAPI_API_KEY`, `VAPI_MORGAN_ASSISTANT_ID`, `VAPI_PHONE_NUMBER_IDS`, `MORGAN_ENABLED`, `PORT`, `LOG_LEVEL`

    9. **Local Development** -- Update Morgan section:
       ```
       Morgan voice service:
       npm ci
       node apps/morgan/index.js
       ```
       Update ops workspace commands -- remove stale `npm run auth:dev`, `npm run manager:dev`, `npm run payroll:dev`, `npm run owner:dev`. Keep:
       ```
       npm run ops:dev        # ops-api on :8080
       npm run dashboard:dev  # ops-dashboard on :3000
       npm run salesboard:dev # sales-board on :3013
       ```

    10. **Morgan Testing** (NEW section, consolidated from TESTING.md):
        ```
        ## Morgan Tests

        npm test                          # run all Morgan tests
        npm test -- helpers.test.js       # single file
        npm test -- -t "test name"        # by name
        npm run test:watch                # watch mode
        npm run test:coverage             # with coverage

        Test files live in `apps/morgan/__tests__/`.
        ```

    11. **Morgan Known Issues** (NEW section, consolidated from ISSUES.md -- brief summary, not full detail):
        ```
        ## Morgan Known Issues

        The Morgan voice service has 19 documented code-level issues from a Feb 2026 audit.
        The 3 highest-severity items (race condition, memory leak, single-lead bottleneck) were fixed.

        Remaining open issues by category:
        - Error handling: inconsistent 429 handling, no retry for failed Convoso updates, silent webhook failures
        - Data validation: no webhook payload validation, unsafe string-to-number conversion
        - Configuration: phone number count not enforced
        - Performance: inefficient queue hydration, synchronous request processing
        - Code quality: monolithic index.js (1500+ lines), mixed logging, global mutable state
        - Security: no rate limiting on webhook endpoints

        These are documented for awareness. Morgan is working in production and these
        issues are deferred to post-launch.
        ```

    12. **Railway Deployment** -- Update the service mapping table:

        | Service | Root directory | Build command | Start command |
        |---|---|---|---|
        | `ai-calling-backend` (Morgan) | `.` | `npm ci --workspaces=false` | `npm start` |
        | `ops-api` | *(blank / unset)* | `npm ci && npm run build -w @ops/ops-api` | `npm run start -w @ops/ops-api` |
        | `ops-dashboard` | *(blank / unset)* | `npm ci && npm run build -w @ops/ops-dashboard` | `npm run start -w @ops/ops-dashboard` |
        | `sales-board` | *(blank / unset)* | `npm ci && npm run build -w @ops/sales-board` | `npm run start -w @ops/sales-board` |

        Update recommended watch paths:
        - Morgan: `/apps/morgan/**`, `/package.json`, `/package-lock.json`
        - ops-api: `apps/ops-api/**`, `packages/**`, `prisma/**`
        - ops-dashboard: `apps/ops-dashboard/**`, `packages/**`
        - sales-board: `apps/sales-board/**`, `packages/**`

        Add note: "Morgan watch paths in Railway UI may need manual update from root paths to `apps/morgan/` paths."

    13. **Railway Deployment Notes** (consolidated from docs/railway-deploy.md):
        Keep the existing "Railway private networking notes" and "Manual Railway + DNS follow-up" sections but update domain references to remove stale apps:
        - Remove auth, manager, payroll, owner subdomains
        - Add dashboard subdomain
        - Keep salesboard

    14. **Crash Prevention** (NEW section, consolidated from FIXES.md -- brief reference):
        ```
        ## Deployment Crash Prevention

        Key deployment pitfalls documented from past incidents:

        1. **Never hardcode `output: "standalone"` in next.config.js** -- use conditional
           `process.env.NEXT_OUTPUT_STANDALONE === "true" ? "standalone" : undefined`.
           Only Docker builds set this var. Railway uses `next start` which is incompatible.

        2. **Dockerfile CMD must use shell form** -- exec form `["node", "..."]` doesn't
           expand `${APP_NAME}`. Use `CMD node apps/${APP_NAME}/server.js`.

        3. **Postgres depends_on needs `condition: service_healthy`** -- plain `depends_on`
           doesn't wait for postgres to accept connections.

        4. **`NEXT_PUBLIC_*` vars are build-time only** -- set as build args, not runtime env.

        5. **`NEXT_PUBLIC_OPS_API_URL` must be browser-reachable** -- never use Docker
           internal hostnames like `http://ops-api:8080`.
        ```

    DO NOT include: full code examples from FIXES.md (too verbose), full reproduction steps from ISSUES.md (too detailed), CI/CD YAML from TESTING.md (not currently in use), test writing guides from TESTING.md (standard Jest knowledge).
  </action>
  <verify>
    <automated>test ! -d apps/payroll-dashboard && test ! -f FIXES.md && test ! -f ISSUES.md && test ! -f TESTING.md && test ! -d docs && echo "PASS: stale files deleted"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/payroll-dashboard/` does not exist (verify: `test ! -d apps/payroll-dashboard`)
    - `FIXES.md` does not exist (verify: `test ! -f FIXES.md`)
    - `ISSUES.md` does not exist (verify: `test ! -f ISSUES.md`)
    - `TESTING.md` does not exist (verify: `test ! -f TESTING.md`)
    - `docs/` directory does not exist (verify: `test ! -d docs`)
    - README.md contains "apps/morgan/" (verify: `grep "apps/morgan" README.md`)
    - README.md does NOT contain "payroll-dashboard" in the monorepo layout or service table (verify: `grep -c "payroll-dashboard" README.md` returns 0)
    - README.md contains "Morgan Known Issues" section (verify: `grep "Morgan Known Issues" README.md`)
    - README.md contains "Morgan Tests" section (verify: `grep "Morgan Tests" README.md`)
    - README.md contains "Crash Prevention" section (verify: `grep "Crash Prevention" README.md`)
    - README.md contains updated Railway service table with only 4 services (verify: `grep "ops-dashboard" README.md`)
    - README.md does NOT list auth-portal, manager-dashboard, or owner-dashboard as separate services (verify: `grep -cE "auth-portal|manager-dashboard|owner-dashboard" README.md` returns 0 or only appears in historical context)
  </acceptance_criteria>
  <done>Stale payroll-dashboard directory deleted. FIXES.md, ISSUES.md, TESTING.md, and docs/ deleted. README.md rewritten with consolidated content from all deleted files, reflecting current 4-app structure with Morgan at apps/morgan/.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md to reflect current repository structure</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md
  </read_first>
  <action>
    Update CLAUDE.md to reflect the actual current state of the repository. The current CLAUDE.md references 6 standalone apps that no longer exist (auth-portal, manager-dashboard, payroll-dashboard, owner-dashboard were consolidated into ops-dashboard in v1.3).

    **Specific changes:**

    1. **Commands section** -- Update test commands:
       - `npm test` already works (package.json updated in Plan 01)
       - Remove `npm run auth:dev`, `npm run payroll:dev`, `npm run manager:dev`, `npm run owner:dev` lines
       - Keep `npm run ops:dev`, `npm run salesboard:dev`
       - Add/confirm `npm run dashboard:dev  # ops-dashboard → localhost:3000`

    2. **Architecture section** -- Update the "two independent workloads" description:
       - Change "legacy AI calling system at repo root (`index.js`)" to "AI calling system at `apps/morgan/` (`apps/morgan/index.js`)"

    3. **Apps table** -- Replace the 6-app table with current 4 apps:

       | App | Port | Purpose |
       |-----|------|---------|
       | `ops-api` | 8080 | Express.js REST API -- auth, RBAC, sales, payroll, clawbacks, exports |
       | `ops-dashboard` | 3000 | Unified dashboard -- role-based views (manager, payroll, owner, CS, admin) |
       | `sales-board` | 3013 | Read-only sales leaderboard (no auth required for board endpoints) |
       | `morgan` | 3001 | AI voice calling service (Convoso + Vapi integration) |

    4. **Remove stale port references** -- Remove lines referencing ports 3011, 3012, 3019, 3026 if they refer to deleted standalone apps

    5. **Known Gotchas section** -- Update "Port assignments are fixed" entry:
       - Change from listing 5 ports to: ops-api:8080, ops-dashboard:3000, sales-board:3013, morgan:3001 (or whatever Morgan's actual port is)

    6. **Test commands** -- Already correct after Plan 01 package.json update. Verify CLAUDE.md matches:
       ```
       npm test                         # run all Morgan tests
       npm test -- helpers.test.js      # single file
       npm test -- -t "test name"       # by name
       npm run test:watch               # watch mode
       npm run test:coverage            # with coverage
       ```

    7. **Stale reference sweep** -- After all edits, search CLAUDE.md for any remaining references to:
       - `auth-portal` (remove or update)
       - `manager-dashboard` (remove or update)
       - `payroll-dashboard` (remove)
       - `owner-dashboard` (remove or update)
       - `root index.js` or just `index.js` referencing Morgan at root (update to `apps/morgan/index.js`)
       - `__tests__/` at root (update to `apps/morgan/__tests__/`)

    IMPORTANT: Preserve all content that is still accurate (Auth and RBAC section, Styling section, API Conventions, Database section, Environment Variables, Deployment section, Skill Usage Guide). Only change what is stale.
  </action>
  <verify>
    <automated>grep -c "payroll-dashboard" CLAUDE.md | grep -q "^0$" && grep "apps/morgan" CLAUDE.md > /dev/null && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "apps/morgan/" reference (verify: `grep "apps/morgan" CLAUDE.md`)
    - CLAUDE.md does NOT contain "payroll-dashboard" (verify: `grep -c "payroll-dashboard" CLAUDE.md` returns 0)
    - CLAUDE.md Apps table lists exactly 4 apps: ops-api, ops-dashboard, sales-board, morgan (verify: `grep -c "ops-api\|ops-dashboard\|sales-board\|morgan" CLAUDE.md` returns at least 4)
    - CLAUDE.md does NOT reference `auth-portal` as a current app (verify: search for auth-portal -- should not appear in Apps table or dev commands)
    - CLAUDE.md does NOT reference `manager-dashboard` as a current app
    - CLAUDE.md does NOT reference `owner-dashboard` as a current app
    - CLAUDE.md contains `npm run dashboard:dev` (verify: `grep "dashboard:dev" CLAUDE.md`)
    - CLAUDE.md does NOT contain `npm run auth:dev` (verify: `grep -c "auth:dev" CLAUDE.md` returns 0)
    - Final stale reference check passes: `grep -rn "payroll-dashboard\|auth-portal\|manager-dashboard\|owner-dashboard" CLAUDE.md README.md` returns 0 matches (excluding historical/planning references)
  </acceptance_criteria>
  <done>CLAUDE.md reflects current 4-app structure. No stale references to deleted standalone apps. Morgan referenced at apps/morgan/ not repo root. All dev commands match actual package.json scripts.</done>
</task>

<task type="auto">
  <name>Task 3: Final stale reference sweep across entire repo</name>
  <files>CLAUDE.md, README.md, package.json</files>
  <read_first>
    CLAUDE.md, README.md
  </read_first>
  <action>
    Run a comprehensive grep across the entire repository (excluding node_modules, .git, .planning/) for stale references that should no longer exist:

    ```bash
    # Check for stale payroll-dashboard references in code/config files
    grep -rn "payroll-dashboard" --include="*.{json,yml,yaml,toml,md,js,ts,tsx}" . \
      --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning

    # Check for Morgan files referenced at root (not apps/morgan/)
    grep -rn '"index\.js"' --include="*.{json,yml,yaml,toml}" . \
      --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning

    # Check for stale doc references
    grep -rn "FIXES\.md\|ISSUES\.md\|TESTING\.md\|docs/railway" --include="*.{md,json}" . \
      --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning

    # Check root directory is clean
    ls *.js *.md 2>/dev/null
    # Expected: only CLAUDE.md and README.md for .md files, NO .js files
    ```

    Fix any remaining stale references found. Expected files at repo root after cleanup:
    - package.json, package-lock.json
    - tsconfig.base.json
    - docker-compose.yml, Dockerfile.nextjs
    - railway.toml, nixpacks.toml
    - CLAUDE.md, README.md
    - .env.example, .env (gitignored), .gitignore
    - .github/ (workflows)
    - prisma/, apps/, packages/, node_modules/, .planning/

    If any unexpected files are found at root (beyond the above list), investigate and either remove or document why they belong there.
  </action>
  <verify>
    <automated>npm test && echo "---" && test ! -f FIXES.md && test ! -f ISSUES.md && test ! -f TESTING.md && test ! -d docs && test ! -d apps/payroll-dashboard && echo "PASS: all clean"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes (Morgan tests still green after all changes)
    - `grep -rn "payroll-dashboard" --include="*.json" --include="*.yml" --include="*.toml" --include="*.ts" --include="*.tsx" --include="*.js" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning` returns 0 matches
    - `ls *.js 2>/dev/null` at repo root returns empty (no .js files at root)
    - `ls *.md 2>/dev/null` at repo root returns exactly CLAUDE.md and README.md
    - No references to FIXES.md, ISSUES.md, TESTING.md, or docs/ remain in CLAUDE.md or README.md (verify: `grep -c "FIXES.md\|ISSUES.md\|TESTING.md\|docs/railway" CLAUDE.md README.md` returns 0 for both files)
  </acceptance_criteria>
  <done>Zero stale references remain in any tracked file. Root directory contains only expected monorepo config files. All Morgan tests pass. Repository structure is clean and professional.</done>
</task>

</tasks>

<verification>
- `test ! -d apps/payroll-dashboard && echo PASS` -- stale directory gone
- `test ! -f FIXES.md && test ! -f ISSUES.md && test ! -f TESTING.md && test ! -d docs && echo PASS` -- stale docs gone
- `npm test` -- Morgan tests still pass
- `grep -rn "payroll-dashboard" --include="*.{json,yml,yaml,toml,md,js,ts,tsx}" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning` -- zero matches
- `ls *.js 2>/dev/null` at root -- returns nothing
- `ls *.md` at root -- returns only CLAUDE.md and README.md
- `grep "apps/morgan" README.md CLAUDE.md` -- both files reference Morgan at new location
</verification>

<success_criteria>
Stale payroll-dashboard deleted. Orphaned docs consolidated into README.md and deleted. CLAUDE.md and README.md reflect the actual current 4-app repository structure. No stale references remain in any tracked file. Morgan tests pass. Root directory contains only monorepo configuration files.
</success_criteria>

<output>
After completion, create `.planning/phases/25-file-structure-cleanup/25-02-SUMMARY.md`
</output>
