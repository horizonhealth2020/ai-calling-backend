---
phase: 19-dashboard-consolidation-uniform-date-ranges
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-19T20:54:29.684Z
---

<current_state>
Phase 19 execution nearly complete. All 8 plans coded and committed (7 with SUMMARYs).
Plan 19-08 has 2/3 tasks done — stuck on Task 3 (verification checkpoint) due to Railway deployment issues.
The code is fully built — the blocker is Railway configuration only.
</current_state>

<completed_work>

- Plan 19-01: Unified app shell — login, auth middleware, role-gated tabs, SocketProvider, DateRangeContext (commit 6251ce6)
- Plan 19-02: Shared package updates — DateRangeFilter presets, decodeTokenPayload export, last_week dateRange (commit 16bd4c4)
- Plan 19-03: CS dashboard migration — 2 sub-tabs (Submissions, Tracking) (commit 5ad7967)
- Plan 19-04: Owner dashboard migration — 4 sub-tabs + SUPER_ADMIN Users gating (commit 718709f)
- Plan 19-05: Payroll dashboard migration — 5 sub-tabs with shared state (commit 94cf7b9)
- Plan 19-06: Manager dashboard migration — 5 sub-tabs with cross-tab refresh (commit 9c1afff)
- Plan 19-07: Uniform date range wiring — 5 API endpoints + all tabs wired (commit a14c695)
- Plan 19-08 Task 1: CORS, Docker consolidation (8→4 containers), root scripts (commit 5368168)
- Plan 19-08 Task 2: Removed old standalone dashboard directories (45 files) (commit 317d094)
</completed_work>

<remaining_work>

- Plan 19-08 Task 3: End-to-end verification checkpoint — needs Railway deployment working
- Phase verification (gsd-verifier) — runs after all plans complete
- ROADMAP.md / STATE.md final updates
</remaining_work>

<decisions_made>

- Consolidated 5 dashboard apps into 1 unified ops-dashboard on port 3011
- Sales board stays standalone (public/no-auth)
- Old app directories deleted (auth-portal, manager-dashboard, owner-dashboard, payroll-dashboard, cs-dashboard)
</decisions_made>

<blockers>

1. **Railway ops-dashboard build fails**: Railpack detects `AUTH_JWT_SECRET` in code and requires it as a Docker build secret. Even with the variable set in Railway, the build fails with `secret AUTH_JWT_SECRET: not found`. Attempted workaround: prefix build command with `AUTH_JWT_SECRET=temp`.

2. **Login returns invalid credentials / silent redirect**: After seeding via Railway start command, login still fails. Root cause: the seed ran via shell command where `$` characters in bcrypt hashes got interpolated. Manually updated hash via SQL UPDATE but login still failing — likely AUTH_JWT_SECRET mismatch between ops-api (signs token) and ops-dashboard (middleware verifies token).

3. **ops-api start command**: Was temporarily changed to include seed (`npx tsx prisma/seed.ts && npm run start --workspace=apps/ops-api`). Needs to be reverted to `npm run start --workspace=apps/ops-api`.
</blockers>

<context>
The entire codebase migration is done — this is purely a Railway deployment issue. The key debugging path:
1. Fix the Railpack AUTH_JWT_SECRET build secret issue (try prefixing build cmd with env var)
2. Ensure ops-api start command is correct (`npm run start --workspace=apps/ops-api`)
3. Verify AUTH_JWT_SECRET is identical between ops-api and ops-dashboard Railway services
4. The "no error" login behavior = middleware silently rejecting the JWT and redirecting back to `/`
</context>

<next_action>
Start with: Fix Railway ops-dashboard build by prefixing build command with `AUTH_JWT_SECRET=temp`. Then verify ops-api is running correctly. Then confirm AUTH_JWT_SECRET matches between both services. Test login flow.
</next_action>
