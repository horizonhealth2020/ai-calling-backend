---
phase: 47-sale-entry-payroll-ui-chargeback-fixes-bundle-standalone-aca
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx
autonomous: false
requirements: [D-04, D-05, D-06, D-07]
must_haves:
  truths:
    - "When loading the Payroll Periods tab at 1080p resolution, the first agent pay card (AgentCard) is visible immediately below the date range header WITHOUT scrolling"
    - "Pay cards collectively occupy more than 1/3 of the visible viewport height"
    - "Date range header remains visible"
    - "Net Payout period KPI remains visible"
    - "Lock/Unlock/Export/Print action buttons remain visible"
    - "Phase 46 Chargebacks (N) badge remains visible"
  artifacts:
    - path: "apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx"
      provides: "Compressed header/ticker layout prioritizing pay cards"
  key_links:
    - from: "StatMini component (lines 46-67)"
      to: "new compressed padding"
      via: "inline React.CSSProperties style object"
      pattern: "padding.*8px 12px|StatMini"
---

<objective>
Fix Sub-feature 2: shrink the top UI chrome (chargeback banner + current-week summary strip + StatMini grid) in PayrollPeriods.tsx so pay cards become the dominant visual element per user feedback on image.png. KPIs stay visible but compressed; pay cards visible immediately below date range at 1080p without scrolling.

Purpose: Payroll staff use pay cards as the primary verification tool — they should NOT be pushed below the fold by the period summary chrome.
Output: PayrollPeriods.tsx with tighter padding, smaller fonts on StatMini, and reduced vertical margins throughout the top chrome region.
</objective>

<context>
@CLAUDE.md
@.planning/phases/47-sale-entry-payroll-ui-chargeback-fixes-bundle-standalone-aca/47-CONTEXT.md
@.planning/phases/47-sale-entry-payroll-ui-chargeback-fixes-bundle-standalone-aca/47-RESEARCH.md
@image.png
@apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx

<interfaces>
Target region (per research against PayrollPeriods.tsx):
- Lines ~884-1173: Chargeback alerts banner card
  - Container: padding: S[4] (16px), borderRadius: R["2xl"] (24px)
  - When alerts.length === 0: one-line empty state
- Lines ~1175-1191: Current-week summary strip
  - Outer marginBottom: S[5] (20px)
  - Date-range header: fontSize: 12, marginBottom: S[2] (8px)
  - Grid: repeat(6, 1fr), gap S[3] (12px), 6 StatMini cards
- StatMini (lines 46-67):
  - padding: "14px 16px"
  - gap: 4
  - label fontSize: 10
  - value rendered via <AnimatedNumber>
- Lines ~1193-1282: AgentCard rendering (DO NOT TOUCH)

Theme tokens from @ops/ui:
- S[2]=8, S[3]=12, S[4]=16, S[5]=20 (spacing scale)
- R["2xl"]=24, R.md=8 (radius)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Compress StatMini + summary strip + chargeback banner padding</name>
  <files>apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx</files>
  <read_first>
    - apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx (lines 1-100 for StatMini; lines 880-1200 for banner + summary strip; grep for "StatMini" and "Current week" or the summary strip markers)
    - image.png at repo root (reference: current spacing problem)
    - .planning/phases/47-sale-entry-payroll-ui-chargeback-fixes-bundle-standalone-aca/47-CONTEXT.md (D-04, D-05, D-06, D-07)
    - .planning/phases/47-sale-entry-payroll-ui-chargeback-fixes-bundle-standalone-aca/47-RESEARCH.md (Sub-feature 2 section — concrete current values + target values)
  </read_first>
  <action>
Apply these CONCRETE value changes (use actual S[]/R[] tokens from the imports already in the file):

**Change A — StatMini component (lines ~46-67):**
- `padding: "14px 16px"` → `padding: "8px 12px"`
- `gap: 4` → `gap: 2`
- label `fontSize: 10` → `fontSize: 9` with `letterSpacing: 0.4`
- If StatMini renders `<AnimatedNumber>` or similar, pass a smaller fontSize override (e.g. `valueStyle={{ fontSize: 18 }}` or equivalent) so the number shrinks from its current default (likely 22-24px) down to ~18px
- If StatMini has a `minHeight`, remove it or reduce by ~30%

**Change B — Current-week summary strip outer container (lines ~1175-1191):**
- Outer `marginBottom: S[5]` (20px) → `marginBottom: S[3]` (12px)
- Date-range header `marginBottom: S[2]` (8px) → `marginBottom: 4`
- Date-range header `fontSize: 12` → keep 12 (stay compact but legible — per D-06 must remain visible)
- Grid `gap: S[3]` (12px) → `gap: S[2]` (8px)

**Change C — Chargeback alerts banner card (lines ~884-1173):**
- Empty state (alerts.length === 0) container: `padding: S[4]` (16px) → `padding: S[3]` (12px)
- Empty state `borderRadius: R["2xl"]` (24px) → `borderRadius: R.lg` (16px) (still rounded but less heavy)
- Expanded state (alerts.length > 0): KEEP `padding: S[4]` — expansion is a deliberate focus state and should stay roomy
- Do NOT remove the Phase 46 `Chargebacks (N)` badge — it must remain visible per D-06

**Change D — Keep all D-06 elements visible:**
- Date range header: still rendered, still shows e.g. `04-20-2026 → 04-19-2026`
- Net Payout stat card: still rendered (this is one of the 6 StatMini cards — don't remove)
- Lock/Unlock/Export/Print buttons: unchanged
- `Chargebacks (N)` badge: unchanged (only the banner padding shrinks)

**Do NOT:**
- Remove any StatMini cards (keep all 6 — shrink them, don't delete)
- Remove the chargeback banner entirely
- Switch to Tailwind classes (inline React.CSSProperties ONLY per CLAUDE.md)
- Touch the AgentCard rendering at lines ~1193+ (out of scope)
- Use fixed pixel widths — stay with grid templates and responsive behavior
  </action>
  <verify>
    <automated>npm run build --workspace=apps/ops-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "StatMini" apps/ops-dashboard/app/\(dashboard\)/payroll/PayrollPeriods.tsx` shows StatMini is still defined and all 6 usages remain
    - `grep -n "8px 12px\|padding: 8" apps/ops-dashboard/app/\(dashboard\)/payroll/PayrollPeriods.tsx` returns at least 1 match (the new compressed StatMini padding)
    - `grep -n "Chargebacks" apps/ops-dashboard/app/\(dashboard\)/payroll/PayrollPeriods.tsx` still shows the `Chargebacks (N)` badge rendering (D-06 compliance)
    - `grep -n "Net Payout\|netPayout\|Net Total" apps/ops-dashboard/app/\(dashboard\)/payroll/PayrollPeriods.tsx` still returns matches (D-06 compliance)
    - `npm run build --workspace=apps/ops-dashboard` exits with code 0
    - Visual checkpoint: at 1080p, first AgentCard top edge is within 400px of the viewport top (was >600px previously per image.png)
  </acceptance_criteria>
  <done>PayrollPeriods.tsx renders with tighter chrome; StatMini shows compressed padding; all D-06 elements still visible; build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify spacing against image.png</name>
  <what-built>PayrollPeriods.tsx header + ticker chrome shrunk to give pay cards dominant screen real estate per D-04/D-07.</what-built>
  <how-to-verify>
1. Run `npm run dashboard:dev` in one terminal and `npm run ops:dev` in another
2. Open http://localhost:3000/payroll/periods in a browser window sized to 1920x1080
3. Visually compare against image.png at repo root
4. Confirm:
   - First pay card (AgentCard) top edge is visible in the top ~40% of the viewport
   - Date range header still visible
   - Net Payout KPI still visible
   - Lock/Unlock/Export/Print buttons still visible
   - Chargebacks (N) badge still visible (click a period with active alerts if needed)
5. Screenshot the result and verify pay cards dominate (>1/3 of visible vertical space)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe adjustments needed (e.g., "StatMini padding too tight, bump to 10px 14px")</resume-signal>
</task>

</tasks>

<verification>
- Build: `npm run build --workspace=apps/ops-dashboard` exits 0
- Visual: pay cards visible above the fold at 1080p
- All D-06 elements present
</verification>

<success_criteria>
1. StatMini padding reduced per Task 1 Change A
2. Summary strip margin reduced per Task 1 Change B
3. Chargeback banner empty-state padding reduced per Task 1 Change C
4. All D-06 elements remain visible
5. Human checkpoint approved against image.png comparison
</success_criteria>

<output>
After completion, create `.planning/phases/47-sale-entry-payroll-ui-chargeback-fixes-bundle-standalone-aca/47-02-SUMMARY.md`
</output>
