---
phase: 35-fix-kpi-polling-issues-and-manager-dashboard-features
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx
  - apps/ops-dashboard/app/(dashboard)/manager/page.tsx
  - apps/ops-dashboard/app/(dashboard)/owner/OwnerKPIs.tsx
  - apps/ops-dashboard/app/(dashboard)/owner/OwnerOverview.tsx
  - apps/ops-dashboard/app/(dashboard)/owner/OwnerScoring.tsx
  - apps/ops-dashboard/app/(dashboard)/cs/CSTracking.tsx
  - apps/ops-dashboard/app/(dashboard)/layout.tsx
  - apps/ops-dashboard/lib/DateRangeContext.tsx
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-09, D-10]

must_haves:
  truths:
    - "Each dashboard manages its own date range state independently"
    - "Switching tabs resets date range to that dashboard's default"
    - "Manager Tracker defaults to Today preset"
    - "Owner KPIs defaults to Today preset"
    - "Owner Overview and Owner Scoring default to Current Week preset"
    - "CS Tracking defaults to Current Week preset"
    - "Today column is removed from Manager Tracker table and CSV export"
  artifacts:
    - path: "apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx"
      provides: "Local date range state (today default), no Today column"
      contains: "useState<DateRangeFilterValue>"
    - path: "apps/ops-dashboard/app/(dashboard)/owner/OwnerKPIs.tsx"
      provides: "Local date range state (today default)"
      contains: 'preset: "today"'
    - path: "apps/ops-dashboard/app/(dashboard)/layout.tsx"
      provides: "DateRangeProvider removed from wrapper"
  key_links:
    - from: "apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx"
      to: "DateRangeFilter component"
      via: "Local useState instead of useDateRange context"
      pattern: "useState.*DateRangeFilterValue.*today"
    - from: "apps/ops-dashboard/app/(dashboard)/layout.tsx"
      to: "DashboardInner"
      via: "No longer wrapped in DateRangeProvider"
      pattern: "SocketProvider.*DashboardInner"
---

<objective>
Scope date range state per dashboard (removing global DateRangeProvider), remove the redundant Today column from Manager Tracker, and set correct defaults per dashboard.

Purpose: The global DateRangeProvider causes custom date ranges to persist when switching between Manager/Owner/CS tabs. Each dashboard needs independent state with appropriate defaults. The Today column in Manager Tracker is redundant once "Today" is a date range filter preset (added in Plan 01).
Output: Independent date range state per dashboard, Today column removed from Manager Tracker, correct per-dashboard defaults.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-fix-kpi-polling-issues-and-manager-dashboard-features/35-CONTEXT.md
@.planning/phases/35-fix-kpi-polling-issues-and-manager-dashboard-features/35-RESEARCH.md
@.planning/phases/35-fix-kpi-polling-issues-and-manager-dashboard-features/35-01-SUMMARY.md

<interfaces>
<!-- Types the executor needs -->

From packages/ui/src/components/DateRangeFilter.tsx:
```typescript
export interface DateRangeFilterValue {
  preset: string;
  from?: string;
  to?: string;
}
```

From apps/ops-dashboard/lib/DateRangeContext.tsx (to be removed):
```typescript
export function DateRangeProvider({ children }: { children: React.ReactNode })
export const useDateRange = () => useContext(DateRangeContext);
```

Current useDateRange consumers (all must switch to local useState):
1. ManagerTracker.tsx:101 — `const { value: dateRangeCtx, onChange: setDateRangeCtx } = useDateRange();`
2. OwnerKPIs.tsx:206 — `const { value: dateRange, onChange: setDateRange } = useDateRange();`
3. OwnerOverview.tsx:352 — `const { value: dateRange, onChange: setDateRange } = useDateRange();`
4. OwnerScoring.tsx:289 — `const { value: dateRange, onChange: setDateRange } = useDateRange();`
5. CSTracking.tsx:168 — `const { value: dateRange, onChange: setDateRange } = useDateRange();`

TrackerEntry type in manager/page.tsx:33:
```typescript
type TrackerEntry = { agent: string; salesCount: number; premiumTotal: number; totalLeadCost: number; costPerSale: number; commissionTotal: number; todaySalesCount: number; todayPremium: number };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace global DateRangeProvider with per-dashboard local state</name>
  <files>
    apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx
    apps/ops-dashboard/app/(dashboard)/owner/OwnerKPIs.tsx
    apps/ops-dashboard/app/(dashboard)/owner/OwnerOverview.tsx
    apps/ops-dashboard/app/(dashboard)/owner/OwnerScoring.tsx
    apps/ops-dashboard/app/(dashboard)/cs/CSTracking.tsx
    apps/ops-dashboard/app/(dashboard)/layout.tsx
    apps/ops-dashboard/lib/DateRangeContext.tsx
  </files>
  <read_first>
    - apps/ops-dashboard/app/(dashboard)/layout.tsx (lines 186-195 for DateRangeProvider wrapper)
    - apps/ops-dashboard/lib/DateRangeContext.tsx (full file — will be deleted)
    - apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx (line 18 import, line 101 usage)
    - apps/ops-dashboard/app/(dashboard)/owner/OwnerKPIs.tsx (line 20 import, line 206 usage)
    - apps/ops-dashboard/app/(dashboard)/owner/OwnerOverview.tsx (line 21 import, line 352 usage)
    - apps/ops-dashboard/app/(dashboard)/owner/OwnerScoring.tsx (line 19 import, line 289 usage)
    - apps/ops-dashboard/app/(dashboard)/cs/CSTracking.tsx (line 26 import, line 168 usage)
  </read_first>
  <action>
**In each of the 5 consumer files, make these exact changes:**

**ManagerTracker.tsx:**
- Remove: `import { useDateRange } from "@/lib/DateRangeContext";` (line 18)
- Ensure `useState` is imported from "react" (already imported at line 1)
- Ensure `DateRangeFilterValue` type is imported: `import type { DateRangeFilterValue } from "@ops/ui";` (may need adding alongside existing @ops/ui imports)
- Replace line 101: `const { value: dateRangeCtx, onChange: setDateRangeCtx } = useDateRange();`
  With: `const [dateRangeCtx, setDateRangeCtx] = useState<DateRangeFilterValue>({ preset: "today" });`
- Default: `"today"` (per D-10)

**OwnerKPIs.tsx:**
- Remove: `import { useDateRange } from "@/lib/DateRangeContext";` (line 20)
- Add `useState` import if not present, add `DateRangeFilterValue` type import from `@ops/ui`
- Replace line 206: `const { value: dateRange, onChange: setDateRange } = useDateRange();`
  With: `const [dateRange, setDateRange] = useState<DateRangeFilterValue>({ preset: "today" });`
- Default: `"today"` (per D-08)

**OwnerOverview.tsx:**
- Remove: `import { useDateRange } from "@/lib/DateRangeContext";` (line 21)
- Add `DateRangeFilterValue` type import from `@ops/ui` if not present
- Replace line 352: `const { value: dateRange, onChange: setDateRange } = useDateRange();`
  With: `const [dateRange, setDateRange] = useState<DateRangeFilterValue>({ preset: "week" });`
- Default: `"week"` (per D-10)

**OwnerScoring.tsx:**
- Remove: `import { useDateRange } from "@/lib/DateRangeContext";` (line 19)
- Add `DateRangeFilterValue` type import from `@ops/ui` if not present
- Replace line 289: `const { value: dateRange, onChange: setDateRange } = useDateRange();`
  With: `const [dateRange, setDateRange] = useState<DateRangeFilterValue>({ preset: "week" });`
- Default: `"week"` (per D-10)

**CSTracking.tsx:**
- Remove: `import { useDateRange } from "@/lib/DateRangeContext";` (line 26)
- Add `DateRangeFilterValue` type import from `@ops/ui` if not present
- Replace line 168: `const { value: dateRange, onChange: setDateRange } = useDateRange();`
  With: `const [dateRange, setDateRange] = useState<DateRangeFilterValue>({ preset: "week" });`
- Default: `"week"` (per D-10)

**layout.tsx:**
- Remove: `import { DateRangeProvider } from "@/lib/DateRangeContext";` (near top imports)
- Remove the `<DateRangeProvider>` and `</DateRangeProvider>` wrapper at lines 188/192, leaving the structure as:
```tsx
<SocketProvider apiUrl={apiUrl}>
  <DashboardInner tabs={tabs}>
    {children}
  </DashboardInner>
</SocketProvider>
```

**DateRangeContext.tsx:**
- Delete the file `apps/ops-dashboard/lib/DateRangeContext.tsx` entirely (all consumers migrated to local state)
  </action>
  <verify>
    <automated>grep -rn "useDateRange\|DateRangeProvider\|DateRangeContext" apps/ops-dashboard/app/ apps/ops-dashboard/lib/ 2>/dev/null | grep -v node_modules | grep -v ".next"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `useDateRange` in any file under `apps/ops-dashboard/app/`
    - Zero occurrences of `DateRangeProvider` in any file under `apps/ops-dashboard/app/`
    - File `apps/ops-dashboard/lib/DateRangeContext.tsx` does not exist
    - ManagerTracker.tsx contains `useState<DateRangeFilterValue>({ preset: "today" })`
    - OwnerKPIs.tsx contains `useState<DateRangeFilterValue>({ preset: "today" })`
    - OwnerOverview.tsx contains `useState<DateRangeFilterValue>({ preset: "week" })`
    - OwnerScoring.tsx contains `useState<DateRangeFilterValue>({ preset: "week" })`
    - CSTracking.tsx contains `useState<DateRangeFilterValue>({ preset: "week" })`
    - layout.tsx does not contain `DateRangeProvider`
  </acceptance_criteria>
  <done>All 5 dashboard components use local useState for date range, global provider removed, correct defaults per dashboard</done>
</task>

<task type="auto">
  <name>Task 2: Remove Today column from Manager Tracker table and CSV export</name>
  <files>
    apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx
    apps/ops-dashboard/app/(dashboard)/manager/page.tsx
  </files>
  <read_first>
    - apps/ops-dashboard/app/(dashboard)/manager/page.tsx (line 33 for TrackerEntry type)
    - apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx (line 64 for CSV headers, lines 66-69 for CSV data, line 148 for table header array, lines 190-193 for Today td cell)
  </read_first>
  <action>
**In manager/page.tsx:**
- Remove `todaySalesCount: number; todayPremium: number` from the `TrackerEntry` type definition at line 33.
  The type becomes:
  ```typescript
  type TrackerEntry = { agent: string; salesCount: number; premiumTotal: number; totalLeadCost: number; costPerSale: number; commissionTotal: number };
  ```

**In ManagerTracker.tsx:**

1. **CSV export** (function `exportAgentPerformanceCSV`, lines 62-82):
   - Change header row from `["Agent", "Today Sales", "Today Premium", "Sales Count", "Commission Earned", "Premium Total", "Lead Cost", "Cost Per Sale"]`
     to: `["Agent", "Sales Count", "Commission Earned", "Premium Total", "Lead Cost", "Cost Per Sale"]`
   - Remove from the data push: `String(t.todaySalesCount),` and `t.todayPremium.toFixed(2),`
   - The data push becomes:
     ```typescript
     rows.push([
       esc(t.agent),
       String(t.salesCount),
       t.commissionTotal.toFixed(2),
       t.premiumTotal.toFixed(2),
       t.totalLeadCost.toFixed(2),
       t.costPerSale.toFixed(2),
     ]);
     ```

2. **Table header** (line 148):
   - Change from: `["Rank", "Agent", "Today", "Calls", "Sales", "Premium Total", "Lead Spend", "Cost / Sale"]`
     to: `["Rank", "Agent", "Calls", "Sales", "Premium Total", "Lead Spend", "Cost / Sale"]`
   - Update the `textAlign` logic: `textAlign: i >= 2 ? "right" : "left"` stays the same (Calls is now at index 2, still right-aligned)

3. **Table body Today cell** (lines 190-193):
   - Remove the entire `<td>` block that renders `row.todaySalesCount` and `row.todayPremium`:
     ```tsx
     <td style={{ ...baseTdStyle, textAlign: "right", fontWeight: 600 }}>
       {row.todaySalesCount > 0
         ? <span ...>{row.todaySalesCount} <span ...>(${row.todayPremium.toLocaleString()})</span></span>
         : <span ...>{"\u2014"}</span>}
     </td>
     ```
     Delete this entire `<td>...</td>` block.
  </action>
  <verify>
    <automated>grep -n "todaySalesCount\|todayPremium\|Today Sales\|Today Premium" apps/ops-dashboard/app/\(dashboard\)/manager/ManagerTracker.tsx apps/ops-dashboard/app/\(dashboard\)/manager/page.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `todaySalesCount` in ManagerTracker.tsx
    - Zero occurrences of `todayPremium` in ManagerTracker.tsx
    - Zero occurrences of `todaySalesCount` in manager/page.tsx
    - Zero occurrences of `todayPremium` in manager/page.tsx
    - Table header array has exactly 7 entries: Rank, Agent, Calls, Sales, Premium Total, Lead Spend, Cost / Sale
    - CSV header array has exactly 6 entries: Agent, Sales Count, Commission Earned, Premium Total, Lead Cost, Cost Per Sale
    - No `"Today"` string in the table header array
  </acceptance_criteria>
  <done>Today column removed from Manager Tracker table and CSV export, TrackerEntry type updated</done>
</task>

</tasks>

<verification>
- `grep -rn "useDateRange\|DateRangeProvider" apps/ops-dashboard/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v ".next"` returns no results
- `grep -rn "todaySalesCount\|todayPremium" apps/ops-dashboard/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v ".next"` returns no results
- `grep 'preset: "today"' apps/ops-dashboard/app/\(dashboard\)/manager/ManagerTracker.tsx apps/ops-dashboard/app/\(dashboard\)/owner/OwnerKPIs.tsx` shows both files
- `grep 'preset: "week"' apps/ops-dashboard/app/\(dashboard\)/owner/OwnerOverview.tsx apps/ops-dashboard/app/\(dashboard\)/owner/OwnerScoring.tsx apps/ops-dashboard/app/\(dashboard\)/cs/CSTracking.tsx` shows all three files
- `npm run dashboard:dev` compiles without errors
</verification>

<success_criteria>
1. Each dashboard component has independent local date range state
2. Manager Tracker and Owner KPIs default to "today" preset
3. Owner Overview, Owner Scoring, and CS Tracking default to "week" preset
4. Global DateRangeProvider removed from layout.tsx
5. DateRangeContext.tsx file deleted
6. Today column removed from Manager Tracker table display
7. Today columns removed from Manager Tracker CSV export
8. TrackerEntry type no longer includes todaySalesCount/todayPremium
</success_criteria>

<output>
After completion, create `.planning/phases/35-fix-kpi-polling-issues-and-manager-dashboard-features/35-02-SUMMARY.md`
</output>
