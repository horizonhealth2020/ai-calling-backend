---
phase: 46
plan_id: 46-P03
plan: 3
type: execute
wave: 2
depends_on: [46-P02]
files_modified:
  - apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The payroll period header area shows a single collapsed `Chargebacks (N)` badge instead of a per-chargeback stack"
    - "Clicking the badge expands an inline panel (not a modal) showing each chargeback row"
    - "Each expanded row shows member name, agent name, chargeback amount, and product selection chips matching the chargeback form UI"
    - "Each expanded row preserves existing approve / dismiss / clawback action buttons"
    - "Count format `Chargebacks (1)` is used even when N=1; the panel does not auto-expand"
  artifacts:
    - path: "apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx"
      provides: "Collapsed Chargebacks badge + expandable inline panel"
      contains: "Chargebacks ("
  key_links:
    - from: "Chargebacks (N) badge"
      to: "inline expand panel state"
      via: "useState boolean toggle"
      pattern: "setExpanded|showChargebacks"
    - from: "expand panel row"
      to: "lifted product chip component"
      via: "reused from chargeback form chip UI"
      pattern: "ProductChip|product chip"
---

<objective>
Sub-feature 3: Collapse the existing per-chargeback alert stack in the payroll period header area into a single `Chargebacks (N)` badge that expands inline to show each chargeback with member name, agent name, amount, and the exact same product selection chips used by the chargeback creation form. Action buttons (approve / dismiss / clawback) remain on each row.

Purpose: Chargeback alerts create visual noise stacked individually. A grouped badge gives payroll staff a clean header and drill-down for details. Lookup supports identifying whose sale a chargeback applies to at a glance.
Output: Updated PayrollChargebacks.tsx with collapsed badge + expandable panel + lifted chip component.

Depends on 46-P02 because the collapsed badge consumes the alert stream that P02 repairs — if P02 is wrong, P03 badge will show incomplete counts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-CONTEXT.md
@apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx

<interfaces>
Existing PayrollChargebacks.tsx:
- Renders the per-chargeback alert stack today in the payroll period header
- Contains the chargeback creation form that uses a product-selection chip UI
- Each chargeback row has approve / dismiss / clawback buttons
- Styling uses inline React.CSSProperties constants (CLAUDE.md)

Data available on each chargeback alert (confirmed via PayrollChargebacks fetch):
- `chargeback.amount`
- `chargeback.sale.memberName` (joined via matched sale)
- `chargeback.sale.agent.name`
- `chargeback.sale.product` + `chargeback.sale.addons` (for chip rendering)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Lift reusable ProductChipRow component and add collapsed Chargebacks (N) badge</name>
  <files>apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx</files>
  <read_first>
    - apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx (full file — find: (a) the chargeback form's product selection chip rendering, (b) the current alert stack render, (c) action button handlers)
    - .planning/phases/46-*/46-CONTEXT.md (D-09, D-10, D-11, D-12)
  </read_first>
  <action>
    In apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx:

    1. **Extract the product chip rendering** used by the chargeback creation form into a reusable inline component (or memoized helper) named `ProductChipRow`. It accepts `{ sale, selectedProductIds }` and renders the same chip visual — product name + premium styling, same CSS constants. Confirm by grep that the form's render tree still uses this component (no visual regression on the form).

    2. **Replace the current per-chargeback alert stack render** with a single badge component:
       - Text content: `Chargebacks (${chargebacks.length})` — exact format, including parens and space
       - Always rendered when `chargebacks.length > 0` (do not hide at 1)
       - Badge styled with existing color tone for alerts (e.g., `C.warning` / `C.danger` from the file's constants — use whatever is already defined)
       - Click toggles a `showChargebacks` boolean local state (useState), default `false` (do NOT auto-expand at count of 1 per D-12)

    3. **Render the inline expand panel** below the badge when `showChargebacks` is true. This is NOT a modal. Use a plain div with top margin + border matching existing panel style:
       - Map `chargebacks` to rows
       - Each row shows, left-to-right:
         - Member name: `{chargeback.sale?.memberName ?? '—'}`
         - Agent name: `{chargeback.sale?.agent?.name ?? '—'}`
         - Amount: formatted as USD (use existing currency formatter in the file)
         - `<ProductChipRow sale={chargeback.sale} selectedProductIds={chargeback.productIds ?? []} />`
         - Existing approve / dismiss / clawback buttons (unchanged handlers — do NOT remove or relocate handler logic, only move their render into the row)

    4. **Empty state:** if `chargebacks.length === 0`, render nothing at all (no badge).

    5. All new UI stays inline React.CSSProperties. No Tailwind. No globals.css. No className utility classes (CLAUDE.md).

    6. Preserve existing Socket.IO subscription and fetch logic — only the render layer changes. Any useState guards (e.g., selectedAgentId from STATE.md Phase 41 pitfall) remain intact.
  </action>
  <verify>
    <automated>grep -q 'Chargebacks (' "apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx" && grep -q 'ProductChipRow' "apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx" && grep -q 'showChargebacks\|setShowChargebacks' "apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'Chargebacks (' apps/ops-dashboard/app/\(dashboard\)/payroll/PayrollChargebacks.tsx
    - grep -q 'ProductChipRow' apps/ops-dashboard/app/\(dashboard\)/payroll/PayrollChargebacks.tsx (lifted component exists and is used)
    - grep -q 'useState' apps/ops-dashboard/app/\(dashboard\)/payroll/PayrollChargebacks.tsx in proximity to showChargebacks
    - grep -q 'memberName' AND 'agent' AND 'amount' in the new expand panel block
    - Existing approve/dismiss/clawback handlers (search for their existing function names) are still referenced in render
    - No Tailwind / className= utility classes introduced
    - `npm run lint` (or dashboard typecheck) exits 0 for this file
  </acceptance_criteria>
  <done>
    Payroll period header shows `Chargebacks (N)` badge when N>0, expands inline on click showing member/agent/amount/chips/actions, reuses the chargeback form chip UI, preserves all existing actions and handlers.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — collapsed chargeback badge behaves per D-09..D-12</name>
  <what-built>Collapsed Chargebacks (N) badge with inline expand panel in PayrollChargebacks.tsx</what-built>
  <how-to-verify>
    1. Navigate to payroll dashboard with a period that has 3+ open chargebacks. Confirm: header shows a single `Chargebacks (3)` badge (or whatever count), NOT a stack.
    2. Click the badge. Confirm: inline panel expands below (not a modal popup).
    3. Verify each row shows: member name, agent name, amount, product chips matching the chargeback form style, and all three action buttons (approve, dismiss, clawback).
    4. Click approve on one row → action still works end-to-end (same behavior as before minimization).
    5. Navigate to a period with exactly 1 chargeback. Confirm: badge reads `Chargebacks (1)`, panel is collapsed by default (not auto-expanded).
    6. Navigate to a period with 0 chargebacks. Confirm: no badge shown.
    7. Trigger a CS chargeback (from P02) → badge count increments.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Single `Chargebacks (N)` badge replaces prior stack
- Expand panel renders per D-10 with member/agent/amount/chips/actions
- Actions unregressed
- Count format used for N=1, panel not auto-expanded
- Integration with P02's CS-origin alerts works (count includes CS-submitted chargebacks)
</verification>

<success_criteria>
- D-09..D-12 all satisfied
- No Tailwind introduced
- Lint/type-check clean
- Existing Socket.IO + fetch logic preserved
</success_criteria>

<output>
Create `.planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-03-SUMMARY.md` after completion.
</output>
