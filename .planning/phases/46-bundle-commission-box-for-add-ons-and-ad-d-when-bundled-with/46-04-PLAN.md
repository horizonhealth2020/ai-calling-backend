---
phase: 46
plan_id: 46-P04
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "printAgentCards renders the ACA chip inline in the Core column for any entry where entry.acaAttached is set"
    - "The ACA chip uses a print-friendly style mirroring .prod-block / .prod-name with a distinct ACA color tone (teal/info)"
    - "No new ACA column is added; bucket logic for CORE/ADDON/AD_D is unchanged"
    - "Sammy Machado's 04-05 → 04-11 week print shows the ACA chip inside the Core column"
  artifacts:
    - path: "apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx"
      provides: "printAgentCards inline ACA chip in Core column"
      contains: "acaAttached"
  key_links:
    - from: "printAgentCards core column render"
      to: "entry.acaAttached"
      via: "inline chip emitted alongside core .prod-block"
      pattern: "acaAttached"
---

<objective>
Sub-feature 4: The payroll card (WeekSection.tsx:272-279) shows an ACA chip next to the core product for any entry where `acaAttached` is set. The print view (`printAgentCards` at PayrollPeriods.tsx:697) does NOT render this chip. Add it inline to the Core column in print HTML.

Purpose: Print view must match the on-screen payroll card so printed rows are unambiguous when reviewing payroll offline. Sammy Machado's 04-05 → 04-11 week is the visual test case.
Output: Updated printAgentCards emitting an ACA chip inside the Core column.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-CONTEXT.md
@apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx
@apps/ops-dashboard/app/(dashboard)/payroll/WeekSection.tsx

<interfaces>
From CONTEXT.md canonical_refs:
- WeekSection.tsx lines 248-279: on-screen ACA chip render — shows `entry.acaAttached` next to core product using `ACA_BADGE` style + `Badge` component
- PayrollPeriods.tsx `printAgentCards` lines 697-795: print HTML generator, inline `<style>` block, PX-based layout, existing classes `.prod-block` / `.prod-name` / `.prod-premium`
- Bucket logic lines 758-784 iterates ENTRIES into CORE/ADDON/AD_D columns

Rule from D-15: ACA chip is rendered as an **addition to the existing Core cell**, NOT via the type buckets. Do not modify bucket logic. Do not add a new column.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Emit inline ACA chip inside Core column of printAgentCards</name>
  <files>apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx</files>
  <read_first>
    - apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx (lines 680-800 — full printAgentCards function including inline style block and bucket render)
    - apps/ops-dashboard/app/(dashboard)/payroll/WeekSection.tsx (lines 248-279 — ACA badge render to mirror visually)
    - .planning/phases/46-*/46-CONTEXT.md (D-13, D-14, D-15, D-16)
    - .planning/phases/45-*/45-CONTEXT.md (GAP-45-07 acaAttached introduction)
  </read_first>
  <action>
    In apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx, locate `printAgentCards` (starting ~line 697).

    1. **Extend the inline `<style>` block** to add a new class `.prod-aca`. Style should mirror `.prod-block` dimensions/font and use a distinct ACA color tone:
       - Background: teal/info (e.g. `#0ea5b7` color with light bg `#e0f7fa` or similar — follow the existing Badge info tone in dashboard `C.info` constant for the on-color print; also must read clearly on black-and-white print, so border + dark text fallback is required)
       - Example:
         ```css
         .prod-aca {
           display: inline-block;
           padding: 2px 6px;
           margin-left: 4px;
           border: 1px solid #0891b2;
           background: #e0f7fa;
           color: #0c4a5f;
           font-size: 10px;
           font-weight: 600;
           border-radius: 3px;
         }
         ```
         (Exact color values are Claude's discretion per D-14 — match what reads clearly on B/W and color.)

    2. **Inside the Core column render** (within the bucket loop where type === "CORE" entries are emitted as `.prod-block` elements), AFTER emitting the existing `.prod-block` HTML for the core entry, check if that entry has `entry.acaAttached` truthy. If so, append an inline chip:

       ```html
       <span class="prod-aca">ACA</span>
       ```

       Or `<span class="prod-aca">${escapedLabel}</span>` where label comes from the acaAttached object if it contains a name (e.g., child product name from Phase 45 GAP-45-07 "render ACA as its own product on payroll and sales tracker"). If acaAttached is a boolean, emit text "ACA". If it's an object with a name field, use the name — grep WeekSection.tsx:272-279 to match the exact source text used on-screen.

    3. **Do NOT add a new column.** Do NOT modify the CORE/ADDON/AD_D bucket classification logic on lines 758-784. Do NOT touch type buckets.

    4. **Do NOT** add the chip anywhere except adjacent to the Core entry in the Core column.

    5. Ensure the chip is escaped via the same HTML-escape helper used elsewhere in printAgentCards (the function already escapes user-provided names — reuse it).
  </action>
  <verify>
    <automated>grep -q 'acaAttached' "apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx" && grep -q 'prod-aca' "apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'acaAttached' apps/ops-dashboard/app/\(dashboard\)/payroll/PayrollPeriods.tsx (new reference in printAgentCards body)
    - grep -q 'prod-aca' apps/ops-dashboard/app/\(dashboard\)/payroll/PayrollPeriods.tsx (new CSS class + chip render)
    - The grep match for 'prod-aca' appears inside the printAgentCards function (not outside — visually inspect)
    - Bucket loop lines 758-784 remain semantically unchanged (no additional type added to bucketing)
    - `npm run lint` (or dashboard typecheck) exits 0 for this file
    - No new column headers added to the print HTML table
  </acceptance_criteria>
  <done>
    printAgentCards emits a print-friendly ACA chip inline inside the Core column for every entry where `entry.acaAttached` is set, styled with `.prod-aca` class.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Sammy Machado print shows ACA chip (D-16)</name>
  <what-built>Print view ACA chip parity inside the Core column of printAgentCards.</what-built>
  <how-to-verify>
    1. Navigate to payroll dashboard, week 04-05 → 04-11.
    2. Open Sammy Machado's agent card — confirm the ACA chip is visible next to the core product on screen (baseline).
    3. Click the Print action (invoked via AgentCard.tsx `onPrintWeek`).
    4. In the print preview, find Sammy Machado's row.
    5. Verify: the ACA chip is now visible in the Core column, inline next to the core product name, using the teal/info-toned `.prod-aca` style.
    6. Switch print preview to black-and-white mode (or simulated grayscale) → verify the chip is still readable (border + dark text fallback).
    7. Verify: no new column was added; existing CORE/ADDON/AD_D columns unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g., "chip not visible in print", "column layout broke")</resume-signal>
</task>

</tasks>

<verification>
- On-screen payroll card still shows ACA chip (no regression to WeekSection.tsx)
- Print view shows ACA chip inline in Core column for Sammy Machado 04-05 → 04-11
- Chip readable on color + B/W print
- No new column, no bucket logic changes
</verification>

<success_criteria>
D-13..D-16 all satisfied. Grep confirms `prod-aca` + `acaAttached` present in printAgentCards. Human verification passes.
</success_criteria>

<output>
Create `.planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-04-SUMMARY.md` after completion.
</output>
