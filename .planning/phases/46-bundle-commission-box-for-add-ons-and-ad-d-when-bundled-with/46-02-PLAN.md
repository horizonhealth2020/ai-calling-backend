---
phase: 46
plan_id: 46-P02
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ops-api/src/routes/chargebacks.ts
  - apps/ops-api/src/services/alerts.ts
  - apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx
  - .planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-DIAGNOSIS.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "A chargeback submitted from the CS dashboard for a member matching a sale produces an alert visible in the payroll dashboard"
    - "The alert reaches the agent who owns the matched sale, surfaced in the same alert area where payroll-originated chargebacks already work"
    - "Root cause is documented in 46-DIAGNOSIS.md with exact file:line citation before the fix is written"
  artifacts:
    - path: ".planning/phases/46-*/46-DIAGNOSIS.md"
      provides: "Written diagnostic trace identifying whether gap is at (a) alert creation, (b) socket emission, (c) payroll-side query filter, or (d) UI rendering"
      min_lines: 20
  key_links:
    - from: "CS chargeback submission"
      to: "alerts.ts createChargebackAlert (or equivalent)"
      via: "chargebacks.ts POST handler"
      pattern: "createChargebackAlert|alerts_batch_created"
    - from: "alerts pipeline"
      to: "payroll dashboard alert render"
      via: "Socket.IO alerts_batch_created event + payroll alert fetch"
      pattern: "alerts_batch_created"
---

<objective>
Sub-feature 2: Chargebacks submitted from the CS dashboard are NOT surfacing in the payroll dashboard alert area, while chargebacks created directly from payroll ARE surfacing. Diagnose root cause first, then apply the minimal fix.

Purpose: Restore parity so CS-submitted chargebacks produce the same alerts as payroll-originated ones. Agents and payroll staff must see all chargebacks regardless of entry point.
Output: 46-DIAGNOSIS.md with root cause + minimal code fix (no refactor of chargeback model or alerts system).

Note: `autonomous: false` because Task 1 (diagnosis) is gated — subsequent fix tasks depend on the root cause identified. The planner does not know a priori whether the fix lives in chargebacks.ts, alerts.ts, or the payroll-side query; a human-verify or re-plan checkpoint may be needed if diagnosis finds a surprise.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-CONTEXT.md
@apps/ops-api/src/routes/chargebacks.ts
@apps/ops-api/src/services/alerts.ts
@apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx
@apps/ops-dashboard/app/(dashboard)/cs/CSSubmissions.tsx

<interfaces>
Known pipeline (from CONTEXT.md canonical_refs):
- CS dashboard → POST /api/chargebacks (CS path) in chargebacks.ts
- → alerts.ts createChargebackAlert (or equivalent) creates alert records
- → Socket.IO `alerts_batch_created` event (Phase 44)
- → payroll dashboard fetches alerts via PayrollChargebacks.tsx alert query

Known working path for comparison:
- Payroll-originated chargebacks created directly in PayrollChargebacks.tsx → same alerts.ts pipeline → same render. This path DOES surface alerts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Diagnostic trace — identify CS chargeback → payroll alert gap (D-06)</name>
  <files>.planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-DIAGNOSIS.md</files>
  <read_first>
    - apps/ops-api/src/routes/chargebacks.ts (full file — find CS submission entry point vs payroll-originated entry point)
    - apps/ops-api/src/services/alerts.ts (full file — find chargeback alert creation function and socket emit)
    - apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx (alert fetch/subscribe logic — which endpoint and which filter)
    - apps/ops-dashboard/app/(dashboard)/cs/CSSubmissions.tsx (CS submission POST body and endpoint)
    - .planning/phases/46-*/46-CONTEXT.md (D-06, D-07, D-08)
    - .planning/STATE.md (Phase 44 alerts_batch_created context)
  </read_first>
  <action>
    Perform a code-level trace without modifying any application code. Produce `.planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-DIAGNOSIS.md` with this exact structure:

    ```
    # 46-DIAGNOSIS: CS chargeback alert surfacing gap

    ## Trace: CS submission → payroll alert

    1. CS entry point: `apps/ops-dashboard/app/(dashboard)/cs/CSSubmissions.tsx:<LINE>` — submits to <ENDPOINT> with body `{...}`
    2. Server route: `apps/ops-api/src/routes/chargebacks.ts:<LINE>` — handler name `<HANDLER>`
       - Does it call `createChargebackAlert` (or equivalent)? YES/NO with file:line citation
       - Does it emit `alerts_batch_created` socket event? YES/NO with file:line citation
    3. Alert creation: `apps/ops-api/src/services/alerts.ts:<LINE>` — function `<FN>`
       - What filters/conditions gate alert row creation? (e.g. `matchedBy`, `status`, agent match)
    4. Payroll-side fetch: `apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx:<LINE>`
       - Endpoint consumed: `<URL>`
       - Server route for that endpoint: `<file:line>`
       - Filter applied on the server (e.g. `where: { source: 'PAYROLL' }` — this is the type of thing we're hunting)

    ## Comparison: payroll-originated chargeback (working path)

    Same five-step trace for a chargeback submitted directly from PayrollChargebacks.tsx, showing EXACTLY which step differs from the CS path.

    ## Root cause

    One of:
    - (a) Alert record never created for CS path
    - (b) Alert record created but socket not emitted
    - (c) Alert record + socket fine, but payroll-side query filter excludes CS-origin alerts
    - (d) Alert reaches UI but render branch drops CS-origin alerts

    State WHICH and cite the exact file:line with a short code excerpt.

    ## Minimal fix

    Specify EXACTLY what change is needed in Task 2 — which file, which function, which line, what the change does. No refactor. No restructuring. Smallest possible diff that makes CS chargebacks emit alerts on the same path as payroll-originated ones.

    ## Impact surface

    List every file Task 2 will need to touch. If Task 2 needs more than 2 files or requires changes to the database schema, STOP and raise as a checkpoint — this plan was sized for a localized fix.
    ```

    All answers must come from real grep/read of the files listed in read_first. Do not speculate. If a step's answer is "unknown" because the code is missing or ambiguous, mark it UNKNOWN and flag as a blocker.
  </action>
  <verify>
    <automated>test -f ".planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-DIAGNOSIS.md" && wc -l ".planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-DIAGNOSIS.md" | awk '{exit ($1>=20)?0:1}' && grep -q 'Root cause' ".planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-DIAGNOSIS.md"</automated>
  </verify>
  <acceptance_criteria>
    - 46-DIAGNOSIS.md exists, >= 20 lines
    - Contains sections: "Trace", "Comparison", "Root cause", "Minimal fix", "Impact surface"
    - Root cause cites at least one `file:line` reference to apps/ops-api/src/routes/chargebacks.ts OR apps/ops-api/src/services/alerts.ts OR PayrollChargebacks.tsx
    - Minimal fix section names the EXACT file and function to modify in Task 2
  </acceptance_criteria>
  <done>
    Diagnosis document written and committed; root cause identified with citation; Task 2 knows exactly what to change.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply minimal fix from diagnosis (D-07)</name>
  <files>apps/ops-api/src/routes/chargebacks.ts or apps/ops-api/src/services/alerts.ts or apps/ops-dashboard/app/(dashboard)/payroll/PayrollChargebacks.tsx</files>
  <read_first>
    - .planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-DIAGNOSIS.md (from Task 1 — drives this task)
    - The exact file identified in the diagnosis "Minimal fix" section
    - .planning/phases/46-*/46-CONTEXT.md (D-07, D-08)
  </read_first>
  <action>
    Read 46-DIAGNOSIS.md produced in Task 1. Apply the minimal fix specified in the "Minimal fix" section verbatim.

    Constraints (from D-07):
    - Do NOT refactor the chargeback model.
    - Do NOT restructure the alerts service.
    - Do NOT change the Socket.IO transport (still use `alerts_batch_created`).
    - Do NOT regress chargeback dedupe (`matchedBy: "chargeback_alert"` per-chargeback dedupe must remain intact — see STATE.md accumulated context).
    - Do NOT regress payroll-originated chargeback alert behavior.

    If diagnosis concluded:
    - (a) Alert not created: add the `createChargebackAlert` (or equivalent alerts.ts call) invocation inside the CS chargeback POST handler in chargebacks.ts, mirroring how the payroll-originated handler already calls it. Same arguments shape.
    - (b) Socket not emitted: add the `alerts_batch_created` emit alongside the alert creation, matching the payroll path's emit.
    - (c) Query filter excludes CS-origin: relax the filter on the payroll alert fetch route (server-side where clause) so CS-origin chargebacks are included. Use the same whitelist approach as the payroll-origin path.
    - (d) UI drops CS-origin: adjust the render branch in PayrollChargebacks.tsx to render regardless of origin field.

    After the fix, re-check that payroll-originated chargebacks still produce alerts (no regression).
  </action>
  <verify>
    <automated>npx tsc -p apps/ops-api/tsconfig.json --noEmit && grep -q 'alerts_batch_created' apps/ops-api/src/routes/chargebacks.ts apps/ops-api/src/services/alerts.ts 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - The file named in 46-DIAGNOSIS.md "Minimal fix" section has a diff containing the described change
    - `npx tsc -p apps/ops-api/tsconfig.json --noEmit` exits 0
    - `grep -q 'matchedBy.*chargeback_alert' apps/ops-api/src/services/alerts.ts` still passes (dedupe preserved)
    - `grep -q 'alerts_batch_created' apps/ops-api/src/services/alerts.ts || grep -q 'alerts_batch_created' apps/ops-api/src/routes/chargebacks.ts` passes
    - No changes to prisma/schema.prisma
  </acceptance_criteria>
  <done>
    CS-submitted chargebacks produce payroll alerts via the same pipeline as payroll-originated ones. Payroll path unregressed. Dedupe intact. Type checks clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — CS chargeback surfaces in payroll (D-08)</name>
  <what-built>CS chargeback alert surfacing fix — CS-submitted chargeback now flows through alerts pipeline to payroll dashboard.</what-built>
  <how-to-verify>
    1. Log in as CS role. Navigate to CS dashboard. Submit a chargeback for a member who has a matching sale with a known agent.
    2. Log in (separate tab or role switch) as payroll role. Navigate to payroll dashboard, Chargebacks area.
    3. Verify the chargeback appears in the alert area within a few seconds (Socket.IO `alerts_batch_created`) or on refresh.
    4. Verify the alert correctly identifies the agent name + member name + amount.
    5. For regression control, submit a chargeback directly from the payroll Chargebacks UI and confirm it STILL surfaces (i.e., the working path did not break).
    6. Verify that submitting the same CS chargeback twice does NOT create a duplicate alert (dedupe preserved).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g., "CS chargeback still not showing", "payroll regressed")</resume-signal>
</task>

</tasks>

<verification>
- 46-DIAGNOSIS.md artifact committed
- Minimal fix applied to exactly the file(s) identified
- CS chargeback end-to-end produces payroll alert (human-verified)
- Payroll-originated chargebacks still work (regression check)
- Dedupe intact
</verification>

<success_criteria>
- Acceptance criterion D-08 met: CS chargeback submitted for a matched member produces a payroll alert visible to the sale-owning agent in the same alert area
- No refactor of chargeback model or alerts pipeline
- Dedupe preserved
- Type checks clean
</success_criteria>

<output>
Create `.planning/phases/46-bundle-commission-box-for-add-ons-and-ad-d-when-bundled-with/46-02-SUMMARY.md` after completion, including a copy-pasted "Root cause" and "Minimal fix" summary from 46-DIAGNOSIS.md.
</output>
