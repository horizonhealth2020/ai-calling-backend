---
phase: 38-quick-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx
autonomous: true
requirements:
  - FIX-01
  - FIX-02

must_haves:
  truths:
    - "User can set bonus, fronted, or hold input to zero and save without browser validation error"
    - "Fronted amount on dashboard pay card header displays as positive dollar value with orange color"
    - "Fronted amount on print summary displays as positive dollar value with orange color, no minus prefix"
  artifacts:
    - path: "apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx"
      provides: "Zero-value input fix and fronted display corrections"
      contains: "min=\"0\""
  key_links:
    - from: "PayrollPeriods.tsx bonus input"
      to: "HTML number input"
      via: "min=\"0\" attribute"
      pattern: "min=\"0\""
    - from: "PayrollPeriods.tsx fronted input style"
      to: "C.warning color"
      via: "inline style color property"
      pattern: "C\\.warning"
    - from: "printAgentCards fronted summary"
      to: "orange color display"
      via: "inline style in template literal"
      pattern: "color:#d97706"
---

<objective>
Fix zero-value input validation (FIX-01) and fronted positive display with orange color (FIX-02) on both dashboard and print view.

Purpose: Payroll staff cannot currently set bonus/fronted/hold to zero without browser validation issues, and fronted displays as a negative red number instead of positive orange -- both block daily payroll workflow.
Output: Updated PayrollPeriods.tsx with min="0" on three inputs, orange fronted styling on dashboard header, and positive orange fronted in print summary.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-quick-fixes/38-CONTEXT.md
@.planning/phases/38-quick-fixes/38-RESEARCH.md

<interfaces>
<!-- Key code regions in PayrollPeriods.tsx the executor needs -->

From PayrollPeriods.tsx lines 725-777 (agent header inputs):
- Line 736: bonus input -- `type="number" step="0.01"` -- needs `min="0"`
- Line 754: fronted input -- `type="number" step="0.01"` -- needs `min="0"`
- Line 772: hold input -- `type="number" step="0.01"` -- needs `min="0"`
- Line 749: fronted background -- `"rgba(248,113,113,0.10)"` (red tint) -- change to orange
- Line 750: fronted color -- `C.danger` -- change to `C.warning`

From PayrollPeriods.tsx line 1285 (print summary):
- Fronted summary: `<div class="summary-value red">-$${agentFronted.toFixed(2)}</div>`
- Change to: `<div class="summary-value" style="color:#d97706">$${agentFronted.toFixed(2)}</div>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add min="0" to bonus/fronted/hold inputs and fix fronted dashboard color</name>
  <files>apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx</files>
  <read_first>
    - apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx (lines 725-785 for input region)
  </read_first>
  <action>
In `apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx`:

**FIX-01: Add min="0" to three inputs:**

1. Bonus input (around line 736): Change `type="number" step="0.01"` to `type="number" step="0.01" min="0"`
2. Fronted input (around line 754): Change `type="number" step="0.01"` to `type="number" step="0.01" min="0"`
3. Hold input (around line 772): Change `type="number" step="0.01"` to `type="number" step="0.01" min="0"`

**FIX-02 Dashboard: Change fronted input styling from red to orange:**

4. Fronted input background (around line 749): Change `"rgba(248,113,113,0.10)"` to `"rgba(251,191,36,0.10)"`
5. Fronted input color (around line 750): Change `C.danger` to `C.warning`

Do NOT change:
- The `liveNet` calculation at line 781 (`agentGross + (Number(headerBonus) || 0) - (Number(headerFronted) || 0) - (Number(headerHold) || 0)`) -- this stays exactly as-is
- Bonus styling (green / C.success) -- stays as-is
- Hold styling (orange / C.warning) -- stays as-is
- The label "Fronted" -- stays as-is per D-05
  </action>
  <verify>
    <automated>cd "C:/Users/javer/OneDrive/Desktop/projects/ai-calling-backend" && grep -n 'min="0"' "apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - PayrollPeriods.tsx contains exactly 3 occurrences of `min="0"` on the bonus, fronted, and hold number inputs
    - Fronted input style contains `C.warning` (not `C.danger`)
    - Fronted input background contains `"rgba(251,191,36,0.10)"` (not `"rgba(248,113,113,0.10)"`)
    - liveNet calculation line still contains `agentGross + (Number(headerBonus) || 0) - (Number(headerFronted) || 0) - (Number(headerHold) || 0)`
  </acceptance_criteria>
  <done>All three inputs accept zero values via min="0", fronted displays with orange color on dashboard</done>
</task>

<task type="auto">
  <name>Task 2: Fix fronted display in print summary from negative-red to positive-orange</name>
  <files>apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx</files>
  <read_first>
    - apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx (lines 1280-1290 for print summary region)
  </read_first>
  <action>
In `apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx`, in the `printAgentCards()` function:

**FIX-02 Print: Change fronted summary display from negative-red to positive-orange:**

Find line 1285 (the fronted summary item):
```
<div class="summary-item"><div class="summary-label">Fronted</div><div class="summary-value red">-$${agentFronted.toFixed(2)}</div></div>
```

Replace with:
```
<div class="summary-item"><div class="summary-label">Fronted</div><div class="summary-value" style="color:#d97706">$${agentFronted.toFixed(2)}</div></div>
```

Changes:
1. Remove `red` class from the div -- use inline `style="color:#d97706"` instead (orange, matches C.warning)
2. Remove the `-` minus prefix before `$` -- fronted is money advanced to the agent, displayed as positive
3. The Hold summary line (line 1286) already uses orange `style="color:#d97706"` and minus prefix -- leave it unchanged

Do NOT change:
- The `agentFronted` calculation (line 1274)
- The `agentNet` calculation (line 1276)
- Any other summary items (Commission, Bonuses, Hold, Net Payout)
  </action>
  <verify>
    <automated>cd "C:/Users/javer/OneDrive/Desktop/projects/ai-calling-backend" && grep -n "Fronted" "apps/ops-dashboard/app/(dashboard)/payroll/PayrollPeriods.tsx" | grep -i "summary"</automated>
  </verify>
  <acceptance_criteria>
    - Print fronted summary line contains `style="color:#d97706"` (not `class="...red"`)
    - Print fronted summary line contains `>$${agentFronted` (positive, no minus prefix)
    - Print fronted summary line does NOT contain `-$${agentFronted`
    - Hold summary line still contains `-$${agentHold` (minus prefix preserved for hold)
  </acceptance_criteria>
  <done>Fronted displays as positive orange dollar amount on both dashboard and print view</done>
</task>

</tasks>

<verification>
- grep for `min="0"` shows 3 matches in the input region (lines 736, 754, 772 area)
- grep for `C.danger` near fronted input returns no matches (replaced with `C.warning`)
- grep for `rgba(248,113,113` near fronted returns no matches (replaced with orange tint)
- grep for `-\$\$\{agentFronted` in print function returns no matches (minus removed)
- grep for `color:#d97706` in fronted print summary returns a match
- liveNet formula unchanged
</verification>

<success_criteria>
1. All three number inputs (bonus, fronted, hold) have `min="0"` attribute
2. Fronted input on dashboard shows orange color (C.warning) and orange background tint
3. Fronted in print summary shows positive dollar amount with orange color (#d97706)
4. Net calculation is completely unchanged on both dashboard and print
</success_criteria>

<output>
After completion, create `.planning/phases/38-quick-fixes/38-01-SUMMARY.md`
</output>
