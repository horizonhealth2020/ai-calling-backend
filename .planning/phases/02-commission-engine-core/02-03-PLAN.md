---
phase: 02-commission-engine-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [apps/payroll-dashboard/app/page.tsx]
autonomous: false
requirements: [COMM-01]
gap_closure: true

must_haves:
  truths:
    - "In edit mode, the Enroll Fee column shows an enrollmentFee input, not premium"
    - "In edit mode, the Product column shows product info (not a carrier input)"
    - "Saving an edited row sends enrollmentFee to the API"
  artifacts:
    - path: "apps/payroll-dashboard/app/page.tsx"
      provides: "Fixed EditableSaleRow with correct column alignment in edit mode"
      contains: "enrollmentFee"
  key_links:
    - from: "apps/payroll-dashboard/app/page.tsx"
      to: "PATCH /sales/:id"
      via: "onSaleUpdate sends enrollmentFee in body"
      pattern: "enrollmentFee"
---

<objective>
Fix payroll dashboard EditableSaleRow column misalignment where premium appears in the Enroll Fee column during edit mode.

Purpose: User reported "PREMIUM IS BEING TOTALED IN ENROLLMENT FEE COLUMN IN PAYROLL" -- edit mode renders the wrong field under the wrong header, and enrollmentFee is not editable at all.
Output: Correct column mapping in edit mode with enrollmentFee editable and saved to API.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-commission-engine-core/02-UAT.md
@.planning/debug/premium-in-enroll-fee-column.md

<interfaces>
<!-- Current table header order (line 1510-1519): -->
<!-- Agent | Member | Product | Enroll Fee | Commission | Bonus | Fronted | Hold | Net | Actions -->

<!-- Current EditableSaleRow edit-mode column mapping (BROKEN): -->
<!-- td1: Agent name (read-only) — correct -->
<!-- td2: memberName input — correct -->
<!-- td3: carrier input — WRONG (should show product, carrier is not a column) -->
<!-- td4: premium input — WRONG (should be enrollmentFee) -->
<!-- td5+: commission, bonus, fronted, hold, net, actions — correct -->

<!-- saleData state (lines 195-200) — missing enrollmentFee: -->
<!-- { memberName, memberId, carrier, premium, notes } -->

<!-- API PATCH /sales/:id already accepts enrollmentFee (line 338): -->
<!-- enrollmentFee: z.number().min(0).nullable().optional() -->

<!-- Save handler (lines 354-358) sends: memberName, memberId, carrier, premium, notes -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix EditableSaleRow column alignment and add enrollmentFee editing</name>
  <files>apps/payroll-dashboard/app/page.tsx</files>
  <action>
In the EditableSaleRow component (around lines 190-370):

1. Add enrollmentFee to saleData state initialization (around line 195-201):
   - Add `enrollmentFee: String(entry.sale?.enrollmentFee ?? "")` to the useState object

2. Fix td #3 (Product column, around line 239):
   - In EDIT mode: show product badges (same as view mode) — remove the carrier input from this cell entirely. The Product column is not user-editable inline.

3. Fix td #4 (Enroll Fee column, around line 271):
   - In EDIT mode: replace the premium input with an enrollmentFee input
   - Use: `value={saleData.enrollmentFee}` with `onChange` updating `enrollmentFee` in saleData
   - Style: same SMALL_INP style, type="number" step="0.01"
   - In VIEW mode: keep the existing `fee` display (already correct — shows enrollmentFee)

4. Update the save handler (around line 354-360):
   - Add `enrollmentFee: saleData.enrollmentFee ? Number(saleData.enrollmentFee) : null` to the onSaleUpdate call
   - Keep carrier and premium in saleData state so existing values are preserved on save, but they no longer have visible inputs in the table row
   - Final save payload: `{ memberName, memberId: saleData.memberId || null, enrollmentFee: saleData.enrollmentFee ? Number(saleData.enrollmentFee) : null, notes: saleData.notes || null }`
  </action>
  <verify>
    <automated>cd apps/payroll-dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
  - In edit mode, td3 (Product header) shows product badges, not carrier input
  - In edit mode, td4 (Enroll Fee header) shows enrollmentFee input, not premium input
  - Save handler sends enrollmentFee to API
  - Build succeeds with no type errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fixed payroll dashboard EditableSaleRow so Enroll Fee column shows enrollment fee (not premium) in edit mode</what-built>
  <how-to-verify>
    1. Start payroll dashboard: `npm run payroll:dev`
    2. Open http://localhost:3012
    3. Find a payroll period with sales entries
    4. Click on a member name to enter edit mode on a row
    5. Verify: The "Enroll Fee" column shows the enrollment fee value in its input, NOT the premium
    6. Verify: The "Product" column shows product badges (not a carrier text input)
    7. Try editing the enrollment fee value and clicking Save
    8. Verify: The new enrollment fee value persists after save
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Build succeeds: `cd apps/payroll-dashboard && npx next build`
- Column alignment: td3=Product (badges), td4=Enroll Fee (enrollmentFee input in edit mode)
- Save sends enrollmentFee to PATCH /sales/:id
</verification>

<success_criteria>
- Premium no longer appears in enrollment fee column during edit mode
- Enrollment fee is editable and saved to the database via API
- Product column shows product info in both view and edit modes
</success_criteria>

<output>
After completion, create `.planning/phases/02-commission-engine-core/02-03-SUMMARY.md`
</output>
