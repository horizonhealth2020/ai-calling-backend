---
phase: 33-core-tv-readability
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - apps/sales-board/app/page.tsx
autonomous: true
requirements:
  - TYPO-04

must_haves:
  truths:
    - "KPI card labels render at fontSize 14"
    - "KPI card numbers render at fontSize 36 (base) with proportional conditional sizing"
    - "KPI card dimensions stay the same size (padding reduced to absorb font growth)"
    - "Gap between stats bar and tab toggles is tighter than before"
  artifacts:
    - path: "apps/sales-board/app/page.tsx"
      provides: "TV-readable KPI stat cards"
      contains: "fontSize: 36"
  key_links:
    - from: "KPI card padding"
      to: "card visual size"
      via: "padding reduction absorbs font growth"
      pattern: "spacing\\[3\\].*spacing\\[4\\]"
    - from: "Today's Premium conditional fontSize"
      to: "card number display"
      via: "ternary expression"
      pattern: "fontSize.*10000.*30.*36"
---

<objective>
Increase KPI stat card font sizes and reduce card padding and stats-to-TabNav gap for TV-distance readability.

Purpose: Make the 4 KPI stat cards at the top of the sales board readable from across the room on a 1080p TV.
Output: Updated `apps/sales-board/app/page.tsx` with KPI card typography, padding, and spacing changes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-core-tv-readability/33-CONTEXT.md
@.planning/phases/33-core-tv-readability/33-RESEARCH.md
@.planning/phases/33-core-tv-readability/33-UI-SPEC.md
@.planning/phases/33-core-tv-readability/33-01-SUMMARY.md

<interfaces>
<!-- Key tokens and current values the executor needs -->

From packages/ui/src/tokens.ts (reference only, not modified):
- spacing[1] = 4, spacing[2] = 8, spacing[3] = 12, spacing[4] = 16, spacing[5] = 20

From apps/sales-board/app/page.tsx KPI stats region (lines ~987-1204):
- Stats grid marginTop: spacing[3] (line ~994)
- All 4 cards use padding: `${spacing[5]}px` (20px all sides) at lines ~1001, ~1049, ~1101, ~1153
- All 4 card labels use fontSize: 11 at lines ~1022, ~1070, ~1122, ~1174
- Today's Sales number: fontSize 30 (line ~1034)
- Today's Premium number: fontSize conditional `data && totalPremToday >= 10000 ? 22 : 26` (line ~1082)
- Weekly Sales number: fontSize 30 (line ~1134)
- Weekly Premium number: fontSize conditional `data && data.grandTotalPremium >= 10000 ? 20 : 26` (line ~1186)
- Tab toggle wrapper marginBottom: spacing[3] (line ~1204)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase KPI card fonts, reduce padding, tighten stats-to-tab gap</name>
  <files>apps/sales-board/app/page.tsx</files>
  <read_first>
    - apps/sales-board/app/page.tsx (lines 987-1210 for KPI stats bar and tab toggle)
    - .planning/phases/33-core-tv-readability/33-RESEARCH.md (code examples for KPI cards)
    - .planning/phases/33-core-tv-readability/33-UI-SPEC.md (KPI stat cards typography table and padding compensation contract)
  </read_first>
  <action>
Apply the following exact changes to the KPI stats bar region in apps/sales-board/app/page.tsx.

**Stats grid container (line ~994):**
- Change `marginTop: spacing[3]` to `marginTop: spacing[2]`

**All 4 KPI cards -- padding (lines ~1001, ~1049, ~1101, ~1153):**
- Change `padding: \`${spacing[5]}px\`` to `padding: \`${spacing[3]}px ${spacing[4]}px\``
  This changes from 20px all sides to 12px vertical, 16px horizontal.

**All 4 KPI card labels -- fontSize (lines ~1022, ~1070, ~1122, ~1174):**
- Change `fontSize: 11` to `fontSize: 14`

**Today's Sales number (line ~1034):**
- Change `fontSize: 30` to `fontSize: 36`

**Today's Premium number (line ~1082):**
- Change `fontSize: data && totalPremToday >= 10000 ? 22 : 26` to `fontSize: data && totalPremToday >= 10000 ? 28 : 36`
  (Proportional scaling: base 26->36 is 1.38x, reduced 22->28 because UI-SPEC merges to 28 for the 4-size budget)

**Weekly Sales number (line ~1134):**
- Change `fontSize: 30` to `fontSize: 36`

**Weekly Premium number (line ~1186):**
- Change `fontSize: data && data.grandTotalPremium >= 10000 ? 20 : 26` to `fontSize: data && data.grandTotalPremium >= 10000 ? 28 : 36`
  (Proportional scaling: base 26->36, reduced 20->28 per UI-SPEC)

**Tab toggle wrapper (line ~1204):**
- Change `marginBottom: spacing[3]` to `marginBottom: spacing[2]`
  </action>
  <verify>
    <automated>cd C:/Users/javer/Documents/Repositories/ai-calling-backend && npx next build apps/sales-board 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 4 KPI card padding values contain `spacing[3]` and `spacing[4]` (not `spacing[5]`)
    - All 4 KPI card label fontSize values are `14` (not `11`)
    - Today's Sales fontSize is `36`
    - Today's Premium fontSize ternary reads `? 28 : 36` (not `? 22 : 26`)
    - Weekly Sales fontSize is `36`
    - Weekly Premium fontSize ternary reads `? 28 : 36` (not `? 20 : 26`)
    - Stats grid marginTop uses `spacing[2]` (not `spacing[3]`)
    - Tab toggle wrapper marginBottom uses `spacing[2]` (not `spacing[3]`)
    - grep for `fontSize: 11` in lines 987-1200 returns zero matches
    - grep for `fontSize: 36` returns at least 2 matches
    - `npx next build apps/sales-board` exits 0
  </acceptance_criteria>
  <done>All 4 KPI stat cards have label fontSize 14, number fontSize 36 (with proportional conditional sizing for premium cards), padding reduced from spacing[5] to spacing[3]/spacing[4], and stats-to-tab gap tightened. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete TV readability at 1080p</name>
  <what-built>Full Phase 33 implementation: WeeklyView table (plan 01) + KPI stat cards (this plan) with all font sizes increased, padding reduced, contrast promoted, overflow protection added, and spacing tightened.</what-built>
  <how-to-verify>
    1. Run `npm run salesboard:dev` and open http://localhost:3013
    2. Set browser to 1920x1080 (DevTools device toolbar)
    3. Check KPI stat cards at top: numbers should be noticeably larger, card dimensions should be about the same size as before
    4. Check gap between cards and the Leaderboard/Weekly Breakdown tabs -- should be tighter
    5. Click "Weekly Breakdown" tab and verify the full weekly table is readable
    6. Verify with 15 agents (or as many as available): all rows + team total should fit without scrolling
    7. Verify team total row is the most prominent row on screen
    8. Overall: step back from monitor to arm's length -- all numbers and names should be legible
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx next build apps/sales-board` completes without errors
2. `grep -c "fontSize: 36" apps/sales-board/app/page.tsx` returns at least 2 (Today's Sales and Weekly Sales base sizes)
3. `grep -c "fontSize: 14" apps/sales-board/app/page.tsx` returns multiple matches (4 card labels + daily premiums + team total daily premiums + TH)
4. `grep "spacing\[5\]" apps/sales-board/app/page.tsx` shows no matches in the KPI card padding lines (all converted to spacing[3]/spacing[4])
</verification>

<success_criteria>
- All 4 KPI card labels at fontSize 14 (was 11)
- All 4 KPI card numbers at fontSize 36 base (was 30), with conditional premium cards at 28/36 (was 22/26 and 20/26)
- Card padding reduced from spacing[5] (20px) to spacing[3] vertical / spacing[4] horizontal (12px/16px)
- Stats-to-tab gap tightened (both marginTop and marginBottom using spacing[2] instead of spacing[3])
- Build passes successfully
- Visual checkpoint confirms TV readability at 1080p resolution
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-tv-readability/33-02-SUMMARY.md`
</output>
