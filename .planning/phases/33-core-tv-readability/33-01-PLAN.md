---
phase: 33-core-tv-readability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/sales-board/app/page.tsx
autonomous: true
requirements:
  - TYPO-01
  - TYPO-03
  - TYPO-05
  - OVFL-01
  - OVFL-02
  - SCAL-04

must_haves:
  truths:
    - "Agent names in weekly table render at fontSize 24"
    - "Daily sale counts render at fontSize 24"
    - "Daily premium amounts render at fontSize 14"
    - "Weekly premium column renders at fontSize 24"
    - "Team total daily counts render at fontSize 24"
    - "Team total daily premiums render at fontSize 14"
    - "Team total grand premium renders at fontSize 24"
    - "Table headers render at fontSize 14"
    - "All textTertiary in WeeklyView replaced with textSecondary"
    - "Agent name span has overflow:hidden and textOverflow:ellipsis"
    - "Agent row vertical padding is 12px (spacing[3])"
    - "Team total row vertical padding is spacing[3] (12px)"
  artifacts:
    - path: "apps/sales-board/app/page.tsx"
      provides: "TV-readable weekly breakdown table"
      contains: "fontSize: 24"
  key_links:
    - from: "WeeklyView TH style object"
      to: "table header cells"
      via: "spread into th elements"
      pattern: "fontSize: 14"
    - from: "agent name span"
      to: "overflow protection"
      via: "inline style with textOverflow"
      pattern: "textOverflow.*ellipsis"
---

<objective>
Increase font sizes, reduce padding, promote contrast, and add overflow protection on the WeeklyView table in the sales board for TV-distance readability.

Purpose: Make the weekly breakdown table readable from 10-15 feet on a 1080p wall-mounted TV in a lit sales office.
Output: Updated `apps/sales-board/app/page.tsx` with all WeeklyView typography, padding, contrast, and overflow changes applied.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-core-tv-readability/33-CONTEXT.md
@.planning/phases/33-core-tv-readability/33-RESEARCH.md
@.planning/phases/33-core-tv-readability/33-UI-SPEC.md

<interfaces>
<!-- Key style constants and tokens the executor needs -->

From packages/ui/src/tokens.ts (reference only, not modified):
- spacing[1] = 4, spacing[2] = 8, spacing[3] = 12, spacing[4] = 16, spacing[5] = 20
- colors.textSecondary = "#94a3b8" (promotion target)
- colors.textTertiary = "#64748b" (being replaced in WeeklyView)

From apps/sales-board/app/page.tsx (current state):
- TH style object: line 539, padding "14px 16px", fontSize 15
- Agent name td: line 601-638, padding `14px ${spacing[5]}px`, fontSize 18
- Agent name span: line 631, bare `<span>{agent}</span>` with no overflow
- Daily cell td: line 646-647, padding "14px 16px"
- Daily count span: line 656, fontSize 20
- Daily premium span: line 668, fontSize 12, color colors.textTertiary
- Total column td: line 680-682, padding "14px 16px", fontSize 24
- Premium column td: line 695-697, padding `14px ${spacing[5]}px`, fontSize 15
- Team total label td: line 716, padding `${spacing[4]}px ${spacing[5]}px`
- Team total daily td: line 733, padding `${spacing[4]}px 16px`
- Team total daily count: line 740, fontSize 20
- Team total daily premium: line 743, fontSize 12
- Team total grand total td: line 755, padding `${spacing[4]}px 16px`, fontSize 28
- Team total grand premium td: line 768, padding `${spacing[4]}px ${spacing[5]}px`, fontSize 16
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase WeeklyView table font sizes and reduce padding</name>
  <files>apps/sales-board/app/page.tsx</files>
  <read_first>
    - apps/sales-board/app/page.tsx (lines 539-784 for WeeklyView, full current state)
    - .planning/phases/33-core-tv-readability/33-RESEARCH.md (complete change map)
    - .planning/phases/33-core-tv-readability/33-UI-SPEC.md (padding compensation contract)
  </read_first>
  <action>
Apply the following exact changes to the WeeklyView function in apps/sales-board/app/page.tsx. All fontSize values are numeric literals (not strings). All padding uses spacing tokens or inline px strings.

**TH style object (line ~539-554):**
- Change `padding: "14px 16px"` to `padding: "12px 16px"`
- Change `fontSize: 15` to `fontSize: 14`

**Agent name td (line ~601-609):**
- Change `padding: \`14px ${spacing[5]}px\`` to `padding: \`${spacing[3]}px ${spacing[5]}px\``
- Change `fontSize: 18` to `fontSize: 24`

**Agent name span (line ~631):**
Replace bare `<span>{agent}</span>` with:
```tsx
<span style={{
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap",
  maxWidth: 160,
  display: "inline-block",
  verticalAlign: "middle",
}}>
  {agent}
</span>
```

**Daily cell td (line ~646-650):**
- Change `padding: "14px 16px"` to `padding: "${spacing[3]}px 16px"` (template literal using spacing[3] which is 12)
  NOTE: This must become a template literal: `padding: \`${spacing[3]}px 16px\``

**Daily count span (line ~656):**
- Change `fontSize: 20` to `fontSize: 24`

**Daily premium span (line ~668):**
- Change `fontSize: 12` to `fontSize: 14`
- Change `color: colors.textTertiary` to `color: colors.textSecondary`

**Total column td (line ~680-682):**
- Change `padding: "14px 16px"` to `padding: \`${spacing[3]}px 16px\``
- fontSize 24 stays unchanged (D-04)

**Premium column td (line ~695-704):**
- Change `padding: \`14px ${spacing[5]}px\`` to `padding: \`${spacing[3]}px ${spacing[5]}px\``
- Change `fontSize: 15` to `fontSize: 24`

**Team total label td (line ~714-723):**
- Change `padding: \`${spacing[4]}px ${spacing[5]}px\`` to `padding: \`${spacing[3]}px ${spacing[5]}px\``

**Team total daily td (line ~730-733):**
- Change `padding: \`${spacing[4]}px 16px\`` to `padding: \`${spacing[3]}px 16px\``

**Team total daily count (line ~740):**
- Change `fontSize: 20` to `fontSize: 24`

**Team total daily premium (line ~743):**
- Change `fontSize: 12` to `fontSize: 14`

**Team total grand total td (line ~753-755):**
- Change `padding: \`${spacing[4]}px 16px\`` to `padding: \`${spacing[3]}px 16px\``
- fontSize 28 stays unchanged (D-05)

**Team total grand premium td (line ~766-768):**
- Change `padding: \`${spacing[4]}px ${spacing[5]}px\`` to `padding: \`${spacing[3]}px ${spacing[5]}px\``
- Change `fontSize: 16` to `fontSize: 24`
  </action>
  <verify>
    <automated>cd C:/Users/javer/Documents/Repositories/ai-calling-backend && npx next build apps/sales-board 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TH object contains `fontSize: 14` and `padding: "12px 16px"`
    - Agent name td contains `fontSize: 24` and padding using `spacing[3]`
    - Agent name span contains `textOverflow: "ellipsis"` and `maxWidth: 160`
    - Daily cell td padding uses `spacing[3]`
    - Daily count span contains `fontSize: 24`
    - Daily premium span contains `fontSize: 14` and `color: colors.textSecondary`
    - grep for `colors.textTertiary` within lines 539-784 returns zero matches
    - Premium column td contains `fontSize: 24`
    - Team total daily count contains `fontSize: 24`
    - Team total daily premium contains `fontSize: 14`
    - Team total grand premium td contains `fontSize: 24`
    - All team total td padding uses `spacing[3]` instead of `spacing[4]`
    - `npx next build apps/sales-board` exits 0
  </acceptance_criteria>
  <done>All WeeklyView font sizes match D-01 through D-06 targets, padding reduced per D-14/D-15, textTertiary promoted per D-11, agent name has ellipsis overflow per D-13. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify WeeklyView TV readability</name>
  <what-built>WeeklyView table with increased font sizes (agent names 24px, daily counts 24px, daily premiums 14px, premium column 24px, team total proportional), reduced padding (12px vertical), textTertiary promoted to textSecondary, agent name ellipsis overflow protection.</what-built>
  <how-to-verify>
    1. Run `npm run salesboard:dev` and open http://localhost:3013 in Chrome
    2. Set browser window to 1920x1080 (DevTools > toggle device toolbar > set dimensions)
    3. Click "Weekly Breakdown" tab
    4. Verify: Agent names, daily sale counts, and premium column are visibly larger than before
    5. Verify: Team total row is the most prominent row (larger numbers, gold color)
    6. Verify: Daily premium amounts under each count are visible (not too dim)
    7. Verify: Row heights are roughly the same as before (padding decrease compensates for font increase)
    8. If an agent has a long name, verify it truncates with "..." instead of overflowing
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx next build apps/sales-board` completes without errors
2. `grep -n "textTertiary" apps/sales-board/app/page.tsx` shows NO matches between lines 539-784 (WeeklyView region)
3. `grep -c "fontSize: 24" apps/sales-board/app/page.tsx` shows increase from before (multiple new instances)
4. `grep "textOverflow" apps/sales-board/app/page.tsx` returns at least one match with "ellipsis"
</verification>

<success_criteria>
- All WeeklyView font sizes match decision targets: agent names 24px, daily counts 24px, daily premiums 14px, headers 14px, premium column 24px, team total daily counts 24px, team total daily premiums 14px, team total grand premium 24px
- Cell vertical padding reduced from 14px to spacing[3] (12px) on agent rows
- Team total vertical padding reduced from spacing[4] to spacing[3]
- All textTertiary references in WeeklyView replaced with textSecondary
- Agent name span has overflow:hidden, textOverflow:ellipsis, maxWidth:160
- Build passes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-tv-readability/33-01-SUMMARY.md`
</output>
