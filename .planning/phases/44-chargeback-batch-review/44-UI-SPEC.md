---
phase: 44
slug: chargeback-batch-review
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-06
revised: 2026-04-06
---

# Phase 44 — UI Design Contract

> Visual and interaction contract for the chargeback batch review table. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (inline React.CSSProperties) |
| Icon library | lucide-react (already imported in CSSubmissions) |
| Font | Inter (via `typography.fontSans` token) |

Source: CLAUDE.md mandates inline CSSProperties with SCREAMING_SNAKE_CASE constants. No Tailwind, no globals.css. All tokens from `packages/ui/src/tokens.ts`.

---

## Spacing Scale

Declared values for this phase (all multiples of 4):

| Token | Value | Usage in This Phase |
|-------|-------|---------------------|
| spacing[1] | 4px | Checkbox-to-label gap, badge dot gap |
| spacing[2] | 8px | Compact element spacing, product checkbox inline gap |
| spacing[4] | 16px | Section heading margin-bottom, cell padding (horizontal), substitute for any 12px needs |
| spacing[6] | 24px | Summary bar horizontal padding, card padding, table container side padding |
| spacing[8] | 32px | Gap between summary bar and table |

### Inherited Pre-existing Tokens (outside phase scope)

The following values are NOT declared by this phase. They exist in the shared `baseThStyle` and `baseTdStyle` tokens from `packages/ui/src/tokens.ts` and are used as-is without modification. They are listed here for awareness only:

- `baseThStyle` vertical padding: 10px -- inherited from existing table token, not a phase-declared spacing value.
- `baseTdStyle` vertical padding: 12px -- inherited from existing table token, not a phase-declared spacing value.

These are outside this phase's control and are not subject to the 4px-multiple spacing rule for phase-declared values.

---

## Typography

All values from existing `typography` token in `packages/ui/src/tokens.ts`. This phase uses exactly **2 font weights**: 400 (normal) and 700 (bold).

| Role | Size | Weight | Line Height | Token Path |
|------|------|--------|-------------|------------|
| Table header | 11px | 700 (bold) | 1.45 | `baseThStyle` (pre-built) |
| Table cell / body | 13px | 400 (normal) | 1.5 | `baseTdStyle` / `typography.sizes.sm` |
| Compact input | 13px | 400 (normal) | 1.6 | `COMPACT_INPUT` pattern |
| Section heading | 16px | 700 (bold) | 1.5 | `SECTION_HEADING` pattern |
| Summary bar total | 16px | 700 (bold) | 1.5 | `typography.sizes.md` + `typography.weights.bold` |
| Badge text | 10px | 700 (bold) | 1 | Badge `size="sm"` default |
| Product checkbox label | 13px | 400 (normal) | 1.5 | `typography.sizes.sm` |

Weight scale summary: 400 for all body/label text, 700 for all emphasis (headings, totals, badges).

---

## Color

All values reference CSS custom properties via `colors.*` tokens from `packages/ui/src/tokens.ts`. Dark theme hex values shown for reference.

| Role | Token | Dark Hex | Usage in This Phase |
|------|-------|----------|---------------------|
| Dominant (60%) | `colors.bgRoot` | #070a0a | Page background |
| Secondary (30%) | `colors.bgSurface` | #0c1414 | Review table container, summary bar background |
| Accent (10%) | `colors.accentTeal` | #14b8a6 | "Submit Batch" CTA button background only |
| Destructive | `colors.danger` | #f87171 | UNMATCHED badge, total chargeback amount, remove (X) button hover |

### Semantic Status Colors (Match Badges)

| Status | Token | Dark Hex | Badge Variant |
|--------|-------|----------|---------------|
| MATCHED | `colors.success` | #34d399 | `subtle` (15% opacity bg, solid text) |
| MULTIPLE | `colors.warning` | #fbbf24 | `subtle` |
| UNMATCHED | `colors.danger` | #f87171 | `subtle` |

Accent reserved for: "Submit Batch" primary CTA button only. Badge filter active state uses `solid` variant (full-color background) of the respective semantic color -- not accent.

### Summary Bar Filter Active State

When a badge in the summary bar is clicked to filter:
- Inactive: `variant="subtle"` (15% opacity background)
- Active: `variant="solid"` (full-color background, white text)
- Toggle behavior: clicking the same badge again clears the filter (returns to showing all rows)

---

## Visual Hierarchy

**Primary focal point:** The summary bar (status badge counts + total amount) is the first element the user sees upon entering review mode. It anchors the top of the review area and provides immediate batch health at a glance.

**Secondary focal point:** The review table body, scanned top-to-bottom. Status badge color in column 1 draws the eye to rows needing attention (yellow MULTIPLE, red UNMATCHED).

**Tertiary focal point:** Action buttons ("Submit Batch" accent CTA, "Clear Batch" ghost) anchored below the table.

---

## Component Inventory

All components are inline within CSSubmissions.tsx using SCREAMING_SNAKE_CASE style constants. No separate component files.

### New Style Constants

| Constant | Element | Key Properties |
|----------|---------|----------------|
| `SUMMARY_BAR` | `<div>` wrapping filter badges + total | `display: flex`, `alignItems: center`, `gap: 16px`, `padding: 16px 24px`, `borderBottom: 1px solid colors.borderSubtle`, `background: colors.bgSurface` |
| `REVIEW_TABLE_WRAP` | `<div>` wrapping review table | Reuse existing `TABLE_WRAP` pattern: `width: 100%`, `overflowX: auto` |
| `PRODUCT_CHECKBOX_WRAP` | `<label>` wrapping each product checkbox | `display: inline-flex`, `alignItems: center`, `gap: 4px`, `fontSize: 13`, `color: colors.textSecondary`, `cursor: pointer`, `marginRight: 8px` |
| `REMOVE_BTN` | `<button>` for row X removal | `background: transparent`, `border: none`, `cursor: pointer`, `color: colors.textTertiary`, `padding: 4px`, hover `color: colors.danger`; `aria-label` required (see Accessibility) |
| `SALE_SELECT` | `<select>` for MULTIPLE match resolution | Reuse `COMPACT_INPUT` pattern with `width: auto` |

Note: `REMOVE_BTN` renders the `X` icon from lucide-react. It is intentionally icon-only (no text label) because the column header "Remove" provides context and the button has a descriptive `aria-label`. A `title` tooltip is also declared: `title="Remove from batch"`.

### Reused Style Constants (from existing CSSubmissions)

| Constant | Purpose |
|----------|---------|
| `SECTION_HEADING` | "Review Batch" heading above summary bar (weight updated to 700 bold) |
| `TABLE_WRAP` | Horizontal scroll container for wide review table |
| `COMPACT_INPUT` | Amount input, rep assignment dropdown |
| `baseThStyle` | Review table header cells |
| `baseTdStyle` | Review table body cells |

### Existing Components Used

| Component | Source | Usage |
|-----------|--------|-------|
| `Badge` | `@ops/ui` | Match status badges in rows + summary bar filter badges |
| `useToast` | `@ops/ui` | Row removal notification (see Undo Toast note below) |
| `EmptyState` | `@ops/ui` | When paste area is empty (existing behavior) |
| `X` icon | `lucide-react` | Row remove button |
| `Loader2` icon | `lucide-react` | Loading spinner during preview API call |

---

## Review Table Column Spec

Columns listed left to right. All use `baseThStyle` for headers, `baseTdStyle` for cells.

| # | Column | Width | Cell Content | Interactive |
|---|--------|-------|-------------|-------------|
| 1 | Status | 90px | `Badge` with match status text | No (read-only) |
| 2 | Member | 140px min | Member name + member ID on second line (smaller, `colors.textTertiary`) | No |
| 3 | Agent | 120px min | Agent name from matched sale; "--" if unmatched | No |
| 4 | Products | 200px min | Inline checkboxes: `[x] ProductName ($XX.XX)` per product | Yes (checkbox toggle) |
| 5 | Amount | 100px | `COMPACT_INPUT` type="text" showing formatted dollar value | Yes (always editable) |
| 6 | Rep | 130px | `<select>` dropdown with `COMPACT_INPUT` style, populated from CS reps | Yes (always editable) |
| 7 | Remove | 44px | `X` icon button with `title="Remove from batch"` | Yes (click to remove) |

### Column Behavior Notes

- **Products column (MATCHED):** All products start checked. Unchecking recalculates Amount to sum of remaining checked premiums. After user manually edits Amount, auto-recalculation stops for that row (set `amountManuallyOverridden: true`).
- **Products column (UNMATCHED):** Empty cell, no checkboxes. Amount stays as parsed.
- **Products column (MULTIPLE):** Shows a `<select>` dropdown listing candidate sale descriptions (agent name + member name). Once user selects a sale, products populate as checkboxes and status promotes to MATCHED. Format for dropdown options: `"AgentName - MemberName (N products)"`.
- **Status badge:** MATCHED rows show green badge, MULTIPLE shows yellow badge, UNMATCHED shows red badge. After user resolves a MULTIPLE by selecting a sale, badge changes to green MATCHED.

---

## Interaction States

### Loading State (Preview API Processing)

After user clicks "Parse & Preview" (modifies existing Parse button):
- Button shows `Loader2` icon spinning + "Matching..." text
- Button is disabled
- Textarea is disabled
- Duration: typically < 2 seconds for batches under 50 records

### Review Table Visible State

When `reviewRecords.length > 0`:
- Hide the textarea (keep rawText in state for error recovery)
- Show summary bar + review table + action buttons
- Action buttons below table: "Submit Batch" (primary, accent color) + "Clear Batch" (secondary, ghost style)

### Row Removal + Undo

- Click X button: row disappears immediately from table
- Toast appears: `info` type, message: `"Removed {memberCompany || memberId || 'entry'}"`
- Toast duration: 5000ms (5 seconds)
- **Undo implementation note:** The existing `useToast()` does NOT support action buttons. Implementation must either: (a) extend ToastProvider to accept an `action` prop with `{ label: string, onClick: () => void }`, or (b) use a local "recently removed" state with a timed restore button rendered inline above the table. Option (a) is recommended -- add optional `action` prop to `ToastItem` interface and render an action button in `ToastEntry`.

### Submit Batch

- Button disabled when `reviewRecords.length === 0`
- Button disabled during submission (shows `Loader2` + "Submitting...")
- On success: clear all state, show success toast "N chargebacks submitted", emit `cs:changed` socket event once
- On error: show error toast with status code, keep review table intact so user can retry

### Empty State

When no paste data and no review records: existing textarea with placeholder is sufficient. No separate EmptyState component needed (matches current CSSubmissions pattern).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Submit Batch" |
| Parse button (modified) | "Parse & Preview" |
| Parse loading state | "Matching..." |
| Submit loading state | "Submitting..." |
| Clear button | "Clear Batch" |
| Summary bar format | "{N} Matched" / "{N} Multiple" / "{N} Unmatched" / "Total: $X,XXX.XX" |
| MULTIPLE select placeholder | "Select matching sale..." |
| MULTIPLE select option format | "{AgentName} - {MemberName} ({N} products)" |
| Row remove toast | "Removed {memberCompany or memberId or 'entry'}" |
| Undo action label | "Undo" |
| Submit success toast | "{N} chargebacks submitted" |
| Submit error toast | "Submit failed ({statusCode})" |
| Preview error toast | "Preview failed ({statusCode})" |
| Empty products cell | "--" (em dash for unmatched rows with no products) |
| Unmatched agent cell | "--" |
| Remove button tooltip | "Remove from batch" |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| N/A | none | not applicable |

No shadcn initialized. No third-party registries. All components are inline CSSProperties following existing project patterns.

---

## Motion / Animation

All transitions use existing `motion` tokens from `packages/ui/src/tokens.ts`.

| Interaction | Duration | Easing | Property |
|-------------|----------|--------|----------|
| Badge hover (summary bar) | 150ms (`motion.duration.fast`) | `motion.easing.out` | opacity, background |
| Remove button hover | 150ms (`motion.duration.fast`) | `motion.easing.out` | color (textTertiary to danger) |
| Toast slide-in | CSS class `animate-slide-in-right` | existing | transform (existing Toast pattern) |
| Button disabled state | 150ms (`motion.duration.fast`) | `motion.easing.out` | opacity |

---

## Accessibility Notes

- Summary bar filter badges must be wrapped in `<button>` elements (Badge component renders `<span>`, which is not keyboard-focusable)
- Product checkboxes use native `<input type="checkbox">` with associated `<label>` for screen reader support
- Remove button must have `aria-label="Remove {memberCompany || memberId || 'entry'}"` and `title="Remove from batch"`
- MULTIPLE select must have `aria-label="Select matching sale for {memberCompany || memberId}"`
- Summary bar container should have `role="toolbar"` and `aria-label="Filter by match status"`
- Review table uses semantic `<table>`, `<thead>`, `<tbody>`, `<th scope="col">` elements (matches existing pattern)

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
