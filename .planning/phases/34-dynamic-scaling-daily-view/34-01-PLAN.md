---
phase: 34-dynamic-scaling-daily-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/sales-board/app/page.tsx
autonomous: false
requirements:
  - TYPO-02
  - SCAL-01
  - SCAL-03

must_haves:
  truths:
    - "Podium card agent names, sale counts, and premium amounts are visibly larger than current sizes"
    - "Rest-of-agents names, counts, and premiums are visibly larger than current sizes"
    - "Rank labels, section labels, and toggle buttons are enlarged for TV distance"
    - "Premium text uses textSecondary color instead of textTertiary on all DailyView elements"
    - "Podium card dimensions (width/height) remain unchanged from current values"
    - "Platform base number font sizes are increased from 11px"
    - "15 agents fit on a 1080p screen on daily view at typical browser zoom"
  artifacts:
    - path: "apps/sales-board/app/page.tsx"
      provides: "DailyView with enlarged fonts and promoted contrast"
      contains: "nameSize: 22"
  key_links:
    - from: "PODIUM_CONFIG"
      to: "PodiumCard component"
      via: "cfg.nameSize, cfg.countSize used in inline styles"
      pattern: "fontSize: cfg\\.(nameSize|countSize)"
    - from: "DailyView rest-of-agents"
      to: "inline styles"
      via: "fontSize literals in style objects"
      pattern: "fontSize: (18|32|14|13)"
---

<objective>
Enlarge all font sizes on the DailyView (podium cards + rest-of-agents columns) for TV-distance readability, promote textTertiary to textSecondary for contrast, and reduce padding to absorb larger fonts within unchanged container dimensions.

Purpose: Make the daily/podium leaderboard view readable from 10-15 feet on a wall-mounted 1080p TV, completing the TV readability milestone.
Output: Updated `apps/sales-board/app/page.tsx` with all DailyView font and contrast changes.

Note: SCAL-02 (15 agents fit on weekly breakdown view) was delivered by Phase 33 (visual verification approved at 1080p). It is not in scope for this plan. Task 3 includes a quick weekly-view sanity check for completeness.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-dynamic-scaling-daily-view/34-CONTEXT.md

<interfaces>
<!-- Key structures in apps/sales-board/app/page.tsx that executor must understand -->

PODIUM_CONFIG (lines 52-95) — object keyed by rank (0, 1, 2) with properties:
- nameSize: number (current: 17, 15, 14)
- countSize: number (current: 36, 28, 26)
- height: number (current: 220, 180, 160) — DO NOT CHANGE
- width: number (current: 200, 175, 165) — DO NOT CHANGE

PodiumCard (lines 99-218) — reads from PODIUM_CONFIG via cfg.nameSize, cfg.countSize
- Rank label above card: fontSize 10 (line 126)
- Premium text: fontSize 12, color: colors.textTertiary (line 212)
- Padding: spacing[5] top, spacing[4] horizontal (line 152)

PodiumPlatform (lines 222-281) — platform base numbers
- Three spans with fontSize: 11 (lines 247, 262, 277)

DailyView (lines 285-521):
- "Top Performers" label: fontSize 11, color: colors.textTertiary (line 327-328)
- Day/Week toggle: fontSize 12 (line 348)
- "All Agents" label: fontSize 11, color: colors.textMuted (line 389-391)
- Agent name: fontSize 14 (line 469)
- Sale count: fontSize 28 (line 481)
- Premium: fontSize 12, color: colors.textTertiary (line 492)
- Rank badge: fontSize 11, color: colors.textMuted (line 440-441)

Design tokens from @ops/ui:
- colors.textSecondary = "#94a3b8" (promotion target)
- colors.textTertiary (current color on premium text — too dim for TV)
- colors.textMuted (current color on labels — too dim for TV)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enlarge podium card fonts, promote contrast, and reduce padding</name>
  <files>apps/sales-board/app/page.tsx</files>
  <read_first>
    - apps/sales-board/app/page.tsx (full file — focus on lines 52-281 for PODIUM_CONFIG, PodiumCard, PodiumPlatform)
    - .planning/phases/34-dynamic-scaling-daily-view/34-CONTEXT.md (decisions D-03 through D-07, D-15, D-16)
  </read_first>
  <action>
Update PODIUM_CONFIG (lines 52-95) with new font sizes per user decisions:

Rank 0 (1st place, key `0`):
- `nameSize: 17` -> `nameSize: 22`
- `countSize: 36` -> keep `countSize: 36` (already large enough)
- `height: 220` -> keep `height: 220` (DO NOT CHANGE per D-15)
- `width: 200` -> keep `width: 200` (DO NOT CHANGE per D-15)

Rank 1 (2nd place, key `1`):
- `nameSize: 15` -> `nameSize: 20`
- `countSize: 28` -> `countSize: 32`
- `height: 180` -> keep (DO NOT CHANGE)
- `width: 175` -> keep (DO NOT CHANGE)

Rank 2 (3rd place, key `2`):
- `nameSize: 14` -> `nameSize: 18`
- `countSize: 26` -> `countSize: 30`
- `height: 160` -> keep (DO NOT CHANGE)
- `width: 165` -> keep (DO NOT CHANGE)

In PodiumCard function:
- Line ~126: Change rank label `fontSize: 10` to `fontSize: 13` (D-06)
- Line ~212: Change premium `fontSize: 12` to `fontSize: 14` AND change `color: colors.textTertiary` to `color: colors.textSecondary` (D-03/D-04/D-05 premium + D-07 contrast promotion)
- Line ~152: Reduce podium card internal padding from `spacing[5]` top / `spacing[4]` horizontal to `spacing[3]` top / `spacing[3]` horizontal (absorb larger fonts per D-16)
- Line ~175: Reduce icon marginBottom from `spacing[2]` to `4` (tighten vertical spacing)
- Line ~190: Reduce name marginBottom from `spacing[2]` to `4` (tighten vertical spacing)

In PodiumPlatform function:
- Line ~247 (2nd place span): Change `fontSize: 11` to `fontSize: 13`
- Line ~262 (1st place span): Change `fontSize: 11` to `fontSize: 13`
- Line ~277 (3rd place span): Change `fontSize: 11` to `fontSize: 13`
  </action>
  <verify>
    <automated>cd C:/Users/javer/Documents/Repositories/ai-calling-backend && grep -n "nameSize: 22" apps/sales-board/app/page.tsx && grep -n "nameSize: 20" apps/sales-board/app/page.tsx && grep -n "nameSize: 18" apps/sales-board/app/page.tsx && grep -n "countSize: 32" apps/sales-board/app/page.tsx && grep -n "countSize: 30" apps/sales-board/app/page.tsx && grep -c "textSecondary" apps/sales-board/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PODIUM_CONFIG rank 0 contains `nameSize: 22` and `countSize: 36`
    - PODIUM_CONFIG rank 1 contains `nameSize: 20` and `countSize: 32`
    - PODIUM_CONFIG rank 2 contains `nameSize: 18` and `countSize: 30`
    - PODIUM_CONFIG rank 0 still contains `height: 220` and `width: 200` (unchanged)
    - PODIUM_CONFIG rank 1 still contains `height: 180` and `width: 175` (unchanged)
    - PODIUM_CONFIG rank 2 still contains `height: 160` and `width: 165` (unchanged)
    - PodiumCard rank label has `fontSize: 13`
    - PodiumCard premium has `fontSize: 14` and `color: colors.textSecondary`
    - PodiumPlatform all three spans have `fontSize: 13`
    - PodiumCard padding uses `spacing[3]` for both top and horizontal
  </acceptance_criteria>
  <done>Podium cards display enlarged fonts with promoted contrast, padding reduced to absorb growth, container dimensions unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Enlarge rest-of-agents fonts, labels, toggle, and promote contrast</name>
  <files>apps/sales-board/app/page.tsx</files>
  <read_first>
    - apps/sales-board/app/page.tsx (focus on lines 285-521 for DailyView function)
    - .planning/phases/34-dynamic-scaling-daily-view/34-CONTEXT.md (decisions D-08 through D-14, D-17)
  </read_first>
  <action>
In DailyView function, update these inline styles:

Section labels and toggle (lines ~326-356):
- "Top Performers" label (line ~327): Change `fontSize: 11` to `fontSize: 13` (D-12)
- "Top Performers" label (line ~328): Change `color: colors.textTertiary` to `color: colors.textSecondary` (additional contrast promotion)
- Crown icons next to "Top Performers" (lines ~323, 335): Change `size={14}` to `size={16}` (proportional crown scaling)
- Day/Week toggle buttons (line ~348): Change `fontSize: 12` to `fontSize: 14` (D-13)
- "All Agents" label (line ~389): Change `fontSize: 11` to `fontSize: 13` (D-12)
- "All Agents" label (line ~391): Change `color: colors.textMuted` to `color: colors.textSecondary` (contrast promotion)

Rest-of-agents columns (lines ~410-498):
- Rank badge number (line ~440): Change `fontSize: 11` to `fontSize: 13` (D-11)
- Rank badge color (line ~442): Change `color: colors.textMuted` to `color: colors.textSecondary` (contrast promotion)
- Agent name (line ~469): Change `fontSize: 14` to `fontSize: 18` (D-08)
- Sale count (line ~481): Change `fontSize: 28` to `fontSize: 32` (D-09)
- Premium amount (line ~492): Change `fontSize: 12` to `fontSize: 14` AND change `color: colors.textTertiary` to `color: colors.textSecondary` (D-10 + D-14)

Column padding adjustment (D-17, Claude's discretion):
- Line ~463: Change padding from `spacing[4]px spacing[2]px spacing[5]px` to `spacing[3]px spacing[2]px spacing[3]px` (reduce vertical padding to absorb larger fonts)
- Line ~454: Change `minHeight: 120` to `minHeight: 100` (allow columns to be shorter with tighter padding)
- Line ~473: Reduce name marginBottom from `spacing[2]` to `4` (tighten vertical spacing)
  </action>
  <verify>
    <automated>cd C:/Users/javer/Documents/Repositories/ai-calling-backend && grep -n "fontSize: 18" apps/sales-board/app/page.tsx | grep -v "nameSize" | head -5 && grep -n "fontSize: 32" apps/sales-board/app/page.tsx | head -5 && grep -n "fontSize: 13" apps/sales-board/app/page.tsx | head -10 && grep -n "fontSize: 14" apps/sales-board/app/page.tsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - DailyView agent name fontSize is 18 (not 14)
    - DailyView sale count fontSize is 32 (not 28)
    - DailyView premium fontSize is 14 (not 12) with `color: colors.textSecondary`
    - Rank badge fontSize is 13 (not 11) with `color: colors.textSecondary`
    - "Top Performers" label fontSize is 13 with `color: colors.textSecondary`
    - "All Agents" label fontSize is 13 with `color: colors.textSecondary`
    - Day/Week toggle fontSize is 14 (not 12)
    - Column padding uses `spacing[3]` for top and bottom (not spacing[4]/spacing[5])
    - No occurrences of `fontSize: 11` remain in the DailyView function (all promoted to 13+)
    - No occurrences of `colors.textTertiary` remain in the DailyView function (all promoted to textSecondary)
  </acceptance_criteria>
  <done>Rest-of-agents section, labels, and toggle display enlarged fonts with promoted contrast, padding adjusted for TV readability</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification on TV or browser</name>
  <what-built>DailyView (podium cards + rest-of-agents columns) with enlarged fonts, promoted contrast, and reduced padding for TV-distance readability. All changes in apps/sales-board/app/page.tsx.</what-built>
  <how-to-verify>
    1. Run `npm run salesboard:dev` (localhost:3013)
    2. Switch to DailyView (click the day/week tab area)
    3. Verify podium cards: 1st place name is noticeably larger, 2nd/3rd counts are bigger, premium text is brighter (#94a3b8 instead of dim tertiary)
    4. Verify rest-of-agents: agent names, sale counts, and premiums are all visibly larger than before
    5. Verify rank badges, section labels ("Top Performers", "All Agents"), and Day/Week toggle text are all larger
    6. Verify podium card dimensions look the same — they should not have grown
    7. Test with 15 agents on DailyView: verify all content fits without scrolling at your typical browser zoom level (SCAL-03)
    8. Switch to WeeklyView: verify 15 agents still fit without scrolling on 1080p (SCAL-02 sanity check — Phase 33 delivered this, confirming no regression)
    9. If on TV: confirm readability from 10-15 feet on both views
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run salesboard:dev` starts without errors
- DailyView renders with enlarged fonts on podium cards and rest-of-agents
- No horizontal or vertical overflow at 15-agent count on daily view
- Weekly view still fits 15 agents without scrolling (Phase 33 regression check)
- All textTertiary references in DailyView replaced with textSecondary
- Podium card height/width values unchanged in PODIUM_CONFIG
</verification>

<success_criteria>
1. Podium card fonts match exact targets: 1st name 22px, 2nd name 20px/count 32px, 3rd name 18px/count 30px, all premiums 14px
2. Rest-of-agents fonts match targets: names 18px, counts 32px, premiums 14px
3. All labels and badges at 13px+, toggle at 14px
4. No textTertiary or textMuted color remaining on DailyView text elements (all promoted to textSecondary)
5. Podium card dimensions unchanged (200x220, 175x180, 165x160)
6. 15 agents fit on daily view without scrolling
7. 15 agents fit on weekly view without scrolling (no regression from Phase 33)
8. User approves visual checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/34-dynamic-scaling-daily-view/34-01-SUMMARY.md`
</output>
