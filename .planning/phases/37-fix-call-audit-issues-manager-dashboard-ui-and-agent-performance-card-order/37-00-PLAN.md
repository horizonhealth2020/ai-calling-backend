---
phase: 37-fix-call-audit-issues-manager-dashboard-ui-and-agent-performance-card-order
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/ops-api/src/services/__tests__/auditQueue.test.ts
  - apps/ops-api/src/services/__tests__/compositeScore.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-10, D-11, D-12, D-13]

must_haves:
  truths:
    - "Test scaffolds exist BEFORE implementation plans run"
    - "auditQueue tests cover orphan recovery, retry backoff, and error categorization"
    - "compositeScore tests cover weighting, cost inversion, no-sales ranking, and division-by-zero"
  artifacts:
    - path: "apps/ops-api/src/services/__tests__/auditQueue.test.ts"
      provides: "Unit tests for orphan recovery, retry, categorizeError"
      contains: "recoverOrphanedJobs"
    - path: "apps/ops-api/src/services/__tests__/compositeScore.test.ts"
      provides: "Unit tests for composite score computation"
      contains: "computeCompositeScores"
  key_links:
    - from: "apps/ops-api/src/services/__tests__/auditQueue.test.ts"
      to: "apps/ops-api/src/services/auditQueue.ts"
      via: "imports functions under test"
      pattern: "import.*auditQueue"
    - from: "apps/ops-api/src/services/__tests__/compositeScore.test.ts"
      to: "computeCompositeScores function"
      via: "tests extracted pure function"
      pattern: "computeCompositeScores"
---

<objective>
Create test scaffolds for audit queue self-healing logic and composite performance scoring BEFORE implementation plans run.

Purpose: Plans 01 and 02 need behavioral tests to verify against. Writing tests first ensures implementations satisfy the required behaviors (D-01 through D-04 for queue, D-10 through D-13 for scoring).
Output: Two test files with failing tests that Plans 01 and 02 will make pass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-fix-call-audit-issues-manager-dashboard-ui-and-agent-performance-card-order/37-CONTEXT.md
@.planning/phases/37-fix-call-audit-issues-manager-dashboard-ui-and-agent-performance-card-order/37-RESEARCH.md

<interfaces>
<!-- Existing test infrastructure -->

From apps/ops-api/jest.config.ts:
```typescript
// Uses ts-jest, testMatch: ['**/__tests__/**/*.test.ts']
// moduleNameMapper: '@ops/db' -> mock, '@ops/*' -> packages/*/src
```

From apps/ops-api/src/services/__tests__/__mocks__/ops-db.ts:
```typescript
// Prisma mock — provides jest.fn() stubs for prisma methods
```

Existing test pattern (from apps/ops-api/src/services/__tests__/period-assignment.test.ts):
```typescript
import { PrismaClient } from "@prisma/client";
// Tests use mocked prisma client via moduleNameMapper
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auditQueue test scaffold</name>
  <files>apps/ops-api/src/services/__tests__/auditQueue.test.ts</files>
  <read_first>
    - apps/ops-api/src/services/__tests__/period-assignment.test.ts (existing test pattern)
    - apps/ops-api/src/services/__tests__/__mocks__/ops-db.ts (prisma mock setup)
    - apps/ops-api/src/services/auditQueue.ts (current exports and function signatures)
    - apps/ops-api/jest.config.ts (test config)
  </read_first>
  <action>
Create `apps/ops-api/src/services/__tests__/auditQueue.test.ts` with tests for the three new functions that Plan 01 will implement. The tests should mock `@ops/db` (Prisma) and test the pure logic.

**IMPORTANT:** These tests will FAIL initially (RED phase). Plan 01 will make them pass (GREEN phase).

The test file should cover:

1. **categorizeError tests (D-03, D-04):**
   - Error containing "Recording download failed" returns "recording_unavailable"
   - Error containing "Recording empty" returns "recording_unavailable"
   - Error containing "Invalid audio" returns "recording_unavailable"
   - Error containing "Whisper" returns "transcription_timeout"
   - Error containing "AbortError" returns "transcription_timeout"
   - Error containing "anthropic" returns "claude_api_error"
   - Error containing "429" returns "claude_api_error"
   - Error containing "overloaded" returns "claude_api_error"
   - Unknown error returns "unknown"

2. **recoverOrphanedJobs tests (D-01):**
   - Calls prisma.convosoCallLog.updateMany with status filter for processing/waiting_recording/transcribing/auditing
   - Sets auditStatus to "queued"
   - Returns count of recovered orphans
   - Logs recovery event when count > 0
   - Does not log when count is 0

3. **retryFailedAudits tests (D-02):**
   - Finds failed audits with retryCount < 3 and recordingUrl not null
   - Respects exponential backoff timing (1min, 5min, 15min)
   - Re-queues eligible audits by setting auditStatus to "queued"
   - Returns 0 when no eligible candidates
   - Skips candidates that failed too recently (backoff not elapsed)

Since `categorizeError`, `recoverOrphanedJobs`, and `retryFailedAudits` are not currently exported, the test file should note that Plan 01 must export these functions (or the test should import the module and access them). The recommended approach: Plan 01 should add `export` to these functions so tests can import them directly.

Alternatively, if the functions remain internal, extract `categorizeError` as a standalone exported helper (it's a pure function with no dependencies) and test `recoverOrphanedJobs`/`retryFailedAudits` indirectly through their effects on the mocked Prisma client. Follow the pattern used in the existing test files.
  </action>
  <verify>
    <automated>cd C:/Users/javer/Documents/Repositories/ai-calling-backend && test -f apps/ops-api/src/services/__tests__/auditQueue.test.ts && grep -q "categorizeError" apps/ops-api/src/services/__tests__/auditQueue.test.ts && grep -q "recoverOrphanedJobs" apps/ops-api/src/services/__tests__/auditQueue.test.ts && grep -q "retryFailedAudits" apps/ops-api/src/services/__tests__/auditQueue.test.ts</automated>
  </verify>
  <done>auditQueue.test.ts exists with failing tests for categorizeError (9 cases), recoverOrphanedJobs (3 cases), and retryFailedAudits (4 cases).</done>
</task>

<task type="auto">
  <name>Task 2: Create compositeScore test scaffold</name>
  <files>apps/ops-api/src/services/__tests__/compositeScore.test.ts</files>
  <read_first>
    - apps/ops-api/src/services/__tests__/period-assignment.test.ts (existing test pattern)
    - apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx (current TrackerEntry type and sort logic)
  </read_first>
  <action>
Create `apps/ops-api/src/services/__tests__/compositeScore.test.ts` with tests for the `computeCompositeScores` function that Plan 02 will implement in ManagerTracker.tsx.

**Key design decision:** Since `computeCompositeScores` is a pure function (no React, no side effects), extract it into a testable location. The test file should import from a shared location. Plan 02 must either:
(a) Export `computeCompositeScores` from ManagerTracker.tsx and the test imports it, OR
(b) Extract it to a utility file like `apps/ops-dashboard/lib/compositeScore.ts` and import from there.

Recommendation: Option (b) -- create `apps/ops-dashboard/lib/compositeScore.ts` as the implementation target. The test file should define the `TrackerEntry` type locally and test the function directly.

**IMPORTANT:** These tests will FAIL initially. Plan 02 will make them pass.

The test file should cover:

1. **Basic scoring (D-10, D-11):**
   - Agent with highest premium + lowest cost ranks first
   - Weights are 40% premium + 60% cost efficiency
   - Sale count is tiebreaker only (equal composite scores break by salesCount desc)

2. **Cost inversion (D-12):**
   - Lower costPerSale produces higher score
   - Agent A (costPerSale=50) ranks above Agent B (costPerSale=100) when premiums are equal

3. **Edge cases (D-13):**
   - Agents with salesCount=0 get compositeScore=-1 (rank last)
   - Agents with costPerSale=0 (no cost data) but has sales get costScore=0 (worst efficiency, not best)
   - Single agent with sales gets compositeScore based on normalized values (premScore=0 since range is 0, guarded by ||1)

4. **Division-by-zero guard:**
   - All agents with same premium: premRange guarded by ||1, all get premScore=0
   - All agents with same costPerSale: costRange guarded by ||1, all get costScore=0.5

Test data fixtures:
```typescript
const fixtures = {
  highPerformer: { agent: "Alice", salesCount: 10, premiumTotal: 50000, totalLeadCost: 500, costPerSale: 50, commissionTotal: 5000 },
  midPerformer:  { agent: "Bob",   salesCount: 8,  premiumTotal: 30000, totalLeadCost: 800, costPerSale: 100, commissionTotal: 3000 },
  lowPerformer:  { agent: "Carol", salesCount: 3,  premiumTotal: 10000, totalLeadCost: 600, costPerSale: 200, commissionTotal: 1000 },
  noSales:       { agent: "Dave",  salesCount: 0,  premiumTotal: 0,     totalLeadCost: 0,   costPerSale: 0,   commissionTotal: 0 },
  noCostData:    { agent: "Eve",   salesCount: 5,  premiumTotal: 20000, totalLeadCost: 0,   costPerSale: 0,   commissionTotal: 2000 },
};
```
  </action>
  <verify>
    <automated>cd C:/Users/javer/Documents/Repositories/ai-calling-backend && test -f apps/ops-api/src/services/__tests__/compositeScore.test.ts && grep -q "computeCompositeScores" apps/ops-api/src/services/__tests__/compositeScore.test.ts && grep -q "costPerSale" apps/ops-api/src/services/__tests__/compositeScore.test.ts && grep -q "compositeScore.*-1" apps/ops-api/src/services/__tests__/compositeScore.test.ts</automated>
  </verify>
  <done>compositeScore.test.ts exists with failing tests for basic scoring (3 cases), cost inversion (2 cases), edge cases (3 cases), and division-by-zero guard (2 cases).</done>
</task>

</tasks>

<verification>
- Both test files exist in apps/ops-api/src/services/__tests__/
- `npx jest --config apps/ops-api/jest.config.ts --testPathPattern=auditQueue` shows test file is recognized (tests will fail -- that's expected for Wave 0)
- `npx jest --config apps/ops-api/jest.config.ts --testPathPattern=compositeScore` shows test file is recognized (tests will fail -- expected)
</verification>

<success_criteria>
- auditQueue.test.ts has tests for categorizeError, recoverOrphanedJobs, retryFailedAudits
- compositeScore.test.ts has tests for weighting, cost inversion, no-sales, no-cost-data, division-by-zero
- Tests are RED (failing) -- Plan 01 and Plan 02 will make them GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/37-fix-call-audit-issues-manager-dashboard-ui-and-agent-performance-card-order/37-00-SUMMARY.md`
</output>
