---
phase: 10-sale-status-payroll-logic
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - apps/manager-dashboard/app/page.tsx
autonomous: true
requirements: [STATUS-09, STATUS-10, STATUS-11]

must_haves:
  truths:
    - "Sales entry form has a status dropdown with blank default and Ran/Declined/Dead options"
    - "Status dropdown is required — form cannot submit without selecting a status"
    - "Agent sales tab shows editable status dropdown per sale"
    - "Changing status to Ran on a Dead/Declined sale shows inline confirmation before proceeding"
    - "StatusBadge shows Ran (green), Declined (red), Dead (gray), Pending Ran (amber with clock icon)"
    - "Pending Ran badge is distinct from other statuses"
  artifacts:
    - path: "apps/manager-dashboard/app/page.tsx"
      provides: "Status dropdown in sales form, editable status in agent sales tab, updated StatusBadge"
      contains: "PENDING_RAN"
  key_links:
    - from: "apps/manager-dashboard/app/page.tsx (sales form)"
      to: "POST /api/sales"
      via: "authFetch with status field in body"
      pattern: "status.*RAN"
    - from: "apps/manager-dashboard/app/page.tsx (agent sales tab)"
      to: "PATCH /api/sales/:id/status"
      via: "authFetch for status change"
      pattern: "sales/.*/status"
---

<objective>
Update the manager dashboard with status dropdown on the sales entry form and editable status on the agent sales tab.

Purpose: Managers need to set sale status at creation time and change it afterwards. The UI must enforce the approval workflow for Dead/Declined-to-Ran transitions.
Output: Updated manager dashboard with status dropdown, editable agent sales tab status, and updated StatusBadge component.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-sale-status-payroll-logic/10-CONTEXT.md
@.planning/phases/10-sale-status-payroll-logic/10-RESEARCH.md
@.planning/phases/10-sale-status-payroll-logic/10-01-SUMMARY.md

@apps/manager-dashboard/app/page.tsx

<interfaces>
<!-- StatusBadge component (manager-dashboard/app/page.tsx line 352): -->
function StatusBadge({ status }: { status: string }) {
  const map: Record<string, string> = {
    APPROVED: colors.success,
    REJECTED: colors.danger,
    CANCELLED: colors.textSecondary,
    SUBMITTED: colors.warning,
  };
  // ... renders colored badge
}

<!-- New API endpoints from Plan 02: -->
PATCH /api/sales/:id/status  { status: "RAN" | "DECLINED" | "DEAD" }
  - Dead/Declined->Ran returns { changeRequest, message }
  - Other transitions return updated sale

<!-- POST /api/sales now expects: -->
status: "RAN" | "DECLINED" | "DEAD"  (required, no default)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Status dropdown on sales entry form and StatusBadge update</name>
  <files>apps/manager-dashboard/app/page.tsx</files>
  <action>
1. Update the StatusBadge component (line ~352):
   - Replace the color map with new statuses:
     RAN: '#22c55e' (green), DECLINED: '#ef4444' (red), DEAD: '#6b7280' (gray), PENDING_RAN: '#f59e0b' (amber)
   - For PENDING_RAN, add a small clock/pending indicator — use a Unicode character like "\u23F3" (hourglass) or "\u25F7" (clock) rendered next to the text, or simply show "Pending Ran" text with the amber background.
   - Display text should be: RAN -> "Ran", DECLINED -> "Declined", DEAD -> "Dead", PENDING_RAN -> "Pending Ran"

2. Add status dropdown to the sales entry form:
   - Add a `status` state variable initialized to "" (empty string for blank default).
   - Create a `<select>` dropdown styled with the existing INP (input) CSSProperties constant.
   - Options: blank/disabled "Select status..." (value=""), "Ran" (value="RAN"), "Declined" (value="DECLINED"), "Dead" (value="DEAD").
   - Place the dropdown in the form layout near the payment type field (logical grouping).
   - Include status in the form submission body sent to POST /api/sales.
   - Add validation: if status is "" (not selected), prevent form submission and show an error message like "Status is required" using the existing alert/message pattern.

3. Reset status to "" when form is successfully submitted (along with other field resets).
  </action>
  <verify>
    <automated>cd C:/projects/ai-calling-backend && npx tsc --noEmit -p apps/manager-dashboard/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Sales form has required status dropdown defaulting to blank. StatusBadge renders correct colors for RAN/DECLINED/DEAD/PENDING_RAN. Form validation prevents submission without status selection.</done>
</task>

<task type="auto">
  <name>Task 2: Editable status dropdown on agent sales tab with confirmation</name>
  <files>apps/manager-dashboard/app/page.tsx</files>
  <action>
1. In the agent sales tab (the table/list that shows individual sales with a status column):
   - Replace the static StatusBadge in the status column with a `<select>` dropdown for editable status.
   - Style the dropdown to match the badge appearance — use inline CSSProperties with the status color as background, white text, rounded corners, small padding. Follow the dark glassmorphism theme.
   - Options: "Ran" (RAN), "Declined" (DECLINED), "Dead" (DEAD).
   - The current value should reflect the sale's current status.
   - If the sale has a pending change request (status shows as PENDING_RAN from the API), render the PENDING_RAN StatusBadge instead of the dropdown (not editable while pending).

2. On status change via the dropdown:
   - If changing FROM Dead/Declined TO Ran: show an inline confirmation before calling the API. Use `window.confirm("This will create a change request for payroll approval. Continue?")` for simplicity (per Claude's discretion — lightweight inline confirmation).
   - If confirmed, call PATCH /api/sales/:id/status with the new status using authFetch.
   - If the API returns a changeRequest in the response (Dead/Declined->Ran), show a success toast/alert: "Change request submitted for payroll approval".
   - For all other transitions (Ran->Dead/Declined, Dead<->Declined), call the same endpoint. On success, update the local state to reflect the new status.
   - On API error, show the error message and revert the dropdown to the previous value.

3. After any status change, refetch the sales list to get the latest data (or optimistically update local state).

4. Handle the "pending" state in the GET /sales response: the API returns sales with their current status. To detect PENDING_RAN, either:
   - Option A: Have the GET /sales endpoint include a `hasPendingStatusChange` boolean (add this to the GET /sales route in routes/index.ts if needed — include it by checking for PENDING StatusChangeRequests for each sale). This is a small addition to routes/index.ts.
   - Option B: Fetch pending change requests separately. Option A is simpler — use it.
   - If Option A is used, add to the GET /sales endpoint: for each sale, include `hasPendingStatusChange: boolean` by doing a subquery or join. Use `_count: { statusChangeRequests: { where: { status: 'PENDING' } } }` in the Prisma include, then map to a boolean.
  </action>
  <verify>
    <automated>cd C:/projects/ai-calling-backend && npx tsc --noEmit -p apps/manager-dashboard/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Agent sales tab shows editable status dropdown per sale. Dead/Declined->Ran triggers confirmation dialog. Pending Ran sales show amber badge instead of dropdown. Status changes call PATCH /api/sales/:id/status. GET /sales includes hasPendingStatusChange flag.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit -p apps/manager-dashboard/tsconfig.json`
- StatusBadge uses correct colors for all four statuses
- Sales form includes required status dropdown
- Agent sales tab has editable status with confirmation for Dead/Declined->Ran
</verification>

<success_criteria>
- Sales entry form has status dropdown defaulting to blank, manager must choose
- Submitting without status selection shows validation error
- Agent sales tab shows editable status dropdown (or Pending Ran badge for pending sales)
- Changing Dead/Declined to Ran shows confirmation dialog
- Successful Dead/Declined->Ran shows "change request submitted" message
- StatusBadge shows Ran=green, Declined=red, Dead=gray, Pending Ran=amber
</success_criteria>

<output>
After completion, create `.planning/phases/10-sale-status-payroll-logic/10-03-SUMMARY.md`
</output>
