---
phase: 10-sale-status-payroll-logic
plan: 06
type: execute
wave: 5
depends_on: [10-04]
files_modified:
  - apps/payroll-dashboard/app/page.tsx
autonomous: false
requirements: [STATUS-12, STATUS-13, STATUS-14]

must_haves:
  truths:
    - "Bonus, fronted, and commission totals are displayed prominently at the top of each agent pay card, not only inline per-row"
    - "Agent-level bonus and fronted are editable from the card header summary"
    - "Chargeback (CLAWBACK_APPLIED) payroll entry rows have a red-tinted background"
    - "Declined/Dead sale rows have an orange-tinted background until status changes to RAN"
    - "Product and premium fields are editable inline when edit mode is active on a sale row"
  artifacts:
    - path: "apps/payroll-dashboard/app/page.tsx"
      provides: "Agent card header financial summary, row status shading, editable product/premium"
      contains: "CLAWBACK_APPLIED"
  key_links:
    - from: "apps/payroll-dashboard/app/page.tsx (EditableSaleRow)"
      to: "PATCH /api/sales/:id"
      via: "onSaleUpdate sends productId and premium"
      pattern: "productId.*saleData"
    - from: "apps/payroll-dashboard/app/page.tsx (agent card header)"
      to: "PATCH /api/payroll-entries/:id"
      via: "onBonusFrontedUpdate from header inputs"
      pattern: "bonusFrontedUpdate"
---

<objective>
Enhance payroll dashboard agent pay cards with four UX improvements: (1) financial summary at top of each agent card with editable bonus/fronted/commission, (2) red-tinted rows for chargebacks, (3) orange-tinted rows for Declined/Dead sales, (4) editable product and premium fields in sale rows.

Purpose: The user wants faster scanning and editing of payroll data -- financial summaries at the card level, visual status differentiation via row colors, and inline editing of product/premium fields.
Output: Updated payroll dashboard with all four enhancements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-sale-status-payroll-logic/10-CONTEXT.md
@.planning/phases/10-sale-status-payroll-logic/10-04-SUMMARY.md

@apps/payroll-dashboard/app/page.tsx

<interfaces>
<!-- PATCH /api/sales/:id already accepts productId and premium in its Zod schema (lines 436-437 of routes/index.ts): -->
```typescript
const editSchema = z.object({
  productId: z.string().optional(),
  premium: z.number().min(0).optional(),
  // ... other fields already accepted
});
```

<!-- Products state already exists in the payroll dashboard (line 747): -->
```typescript
const [products, setProducts] = useState<Product[]>([]);
// Loaded on mount from GET /api/products
```

<!-- Entry type includes sale with status and product: -->
```typescript
type Entry = {
  id: string; payoutAmount: number; adjustmentAmount: number; bonusAmount: number;
  frontedAmount: number; holdAmount: number; netAmount: number; status: string;
  sale?: SaleInfo;
};
// Entry.status can be: PENDING, READY, PAID, ZEROED_OUT, CLAWBACK_APPLIED
// Entry.sale.status can be: RAN, DECLINED, DEAD
```

<!-- EditableSaleRow currently sends to onSaleUpdate (line 396-402): -->
```typescript
await onSaleUpdate(entry.sale!.id, {
  memberName: saleData.memberName,
  memberId: saleData.memberId || null,
  enrollmentFee: saleData.enrollmentFee ? Number(saleData.enrollmentFee) : null,
  notes: saleData.notes || null,
});
```

<!-- Agent card header (line 1610-1653) shows agentName, sale count, Commission total, Net total -->
<!-- Agent card subtotal row (line 1691-1693) shows gross below the table -->

<!-- isActiveEntry helper (line 189-195) filters ZEROED_OUT and non-RAN entries from totals -->
<!-- rowBg logic (line 250-254): currently borderLeft only, no background color -->

<!-- onBonusFrontedUpdate updates a single entry's bonus/fronted/hold -->
<!-- Products list is available in the parent scope but NOT passed to EditableSaleRow -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Row status shading and editable product/premium in EditableSaleRow</name>
  <files>apps/payroll-dashboard/app/page.tsx</files>
  <read_first>apps/payroll-dashboard/app/page.tsx (lines 218-458 for EditableSaleRow, lines 182-186 for SALE_STATUS_COLORS, lines 250-254 for rowBg)</read_first>
  <action>
1. **Update EditableSaleRow props** to accept `products: Product[]` so the component can render a product dropdown in edit mode.

2. **Update rowBg logic** (currently at line 250-254) to add background colors based on entry/sale status. Replace the existing logic with:
   - If `entry.status === 'CLAWBACK_APPLIED'`: set `backgroundColor: 'rgba(239,68,68,0.08)'` (red tint) AND keep `borderLeft: '3px solid rgba(239,68,68,0.4)'`
   - Else if sale status is DECLINED or DEAD (`saleStatus === 'DECLINED' || saleStatus === 'DEAD'`): set `backgroundColor: 'rgba(251,191,36,0.08)'` (orange/amber tint) AND `borderLeft: '3px solid rgba(251,191,36,0.4)'`. Remove the old opacity: 0.55 for zeroed entries -- the orange background replaces it.
   - Else if `needsApproval`: keep existing `borderLeft: '3px solid rgba(248,113,113,0.5)'`
   - Else: `borderLeft: '3px solid transparent'`

3. **Make product editable** in the Product column (currently lines 291-311). When `editSale` is true:
   - Render a `<select>` dropdown populated from the `products` prop, filtered to show only active products. The current product should be pre-selected.
   - Add `productId` to the `saleData` state, initialized from `entry.sale?.product?.id ?? ""`.
   - Style the select with SMALL_INP styles plus `width: 140`.

4. **Make premium editable** in the Product column. When `editSale` is true:
   - Render a number input below the product dropdown for premium.
   - Add `premium` to the `saleData` state (already partially there at line 233 as `premium: String(entry.sale?.premium ?? "")`).
   - Style with SMALL_INP, width: 90.

5. **Include productId and premium in the save payload** (line 396-402). Update the onSaleUpdate call to also send:
   ```
   productId: saleData.productId || undefined,
   premium: saleData.premium ? Number(saleData.premium) : undefined,
   ```
   The API already accepts both fields in its Zod schema.

6. **Update the EditableSaleRow call site** (around line 1680) to pass `products={products}` as a prop. The `products` state is already available in the parent scope.
  </action>
  <verify>
    <automated>cd C:/projects/ai-calling-backend && npx tsc --noEmit -p apps/payroll-dashboard/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep: "CLAWBACK_APPLIED" exists in rowBg logic in EditableSaleRow
    - grep: "rgba(239,68,68,0.08)" appears for chargeback row background
    - grep: "rgba(251,191,36,0.08)" appears for declined/dead row background
    - grep: "productId" appears in saleData state and in onSaleUpdate payload
    - grep: "<select" appears in EditableSaleRow for product dropdown
    - grep: "products={products}" appears at EditableSaleRow call site
  </acceptance_criteria>
  <done>Chargeback rows have red background tint. Declined/Dead rows have orange background tint. Product and premium are editable inline when edit mode is active. All changes type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Agent card header financial summary with editable bonus/fronted/commission</name>
  <files>apps/payroll-dashboard/app/page.tsx</files>
  <read_first>apps/payroll-dashboard/app/page.tsx (lines 1584-1700 for agent card header and table, lines 1625-1627 for current Commission/Net display)</read_first>
  <action>
1. **Add a financial summary section** between the agent header row (line 1610-1653) and the commission table (line 1661). This section sits INSIDE each agent's card, below the agent name/buttons row and above the date range row.

2. **Summary section layout**: A horizontal strip with 4 fields: Commission (read-only total), Bonus (editable total), Fronted (editable total), Hold (editable total). Use a flex row with gap, styled with:
   ```
   {
     display: 'flex', gap: '16px', alignItems: 'center',
     padding: '10px 20px',
     background: 'rgba(255,255,255,0.02)',
     borderBottom: `1px solid ${C.borderSubtle}`,
   }
   ```
   Each field: a label (fontSize: 11, color: C.textMuted, textTransform: uppercase) above a value/input.

3. **Commission total**: Display-only, large font (fontSize: 16, fontWeight: 700, color: C.textPrimary). Shows `agentGross` value with `$` prefix.

4. **Bonus total editable**: Show the sum of all entry bonuses for this agent. Render as an editable number input (using SMALL_INP style with green-tinted background when > 0, same pattern as per-row bonus inputs). On blur, distribute the new total proportionally across the agent's entries by calling `onBonusFrontedUpdate` for each entry. Implementation approach:
   - Calculate `totalBonus = entries.reduce((s, e) => s + Number(e.bonusAmount), 0)`
   - On blur, if the value changed, calculate the delta and add it to the first active entry's bonus (simplest approach -- avoid complex proportional distribution). Call `onBonusFrontedUpdate(entries[0].id, newFirstBonus, entries[0].frontedAmount, entries[0].holdAmount)`.
   - This keeps it simple: the header input adjusts the first entry's bonus to make the total match.

5. **Fronted total editable**: Same pattern as bonus. Shows sum of frontedAmount. On change, adjusts first entry's fronted value.

6. **Hold total editable**: Same pattern. Shows sum of holdAmount. On change, adjusts first entry's hold.

7. **Use local state** for the header inputs. Create a small inline component or use useState within the agent map callback (since it's already inside a render function). Use `useState` with keys like `headerBonus_${agentName}` or extract into a small `AgentHeaderSummary` component defined inside the file. The simplest approach: add state variables for each agent's header values using a `Record<string, { bonus: string; fronted: string; hold: string }>` state at the period card level. Initialize from entry sums when data loads.

   Actually, the cleanest approach: extract a small `AgentPayCard` component (defined inside the file, before the main component) that receives `entries`, `agentName`, `agentGross`, `agentNet`, `activeCount`, `isTopEarner`, and callback props. This component manages its own header input state. This avoids polluting the parent with per-agent state.

8. **Keep the per-row bonus/fronted/hold inputs** as they are. The header is a summary/shortcut, not a replacement. When the header input changes and updates an entry, the per-row inputs should reflect the new values after `refreshPeriods()` is called.

9. **Move Commission and Net from the current header location** (lines 1625-1627) into this new summary strip so they are not duplicated. The agent name row should keep: agent name, top earner badge, sale count, print button, paid/unpaid button. The financial totals move to the new summary strip below.
  </action>
  <verify>
    <automated>cd C:/projects/ai-calling-backend && npx tsc --noEmit -p apps/payroll-dashboard/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep: "Commission" label appears in a summary strip section (not just in the old header location)
    - grep: "totalBonus" or "agentBonus" computed from entries.reduce for the header summary
    - grep: editable input for bonus in the header area (type="number" near the Commission label)
    - grep: "Fronted" label in the summary strip alongside Bonus and Hold
    - grep: "AgentPayCard" or equivalent component/section that encapsulates the agent card with header summary
  </acceptance_criteria>
  <done>Each agent pay card has a prominent financial summary strip at the top showing Commission (read-only), Bonus (editable), Fronted (editable), and Hold (editable). Editing header values updates the underlying payroll entries. Per-row inputs remain for granular editing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Four payroll dashboard UX enhancements: (1) financial summary at top of each agent card with editable bonus/fronted/hold, (2) red-tinted rows for CLAWBACK_APPLIED entries, (3) orange-tinted rows for Declined/Dead sales, (4) inline-editable product dropdown and premium field in sale rows.</what-built>
  <how-to-verify>
    1. Start dev servers: `npm run ops:dev` and `npm run payroll:dev`
    2. Open payroll dashboard at http://localhost:3012
    3. Expand a pay period with agent entries
    4. Verify agent card header: Should show a summary strip with Commission (read-only), Bonus (editable input), Fronted (editable input), Hold (editable input)
    5. Edit the Bonus value in the header strip -- verify it saves and the per-row bonus updates on refresh
    6. Check row colors:
       a. If any CLAWBACK_APPLIED entry exists, its row should have a subtle red background
       b. If any Declined or Dead sale exists, its row should have a subtle orange/amber background
       c. RAN entries should have no special background
    7. Click "Edit" on a sale row:
       a. Verify a product dropdown appears with the current product pre-selected
       b. Verify a premium input appears with the current premium value
       c. Change the product and/or premium, click Save
       d. Verify the changes persist after save
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles for payroll-dashboard without errors
- Chargeback rows visually distinct with red tint
- Declined/Dead rows visually distinct with orange tint
- Product and premium editable in edit mode
- Agent card header shows financial summary with editable fields
- All inline styles use React.CSSProperties (no Tailwind, no CSS classes for styling)
</verification>

<success_criteria>
- Agent pay cards have a financial summary strip at the top with editable bonus/fronted/hold and read-only commission
- CLAWBACK_APPLIED entry rows render with rgba(239,68,68,0.08) red background
- DECLINED and DEAD sale rows render with rgba(251,191,36,0.08) orange background
- Product column shows a dropdown in edit mode populated from the products list
- Premium field is editable as a number input in edit mode
- Saving edits sends productId and premium to PATCH /api/sales/:id
- All changes follow inline CSSProperties pattern (no Tailwind)
</success_criteria>

<output>
After completion, create `.planning/phases/10-sale-status-payroll-logic/10-06-SUMMARY.md`
</output>
