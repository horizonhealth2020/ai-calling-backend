---
phase: 19-dashboard-consolidation-uniform-date-ranges
plan: 08
type: execute
wave: 4
depends_on: ["19-07"]
files_modified:
  - apps/ops-api/src/index.ts
  - docker-compose.yml
  - package.json
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04

must_haves:
  truths:
    - "CORS config allows only 2 origins: localhost:3011 (unified app) and localhost:3013 (sales board)"
    - "Docker runs 4 containers: postgres, ops-api, ops-dashboard, sales-board (not 8)"
    - "Old standalone dashboard app directories are deleted"
    - "Sales board still builds and runs on port 3013"
    - "Root package.json dev scripts updated for unified app"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Consolidated Docker config"
      contains: "ops-dashboard"
    - path: "apps/ops-api/src/index.ts"
      provides: "Updated CORS default"
      contains: "localhost:3011"
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile.nextjs"
      via: "APP_NAME: ops-dashboard"
      pattern: "ops-dashboard"
    - from: "apps/ops-api/src/index.ts"
      to: "ALLOWED_ORIGINS"
      via: "CORS middleware"
      pattern: "ALLOWED_ORIGINS"
---

<objective>
Update deployment configuration: CORS for unified app, Docker consolidation (1 container instead of 5), remove old standalone dashboard directories, verify sales board remains functional.

Purpose: Clean up infrastructure to match the consolidated app architecture. Reduce runtime from 8 containers to 4.
Output: Updated docker-compose.yml, CORS config, removed old app directories.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-dashboard-consolidation-uniform-date-ranges/19-CONTEXT.md
@.planning/phases/19-dashboard-consolidation-uniform-date-ranges/19-RESEARCH.md

<interfaces>
From docker-compose.yml (current):
- 7 services: postgres, ops-api, auth-portal, sales-board, manager-dashboard, owner-dashboard, payroll-dashboard, cs-dashboard
- ALLOWED_ORIGINS default: http://localhost:3011,http://localhost:3012,http://localhost:3013,http://localhost:3014,http://localhost:3019,http://localhost:3026

From apps/ops-api/src/index.ts:
- CORS uses ALLOWED_ORIGINS env var with a default fallback
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CORS config, Docker, and root scripts</name>
  <files>
    apps/ops-api/src/index.ts,
    docker-compose.yml,
    package.json
  </files>
  <read_first>
    apps/ops-api/src/index.ts,
    docker-compose.yml,
    package.json,
    apps/ops-dashboard/package.json,
    Dockerfile.nextjs
  </read_first>
  <action>
1. **Update CORS default in `apps/ops-api/src/index.ts`:**
   - Find the `ALLOWED_ORIGINS` default value (currently includes 6 origins for all standalone apps)
   - Change default to: `http://localhost:3011,http://localhost:3013` (unified dashboard + sales board)
   - The env var override still works — production deploys set their own ALLOWED_ORIGINS

2. **Update `docker-compose.yml`:**
   - REMOVE these services: `auth-portal`, `manager-dashboard`, `owner-dashboard`, `payroll-dashboard`, `cs-dashboard`
   - ADD new `ops-dashboard` service:
     ```yaml
     ops-dashboard:
       build:
         context: .
         dockerfile: Dockerfile.nextjs
         args:
           APP_NAME: ops-dashboard
           NEXT_PUBLIC_OPS_API_URL: ${OPS_API_URL:-http://localhost:8080}
       restart: unless-stopped
       depends_on:
         - ops-api
       environment:
         AUTH_JWT_SECRET: ${AUTH_JWT_SECRET}
       ports:
         - "3011:3000"
     ```
   - KEEP: `postgres`, `ops-api`, `sales-board` services unchanged
   - Update `ops-api` service `ALLOWED_ORIGINS` default:
     `ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:3011,http://localhost:3013}`
   - Final services list: postgres, ops-api, ops-dashboard, sales-board (4 total)
   - Remove old dashboard URL env vars from ops-api that are no longer needed: `MANAGER_DASHBOARD_URL`, `PAYROLL_DASHBOARD_URL`, `OWNER_DASHBOARD_URL`, `CS_DASHBOARD_URL` (these were only used by auth-portal which is now gone)

3. **Update root `package.json` scripts:**
   - Add: `"dashboard:dev": "npm run dev --workspace=apps/ops-dashboard"` (if not already added in Plan 01)
   - Remove or comment out old dev scripts that reference deleted apps: `auth:dev`, `payroll:dev`, `manager:dev`, `owner:dev`
   - KEEP: `salesboard:dev` (sales board stays standalone), `ops:dev` (API stays)
   - Keep `dashboard:dev` script pointing to ops-dashboard
  </action>
  <verify>
    <automated>grep "ALLOWED_ORIGINS" docker-compose.yml && grep -c "auth-portal\|manager-dashboard\|owner-dashboard\|payroll-dashboard\|cs-dashboard" docker-compose.yml && grep "ops-dashboard" docker-compose.yml | head -3</automated>
  </verify>
  <acceptance_criteria>
    - apps/ops-api/src/index.ts ALLOWED_ORIGINS default contains only `localhost:3011` and `localhost:3013`
    - docker-compose.yml contains `ops-dashboard` service with `APP_NAME: ops-dashboard` and port `3011:3000`
    - docker-compose.yml does NOT contain `auth-portal`, `manager-dashboard`, `owner-dashboard`, `payroll-dashboard`, or `cs-dashboard` services
    - docker-compose.yml ALLOWED_ORIGINS updated to `http://localhost:3011,http://localhost:3013`
    - docker-compose.yml keeps `postgres`, `ops-api`, `sales-board` services
    - root package.json has `dashboard:dev` script
    - root package.json does not have dev scripts for removed apps (auth:dev, payroll:dev, manager:dev, owner:dev)
    - root package.json keeps `salesboard:dev` and `ops:dev`
  </acceptance_criteria>
  <done>CORS, Docker, and scripts updated for unified app architecture</done>
</task>

<task type="auto">
  <name>Task 2: Remove old standalone dashboard app directories</name>
  <files>
    (directories to delete)
  </files>
  <read_first>
    apps/ops-dashboard/package.json
  </read_first>
  <action>
Delete the following directories that are now superseded by the unified app:

```bash
rm -rf apps/auth-portal
rm -rf apps/manager-dashboard
rm -rf apps/payroll-dashboard
rm -rf apps/owner-dashboard
rm -rf apps/cs-dashboard
```

Do NOT delete:
- `apps/ops-api` — still the backend API
- `apps/sales-board` — stays standalone
- `apps/ops-dashboard` — the new unified app

After deletion, run `npm install` from the monorepo root to update the workspace resolution (the removed packages will be cleaned from node_modules).

Also update `tsconfig.base.json` if it references any of the deleted app paths (check `references` or `paths` sections).
  </action>
  <verify>
    <automated>ls apps/ && npm install 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `apps/auth-portal` directory does not exist
    - `apps/manager-dashboard` directory does not exist
    - `apps/payroll-dashboard` directory does not exist
    - `apps/owner-dashboard` directory does not exist
    - `apps/cs-dashboard` directory does not exist
    - `apps/ops-api` directory still exists
    - `apps/sales-board` directory still exists
    - `apps/ops-dashboard` directory still exists
    - `npm install` completes without errors
  </acceptance_criteria>
  <done>Old standalone dashboard directories removed, workspace clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete unified dashboard app with all 4 migrated dashboards, date range filtering, and deployment cleanup. The following has been automated:
1. Unified app shell with login, auth middleware, role-gated tab navigation
2. All 4 dashboards migrated (CS, Owner, Payroll, Manager) with identical features
3. DateRangeFilter wired to all KPI sections with uniform presets
4. CORS, Docker, and scripts updated
5. Old app directories removed
  </what-built>
  <how-to-verify>
1. Start the stack: `npm run ops:dev` in one terminal, `npm run dashboard:dev` in another
2. Open http://localhost:3011 — verify login form renders with "Horizon Operations" title and H favicon
3. Login as SUPER_ADMIN — verify redirect to /owner, all 4 tabs visible (Manager, Payroll, Owner, CS)
4. Login as MANAGER — verify redirect to /manager, NO tab bar (single role)
5. Login as PAYROLL — verify redirect to /payroll
6. Login as CUSTOMER_SERVICE — verify redirect to /cs
7. As SUPER_ADMIN on Owner tab: verify all sub-tabs work (Overview, Agent KPIs, AI Config, Users)
8. Switch to Manager tab: verify all sub-tabs (Entry, Tracker, Sales, Audits, Config)
9. Switch to Payroll tab: verify all sub-tabs (Periods, Chargebacks, Exports, Service, Products)
10. Switch to CS tab: verify all sub-tabs (Submissions, Tracking)
11. On any tab: verify DateRangeFilter shows 4 presets (Current Week, Last Week, 30 Days, Custom)
12. Select "Last Week" on Manager Tracker — verify KPI numbers change
13. Switch to Owner tab — verify "Last Week" is still selected (persisted)
14. Switch back to Manager — verify "Last Week" still selected
15. Try "Custom" preset — date inputs appear, entering dates updates KPIs
16. Verify socket: create a sale on Manager Entry, verify it appears in Sales tab without refresh
17. Start sales board: `npm run salesboard:dev`, open http://localhost:3013 — verify it works
18. Verify browser back/forward works between tabs
19. Navigate directly to http://localhost:3011/manager — verify it loads (URL routing works)
20. Navigate to http://localhost:3011/manager without auth — verify redirect to login
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- CORS config has 2 origins (3011, 3013)
- Docker compose has 4 services (postgres, ops-api, ops-dashboard, sales-board)
- Old app directories deleted
- Sales board still functional at port 3013
- Unified app functional at port 3011
- All role types can login and see correct tabs
- Date range filtering works across all tabs
</verification>

<success_criteria>
- CORS updated for 2-origin setup
- Docker consolidated from 8 to 4 containers
- Old directories removed
- Sales board unaffected
- Full end-to-end verification passed by human
</success_criteria>

<output>
After completion, create `.planning/phases/19-dashboard-consolidation-uniform-date-ranges/19-08-SUMMARY.md`
</output>
