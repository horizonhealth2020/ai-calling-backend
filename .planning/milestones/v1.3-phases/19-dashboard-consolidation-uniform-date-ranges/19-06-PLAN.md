---
phase: 19-dashboard-consolidation-uniform-date-ranges
plan: 06
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - apps/ops-dashboard/app/(dashboard)/manager/page.tsx
  - apps/ops-dashboard/app/(dashboard)/manager/ManagerEntry.tsx
  - apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx
  - apps/ops-dashboard/app/(dashboard)/manager/ManagerSales.tsx
  - apps/ops-dashboard/app/(dashboard)/manager/ManagerAudits.tsx
  - apps/ops-dashboard/app/(dashboard)/manager/ManagerConfig.tsx
autonomous: true
requirements:
  - MIG-04

must_haves:
  truths:
    - "Manager tab at /manager renders with 5 sub-tabs matching standalone app"
    - "All Manager dashboard features work identically to standalone app"
    - "Sales entry form creates sales and updates payroll"
    - "Agent tracker shows agent performance data"
    - "Socket.IO events (sale:changed) received via SocketProvider context"
    - "Sub-tab switching works via PageShell sidebar"
  artifacts:
    - path: "apps/ops-dashboard/app/(dashboard)/manager/page.tsx"
      provides: "Manager dashboard orchestrator"
      contains: "ManagerEntry"
    - path: "apps/ops-dashboard/app/(dashboard)/manager/ManagerEntry.tsx"
      provides: "Manager Sales Entry sub-tab"
      contains: "use client"
    - path: "apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx"
      provides: "Manager Agent Tracker sub-tab"
      contains: "use client"
    - path: "apps/ops-dashboard/app/(dashboard)/manager/ManagerSales.tsx"
      provides: "Manager Sales List sub-tab"
      contains: "use client"
    - path: "apps/ops-dashboard/app/(dashboard)/manager/ManagerAudits.tsx"
      provides: "Manager Call Audits sub-tab"
      contains: "use client"
    - path: "apps/ops-dashboard/app/(dashboard)/manager/ManagerConfig.tsx"
      provides: "Manager Config sub-tab"
      contains: "use client"
  key_links:
    - from: "apps/ops-dashboard/app/(dashboard)/manager/page.tsx"
      to: "@ops/ui"
      via: "PageShell import"
      pattern: "PageShell"
    - from: "apps/ops-dashboard/app/(dashboard)/manager/page.tsx"
      to: "apps/ops-dashboard/lib/SocketProvider.tsx"
      via: "useSocketContext"
      pattern: "useSocketContext"
---

<objective>
Migrate the Manager dashboard (2,702 lines, 5 sub-tabs with deeply shared state) from `apps/manager-dashboard/app/page.tsx` into the unified app at `apps/ops-dashboard/app/(dashboard)/manager/`.

Purpose: Manager is the most complex dashboard with deeply interrelated state across sub-tabs (agents, products, lead sources shared between Entry, Tracker, Sales, Config). Sales entry is the core business operation.
Output: Fully functional Manager tab with Entry, Tracker, Sales, Audits, and Config sub-tabs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-dashboard-consolidation-uniform-date-ranges/19-CONTEXT.md
@.planning/phases/19-dashboard-consolidation-uniform-date-ranges/19-RESEARCH.md
@.planning/phases/19-dashboard-consolidation-uniform-date-ranges/19-01-SUMMARY.md

<interfaces>
From apps/ops-dashboard/lib/SocketProvider.tsx:
```typescript
export const useSocketContext: () => { socket: SocketClient | null; disconnected: boolean };
```

From apps/ops-dashboard/lib/DateRangeContext.tsx:
```typescript
export const useDateRange: () => { value: DateRangeFilterValue; onChange: (v: DateRangeFilterValue) => void };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Manager sub-tab components from standalone app</name>
  <files>
    apps/ops-dashboard/app/(dashboard)/manager/ManagerEntry.tsx,
    apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx,
    apps/ops-dashboard/app/(dashboard)/manager/ManagerSales.tsx,
    apps/ops-dashboard/app/(dashboard)/manager/ManagerAudits.tsx,
    apps/ops-dashboard/app/(dashboard)/manager/ManagerConfig.tsx
  </files>
  <read_first>
    apps/manager-dashboard/app/page.tsx
  </read_first>
  <action>
Read the entire `apps/manager-dashboard/app/page.tsx` (2,702 lines). It has 5 sub-tabs with heavily shared state. This is the most complex migration.

FIRST: Identify all shared state that spans multiple sub-tabs:
- `agents` array — used by Entry (agent select), Tracker (agent list), Sales (agent display), Config (agent management)
- `products` array — used by Entry (product select), Sales (product display), Config (product management)
- `leadSources` array — used by Entry (source select), Config (source management)
- Socket event handlers for `sale:changed` that refresh multiple lists
- Any other data loaded once and referenced by multiple sub-tabs

These shared variables will be props received from the orchestrator.

For EACH sub-tab, create a component file:

1. **ManagerEntry.tsx** — Sales Entry form (the core business operation)
   - `"use client"` directive
   - Extract the sales entry form and all its state (form fields, validation, submission)
   - Accept props: `{ socket: SocketClient | null; API: string; agents: Agent[]; products: Product[]; leadSources: LeadSource[]; onSaleCreated?: () => void }` (adjust types based on original)
   - Move all style constants for the entry form into this file
   - Preserve EVERY form field, validation rule, submit handler, commission preview, etc.

2. **ManagerTracker.tsx** — Agent Tracker with KPI data
   - Extract agent tracking/performance display
   - Accept props: `{ API: string; agents: Agent[]; ... }`

3. **ManagerSales.tsx** — Sales listing/management
   - Extract sales table, edit/delete functionality
   - Accept props: `{ socket: SocketClient | null; API: string; agents: Agent[]; products: Product[]; ... }`

4. **ManagerAudits.tsx** — Call Audits
   - Extract call audit display and controls
   - Accept props: `{ socket: SocketClient | null; API: string; ... }`
   - Socket events: listen for `new_audit`, `audit_status` events

5. **ManagerConfig.tsx** — Configuration (agents, products, lead sources management)
   - Extract all CRUD operations for agents, products, lead sources
   - Accept props: `{ API: string; agents: Agent[]; products: Product[]; leadSources: LeadSource[]; refreshAgents: () => void; refreshProducts: () => void; refreshLeadSources: () => void; ... }`

CRITICAL: MECHANICAL extraction. The manager dashboard has the most deeply intertwined state of any dashboard. Pay special attention to:
- Callbacks that refresh data after mutations (e.g., after creating a sale, refresh the sales list AND the tracker)
- Socket event handlers that trigger cross-tab updates
- Form state that references shared data (dropdowns populated from agents/products/leadSources)
- Commission preview calculations that depend on product data

Do NOT simplify or refactor. Preserve every fetch URL, handler, and UI element exactly.
  </action>
  <verify>
    <automated>ls apps/ops-dashboard/app/\(dashboard\)/manager/*.tsx | wc -l && grep -c "use client" apps/ops-dashboard/app/\(dashboard\)/manager/ManagerEntry.tsx apps/ops-dashboard/app/\(dashboard\)/manager/ManagerTracker.tsx apps/ops-dashboard/app/\(dashboard\)/manager/ManagerSales.tsx apps/ops-dashboard/app/\(dashboard\)/manager/ManagerAudits.tsx apps/ops-dashboard/app/\(dashboard\)/manager/ManagerConfig.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 5 component files exist in the manager directory
    - Each contains `"use client"` directive
    - Each imports `authFetch` from `@ops/auth/client`
    - ManagerEntry.tsx accepts agents, products, leadSources props for form dropdowns
    - ManagerEntry.tsx preserves the sales entry form with all fields and validation
    - ManagerSales.tsx has sale edit/delete functionality
    - ManagerAudits.tsx subscribes to `new_audit` and `audit_status` socket events
    - ManagerConfig.tsx has CRUD for agents, products, lead sources
    - All fetch URLs from the original page.tsx are distributed across the 5 files
    - Shared state dependencies are accepted as props
  </acceptance_criteria>
  <done>All 5 Manager sub-tab components extracted preserving deeply shared state patterns</done>
</task>

<task type="auto">
  <name>Task 2: Create Manager page orchestrator with shared state and cross-tab refresh</name>
  <files>
    apps/ops-dashboard/app/(dashboard)/manager/page.tsx
  </files>
  <read_first>
    apps/manager-dashboard/app/page.tsx,
    apps/ops-dashboard/app/(dashboard)/manager/ManagerEntry.tsx,
    apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx,
    apps/ops-dashboard/app/(dashboard)/manager/ManagerSales.tsx,
    apps/ops-dashboard/app/(dashboard)/manager/ManagerAudits.tsx,
    apps/ops-dashboard/app/(dashboard)/manager/ManagerConfig.tsx,
    apps/ops-dashboard/lib/SocketProvider.tsx,
    packages/ui/src/index.tsx
  </read_first>
  <action>
Create `apps/ops-dashboard/app/(dashboard)/manager/page.tsx` as the orchestrator:

1. `"use client"` directive
2. Import useState, useEffect, useCallback from react
3. Import `PageShell` from `@ops/ui`
4. Import `useSocketContext` from `@/lib/SocketProvider`
5. Import all 5 sub-tab components
6. Import icons from `lucide-react` matching original manager-dashboard icons

7. `const API = process.env.NEXT_PUBLIC_OPS_API_URL ?? "";`

8. Shared state management — replicate EXACTLY from the original page.tsx:
   - `agents` state + `loadAgents` useCallback fetching from `${API}/api/agents`
   - `products` state + `loadProducts` useCallback fetching from `${API}/api/products`
   - `leadSources` state + `loadLeadSources` useCallback fetching from `${API}/api/lead-sources`
   - Initial load useEffect calling all three loaders
   - Any other shared state identified when reading the original

9. Socket event handling at orchestrator level:
   - Subscribe to `sale:changed` on the socket from useSocketContext
   - On `sale:changed`, trigger appropriate refreshes (e.g., reload sales list, reload tracker data)
   - Pass socket to sub-tabs that need additional event subscriptions

10. Cross-tab refresh callbacks:
    - `onSaleCreated` / `onSaleUpdated` — triggers data refresh that spans multiple sub-tabs
    - `refreshAgents`, `refreshProducts`, `refreshLeadSources` — passed to Config for post-CRUD updates

11. Build navItems matching original manager-dashboard:
    ```typescript
    const navItems: NavItem[] = [
      { icon: <PlusCircle size={18} />, label: "Entry", key: "entry" },
      { icon: <Activity size={18} />, label: "Tracker", key: "tracker" },
      { icon: <BarChart3 size={18} />, label: "Sales", key: "sales" },
      { icon: <Phone size={18} />, label: "Audits", key: "audits" },
      { icon: <Settings size={18} />, label: "Config", key: "config" },
    ];
    ```
    (Confirm exact labels and icons from original)

12. Render with all shared state passed as props:
    ```typescript
    <PageShell title="Manager" navItems={navItems} activeNav={activeTab} onNavChange={setActiveTab}>
      {activeTab === "entry" && <ManagerEntry socket={socket} API={API} agents={agents} products={products} leadSources={leadSources} onSaleCreated={handleSaleCreated} />}
      {activeTab === "tracker" && <ManagerTracker API={API} agents={agents} />}
      {activeTab === "sales" && <ManagerSales socket={socket} API={API} agents={agents} products={products} />}
      {activeTab === "audits" && <ManagerAudits socket={socket} API={API} />}
      {activeTab === "config" && <ManagerConfig API={API} agents={agents} products={products} leadSources={leadSources} refreshAgents={loadAgents} refreshProducts={loadProducts} refreshLeadSources={loadLeadSources} />}
    </PageShell>
    ```
  </action>
  <verify>
    <automated>grep -n "PageShell" apps/ops-dashboard/app/\(dashboard\)/manager/page.tsx && grep -c "ManagerEntry\|ManagerTracker\|ManagerSales\|ManagerAudits\|ManagerConfig" apps/ops-dashboard/app/\(dashboard\)/manager/page.tsx && grep -n "loadAgents\|loadProducts\|loadLeadSources" apps/ops-dashboard/app/\(dashboard\)/manager/page.tsx | head -6</automated>
  </verify>
  <acceptance_criteria>
    - manager/page.tsx contains `"use client"` directive
    - manager/page.tsx imports and uses `PageShell` from `@ops/ui`
    - manager/page.tsx imports and renders all 5 sub-tab components
    - manager/page.tsx uses `useSocketContext` from `@/lib/SocketProvider`
    - manager/page.tsx fetches shared data: agents, products, leadSources via useCallback + useEffect
    - manager/page.tsx passes shared data as props to sub-tab components
    - manager/page.tsx passes refresh callbacks to Config sub-tab
    - manager/page.tsx subscribes to `sale:changed` socket event at orchestrator level
    - manager/page.tsx defines `const API = process.env.NEXT_PUBLIC_OPS_API_URL ?? ""`
  </acceptance_criteria>
  <done>Manager page orchestrator wires 5 sub-tabs with shared state, cross-tab refresh, and socket events</done>
</task>

</tasks>

<verification>
- Navigate to `/manager` — should render Manager dashboard with 5 sub-tabs
- Sales Entry form populates agent, product, lead source dropdowns
- Creating a sale via Entry updates the Sales list tab
- Agent Tracker shows performance data
- Call Audits display with real-time updates (new_audit, audit_status events)
- Config tab allows CRUD on agents, products, lead sources
- Socket sale:changed events trigger data refreshes
- Compare side-by-side with standalone manager-dashboard
</verification>

<success_criteria>
- Manager dashboard migrated with all features identical to standalone app
- 5 sub-tabs accessible via PageShell sidebar
- Shared state (agents, products, lead sources) managed at orchestrator level
- Cross-tab data refresh works (create sale in Entry, see it in Sales)
- Socket.IO events received through SocketProvider context
</success_criteria>

<output>
After completion, create `.planning/phases/19-dashboard-consolidation-uniform-date-ranges/19-06-SUMMARY.md`
</output>
