---
phase: 04-multi-product-sales-form
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - apps/manager-dashboard/app/page.tsx
autonomous: false
requirements:
  - SALE-02

must_haves:
  truths:
    - "Form fields appear in natural flow order: Agent|MemberName, MemberID|MemberState, SaleDate|EffectiveDate, Product|LeadSource, Carrier|Status, Premium|EnrollmentFee, Notes, PaymentType, Submit"
    - "Stagger animation classes are sequential (stagger-1 through stagger-11)"
    - "Full sales submission works end-to-end with the reordered form"
  artifacts:
    - path: "apps/manager-dashboard/app/page.tsx"
      provides: "Reordered form fields with sequential stagger animations"
      contains: "stagger-1"
  key_links:
    - from: "apps/manager-dashboard/app/page.tsx"
      to: "/api/sales"
      via: "submitSale POST with all fields from reordered form"
      pattern: "submitSale"
---

<objective>
Reorder form fields to match the natural data entry flow and verify the complete sales form works end-to-end.

Purpose: The current field order is scattered (Status before Carrier, Effective Date after Carrier, Product near bottom). Reordering to the user-specified flow makes data entry faster and more intuitive.
Output: Sales form with logically ordered fields and working end-to-end submission.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-multi-product-sales-form/04-CONTEXT.md
@.planning/phases/04-multi-product-sales-form/04-01-SUMMARY.md
@apps/manager-dashboard/app/page.tsx

<interfaces>
<!-- Current field order (lines 889-960) vs target order -->

Current JSX order in the left column grid:
1. Agent (stagger-1)
2. Member Name (stagger-1)
3. Member ID (stagger-2)
4. Sale Date (stagger-3)
5. Status (stagger-4)
6. Carrier (stagger-5)
7. Premium (stagger-6)
8. Effective Date (stagger-7)
9. Product (stagger-8)
10. Lead Source (stagger-9)
11. Enrollment Fee (stagger-11)
12. Notes (no stagger)
13. Member State (no stagger)

Target JSX order (from user decisions in CONTEXT.md):
Row 1: Agent (stagger-1) | Member Name (stagger-1)
Row 2: Member ID (stagger-2) | Member State (stagger-2)
Row 3: Sale Date (stagger-3) | Effective Date (stagger-3)
Row 4: Product (stagger-4) | Lead Source (stagger-4)
Row 5: Carrier (stagger-5) | Status (stagger-5)
Row 6: Premium (stagger-6) | Enrollment Fee (stagger-6)
Row 7: Notes (stagger-7, gridColumn: "1/-1" full width)
Row 8: Payment Type (full width, as-is)
Row 9: Submit (full width, as-is)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder form fields and update stagger animations</name>
  <files>apps/manager-dashboard/app/page.tsx</files>
  <action>
Rearrange the `<div>` blocks within the left column grid (between lines ~889-960) to match this order. The grid uses `gridTemplateColumns: "1fr 1fr"` so fields pair left-right in reading order.

**Target field order (cut and paste existing div blocks, do NOT rewrite them):**

1. Agent — keep at position 1, stagger-1
2. Member Name — keep at position 2, stagger-1
3. Member ID — keep at position 3, stagger-2
4. Member State — MOVE from position 13 to position 4, ADD class "animate-fade-in-up stagger-2"
5. Sale Date — MOVE from position 4 to position 5, CHANGE to stagger-3
6. Effective Date — MOVE from position 8 to position 6, CHANGE to stagger-3
7. Product — MOVE from position 9 to position 7, CHANGE to stagger-4
8. Lead Source — MOVE from position 10 to position 8, CHANGE to stagger-4
9. Carrier — MOVE from position 6 to position 9, CHANGE to stagger-5
10. Status — MOVE from position 5 to position 10, CHANGE to stagger-5
11. Premium — keep, CHANGE to stagger-6
12. Enrollment Fee — MOVE from position 11 to position 12, CHANGE to stagger-6
13. Notes — make full width with `style={{ gridColumn: "1/-1" }}` on the wrapper div, ADD class "animate-fade-in-up stagger-7"

After Notes, keep Payment Type (already gridColumn: "1/-1") and Submit button unchanged.

**Key rules:**
- Only cut/paste the existing `<div>` blocks — do NOT change any input attributes, onChange handlers, or style constants
- Update ONLY the `className` stagger numbers and the div order
- Notes div gets `gridColumn: "1/-1"` since it spans full width per user decision (Row 7 is Notes alone)
- Member State div: ensure it has "animate-fade-in-up stagger-2" class (currently has no animation class)
  </action>
  <verify>
    <automated>cd C:/projects/ai-calling-backend && node -e "const fs=require('fs'); const c=fs.readFileSync('apps/manager-dashboard/app/page.tsx','utf8'); const grid=c.indexOf('LEFT COLUMN'); const end=c.indexOf('END LEFT COLUMN'); const chunk=c.slice(grid,end); const labels=['Agent','Member Name','Member ID','Member State','Sale Date','Effective Date','Product','Lead Source','Carrier','Premium','Enrollment Fee','Notes']; const positions=labels.map(l=>{const i=chunk.indexOf(l+'<');return i===-1?chunk.indexOf('>'+l):i;}); let ordered=true; for(let i=1;i<positions.length;i++){if(positions[i]<positions[i-1]&&positions[i]!==-1){ordered=false;break;}} console.log(ordered?'FIELDS IN CORRECT ORDER':'FIELD ORDER MISMATCH'); console.log(labels.map((l,i)=>l+':'+positions[i]).join(', '));"</automated>
  </verify>
  <done>Form fields appear in the order: Agent, Member Name, Member ID, Member State, Sale Date, Effective Date, Product, Lead Source, Carrier, Status, Premium, Enrollment Fee, Notes — with sequential stagger animations</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete multi-product sales form with reordered fields, blank defaults, type-filtered product dropdowns, sorted addon picker, optional carrier, and CC/ACH payment type</what-built>
  <how-to-verify>
1. Start the manager dashboard: `npm run manager:dev`
2. Start the API: `npm run ops:dev`
3. Visit http://localhost:3019
4. Login and navigate to the sales entry form
5. Verify field order top to bottom: Agent | Member Name, Member ID | Member State, Sale Date | Effective Date, Product | Lead Source, Carrier | Status, Premium | Enrollment Fee, Notes (full width), Payment Type (full width), Submit
6. Verify Product dropdown starts blank with "Select product..." — only CORE products listed
7. Verify Lead Source dropdown starts blank with "Select lead source..."
8. Verify Carrier field shows "Optional" placeholder and is NOT required
9. Verify addon picker in right column shows ADDON products first (alphabetical), then AD&D products (alphabetical)
10. Select an agent, member name, sale date, effective date, product, lead source, premium, enrollment fee, payment type (CC or ACH)
11. Leave carrier blank
12. Submit the sale — should succeed without errors
13. Verify the sale appears in the sales list
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Field order matches the user-specified layout from CONTEXT.md
2. Stagger animations are sequential (1-7)
3. Full sale submission works with blank carrier
4. Product dropdown only shows CORE products
5. Addon picker sorts correctly
</verification>

<success_criteria>
- Form field order matches CONTEXT.md specification exactly
- Stagger animations play sequentially without gaps
- End-to-end sale submission works with all the new defaults
- No regression in receipt parser auto-fill functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-product-sales-form/04-02-SUMMARY.md`
</output>
