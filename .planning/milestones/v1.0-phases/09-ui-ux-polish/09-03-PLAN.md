---
phase: 09-ui-ux-polish
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - apps/payroll-dashboard/app/page.tsx
autonomous: true
requirements: [UIUX-01, UIUX-02, UIUX-03]
must_haves:
  truths:
    - "Payroll dashboard uses shared Button component instead of 6 local button style constants"
    - "Payroll dashboard tables use shared baseThStyle/baseTdStyle tokens"
    - "All alert() calls in payroll dashboard replaced with toast notifications"
    - "Table rows have consistent hover states via row-hover class"
    - "Payroll dashboard uses EmptyState component for empty agent lists or empty periods"
  artifacts:
    - path: "apps/payroll-dashboard/app/page.tsx"
      provides: "Payroll dashboard with shared components, toast notifications, consistent polish"
      contains: "useToast"
  key_links:
    - from: "apps/payroll-dashboard/app/page.tsx"
      to: "packages/ui/src/components/Button.tsx"
      via: "import { Button } from '@ops/ui'"
      pattern: "import.*Button.*from.*@ops/ui"
    - from: "apps/payroll-dashboard/app/page.tsx"
      to: "packages/ui/src/components/Toast.tsx"
      via: "import { useToast } from '@ops/ui'"
      pattern: "useToast"
---

<objective>
Migrate the payroll dashboard (2808 lines) to shared @ops/ui components. Replace 6 local button style constants with Button component, replace all alert() calls with toast notifications, standardize table styles, and add EmptyState components.

Purpose: The payroll dashboard has the most local button variants (BTN_PRIMARY, BTN_SUCCESS, BTN_DANGER, BTN_GHOST, BTN_WARNING, BTN_ICON) that duplicate the shared Button component. It also has 10 alert() calls for error handling. This plan brings it to design system consistency.

Output: Payroll dashboard using shared @ops/ui components with toast notifications and consistent visual polish.
</objective>

<execution_context>
@./.claude/skills/ui-ux-pro-max/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ui-ux-polish/09-CONTEXT.md
@.planning/phases/09-ui-ux-polish/09-RESEARCH.md
@.planning/phases/09-ui-ux-polish/09-01-SUMMARY.md

@packages/ui/src/tokens.ts
@packages/ui/src/components/Button.tsx
@packages/ui/src/components/Toast.tsx
@packages/ui/src/components/Card.tsx
@packages/ui/src/components/Skeleton.tsx
@packages/ui/src/components/EmptyState.tsx

<interfaces>
<!-- Key types the executor needs from @ops/ui -->

From packages/ui/src/components/Button.tsx:
```typescript
export function Button({
  variant = "primary",  // "primary" | "secondary" | "ghost" | "danger" | "success"
  size = "md",          // "sm" | "md" | "lg"
  loading = false,
  icon,
  iconPosition = "left",
  fullWidth = false,
  children, disabled, style, className, ...rest
}: ButtonProps);
// Automatically adds btn-hover class, handles loading spinner, opacity on disabled
```

From packages/ui/src/components/Toast.tsx:
```typescript
export function ToastProvider({ children }: { children: React.ReactNode }): JSX.Element;
export function useToast(): { toast: (msg: string, type?: "success" | "error" | "info" | "warning") => void };
```

From packages/ui/src/tokens.ts:
```typescript
export const baseThStyle: React.CSSProperties;  // padding: 10px 14px, fontSize: 11, fontWeight: 700, uppercase, caps tracking
export const baseTdStyle: React.CSSProperties;   // padding: 12px 14px, fontSize: 13, textSecondary
```

Button variant mapping for payroll constants:
- BTN_PRIMARY (teal gradient) -> `<Button variant="primary">`
- BTN_SUCCESS (green) -> `<Button variant="success">`
- BTN_DANGER (red) -> `<Button variant="danger">`
- BTN_GHOST (transparent) -> `<Button variant="ghost">`
- BTN_WARNING (amber/yellow bg) -> `<Button variant="secondary" style={{ background: colors.warningBg, color: colors.warning }}>` (no warning variant exists, use secondary with override)
- BTN_ICON (small icon-only) -> `<Button variant="ghost" size="sm">`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace payroll dashboard button constants and alert() calls with shared components</name>
  <files>apps/payroll-dashboard/app/page.tsx</files>
  <read_first>
    - apps/payroll-dashboard/app/page.tsx (full file -- 2808 lines, focus on: imports, style constants lines 79-166, all alert() calls, button JSX usage patterns)
    - packages/ui/src/components/Button.tsx (variant API and size options)
    - packages/ui/src/components/Toast.tsx (useToast API)
  </read_first>
  <action>
1. **Add ToastProvider wrapper and useToast hook:**
   - Wrap the top-level return with `<ToastProvider>...</ToastProvider>`
   - Add `const { toast } = useToast();` inside the component
   - Add `ToastProvider, useToast, Button` to existing @ops/ui import line

2. **Delete all 6 local button constants:**
   - Delete `const BTN_PRIMARY: React.CSSProperties = { ... }`
   - Delete `const BTN_SUCCESS: React.CSSProperties = { ... }`
   - Delete `const BTN_DANGER: React.CSSProperties = { ... }`
   - Delete `const BTN_GHOST: React.CSSProperties = { ... }`
   - Delete `const BTN_WARNING: React.CSSProperties = { ... }`
   - Delete `const BTN_ICON: React.CSSProperties = { ... }`

3. **Replace all button JSX using local constants:**
   - `<button style={BTN_PRIMARY} ...>` -> `<Button variant="primary" ...>`
   - `<button style={BTN_SUCCESS} ...>` -> `<Button variant="success" ...>`
   - `<button style={BTN_DANGER} ...>` -> `<Button variant="danger" ...>`
   - `<button style={BTN_GHOST} ...>` -> `<Button variant="ghost" ...>`
   - `<button style={BTN_WARNING} ...>` -> `<Button variant="secondary" style={{ background: colors.warningBg, color: colors.warning, border: \`1px solid ${colors.warning}\` }} ...>` (closest match since no warning variant exists)
   - `<button style={BTN_ICON} ...>` -> `<Button variant="ghost" size="sm" ...>`
   - For buttons with `style={{ ...BTN_PRIMARY, width: ... }}` patterns, pass the override to Button's style prop: `<Button variant="primary" style={{ width: ... }}>`
   - Preserve all onClick handlers, disabled attributes, title attributes, and children
   - Preserve existing `className` values (like "btn-hover") -- Button already adds btn-hover internally, so remove redundant className="btn-hover" from the JSX

4. **Replace all 10 alert() calls with toast():**
   - Line ~1381: `alert(\`Error: ${err.error ?? \`Request failed (${res.status})\`}\`)` -> `toast(\`Error: ${err.error ?? \`Request failed (${res.status})\`}\`, "error")`
   - Line ~1384: `alert(\`Error: Unable to reach API...\`)` -> `toast(\`Error: Unable to reach API -- ${e.message ?? "network error"}\`, "error")`
   - Line ~1398: same pattern -> `toast(..., "error")`
   - Line ~1401: same pattern -> `toast(..., "error")`
   - Line ~1422: same pattern -> `toast(..., "error")`
   - Line ~1425: same pattern -> `toast(..., "error")`
   - Line ~1440: same pattern -> `toast(..., "error")`
   - Line ~1443: same pattern -> `toast(..., "error")`
   - Line ~1487: same pattern -> `toast(..., "error")`
   - Line ~1490: same pattern -> `toast(..., "error")`
   - For any success-path alerts (approve/reject success), use `toast("...", "success")`

5. **Remove unused imports** -- if `baseButtonStyle` was imported only for local button constants, remove it.
  </action>
  <verify>
    <automated>cd /c/Users/javer/Documents/Repositories/ai-calling-backend && node -e "const s = require('fs').readFileSync('apps/payroll-dashboard/app/page.tsx','utf8'); console.assert(!s.includes('const BTN_PRIMARY:'), 'removed BTN_PRIMARY'); console.assert(!s.includes('const BTN_SUCCESS:'), 'removed BTN_SUCCESS'); console.assert(!s.includes('const BTN_DANGER:'), 'removed BTN_DANGER'); console.assert(!s.includes('const BTN_GHOST:'), 'removed BTN_GHOST'); console.assert(!s.includes('const BTN_WARNING:'), 'removed BTN_WARNING'); console.assert(!s.includes('const BTN_ICON:'), 'removed BTN_ICON'); console.assert(s.includes('<Button'), 'uses Button component'); console.assert(s.includes('useToast'), 'uses useToast'); const alertCount = (s.match(/\balert\s*\(/g) || []).length; console.assert(alertCount === 0, 'all alert() removed, found: ' + alertCount); console.log('payroll buttons+toast OK')"</automated>
  </verify>
  <acceptance_criteria>
    - apps/payroll-dashboard/app/page.tsx does NOT contain `const BTN_PRIMARY: React.CSSProperties`
    - apps/payroll-dashboard/app/page.tsx does NOT contain `const BTN_SUCCESS: React.CSSProperties`
    - apps/payroll-dashboard/app/page.tsx does NOT contain `const BTN_DANGER: React.CSSProperties`
    - apps/payroll-dashboard/app/page.tsx does NOT contain `const BTN_GHOST: React.CSSProperties`
    - apps/payroll-dashboard/app/page.tsx does NOT contain `const BTN_WARNING: React.CSSProperties`
    - apps/payroll-dashboard/app/page.tsx does NOT contain `const BTN_ICON: React.CSSProperties`
    - apps/payroll-dashboard/app/page.tsx contains `<Button` component (multiple instances)
    - apps/payroll-dashboard/app/page.tsx contains `useToast` and `ToastProvider`
    - apps/payroll-dashboard/app/page.tsx contains zero occurrences of `alert(`
    - apps/payroll-dashboard/app/page.tsx contains `toast(` calls (at least 8 instances)
  </acceptance_criteria>
  <done>All 6 local button constants removed and replaced with shared Button component. All 10 alert() calls replaced with toast notifications.</done>
</task>

<task type="auto">
  <name>Task 2: Replace payroll dashboard table/card/input constants with shared tokens and add EmptyState</name>
  <files>apps/payroll-dashboard/app/page.tsx</files>
  <read_first>
    - apps/payroll-dashboard/app/page.tsx (after Task 1 -- focus on remaining local constants: CARD, CARD_SM, INP, TH, TD, TH_R, TH_C, TD_R, TD_C; table JSX; loading sections; empty-data sections)
    - packages/ui/src/tokens.ts (baseThStyle, baseTdStyle, baseCardStyle, baseInputStyle)
    - packages/ui/src/components/Card.tsx (Card component API)
    - packages/ui/src/components/EmptyState.tsx (EmptyState API)
    - packages/ui/src/components/Input.tsx (Input component API)
    - packages/ui/src/components/Select.tsx (Select component API -- created in Plan 01)
  </read_first>
  <action>
1. **Update imports** -- add to existing @ops/ui import:
   ```typescript
   import { ..., Card, Input, Select, EmptyState, baseThStyle, baseTdStyle } from "@ops/ui";
   ```

2. **Replace local TH/TD constants with shared tokens:**
   - Delete `const TH: React.CSSProperties = { ... }` (line ~146)
   - Delete `const TH_R` and `const TH_C` derived constants (line ~161-162)
   - Delete `const TD: React.CSSProperties = { ... }` (line ~164)
   - Delete `const TD_R` and `const TD_C` derived constants (line ~165-166)
   - Replace all `style={TH}` with `style={baseThStyle}`
   - Replace all `style={TH_R}` with `style={{ ...baseThStyle, textAlign: "right" }}`
   - Replace all `style={TH_C}` with `style={{ ...baseThStyle, textAlign: "center" }}`
   - Replace all `style={TD}` with `style={baseTdStyle}`
   - Replace all `style={TD_R}` with `style={{ ...baseTdStyle, textAlign: "right" }}`
   - Replace all `style={TD_C}` with `style={{ ...baseTdStyle, textAlign: "center" }}`

3. **Replace local CARD and CARD_SM constants:**
   - Delete `const CARD: React.CSSProperties = { ... }` (line ~79)
   - Delete `const CARD_SM: React.CSSProperties = { ... }` (line ~84)
   - Replace `<div style={CARD}>` with `<Card>`
   - Replace `<div style={CARD_SM}>` with `<Card padding="sm">` (CARD_SM likely has smaller padding)
   - If CARD_SM has additional differences beyond padding, pass them via style prop

4. **Replace local INP constant:**
   - Delete `const INP: React.CSSProperties = { ... }` (line ~91)
   - For `<input style={INP}>` used in editable payroll fields, replace with `<Input>` component
   - For `<select style={INP}>` used in period/status selectors, replace with `<Select>` component
   - These are inline edit fields in payroll cards, so they may not need labels -- use `<Input style={{ ... }} />` without label prop for inline fields
   - For standalone dropdowns (period filter), add a label

5. **Add EmptyState for no-data scenarios:**
   - Find sections where the payroll dashboard shows empty text like "No agents" or "No entries" or renders an empty list
   - Replace with `<EmptyState title="No agents in this period" description="Agents will appear here when sales are entered." />`
   - Import relevant lucide-react icons for the empty state icon prop (e.g., `Users` for no agents, `FileText` for no entries)

6. **Add row-hover class to all table body <tr> elements:**
   - Find all `<tr>` inside `<tbody>` sections
   - Add `className="row-hover"` to each data row
   - Preserve existing className values by combining: `className={\`row-hover ${existing}\`}` or `className={["row-hover", existing].filter(Boolean).join(" ")}`

7. **Remove unused style imports** -- if `baseButtonStyle` was imported only for deleted constants, remove it.
  </action>
  <verify>
    <automated>cd /c/Users/javer/Documents/Repositories/ai-calling-backend && node -e "const s = require('fs').readFileSync('apps/payroll-dashboard/app/page.tsx','utf8'); console.assert(!s.includes('const TH:') && !s.includes('const TH :'), 'removed TH'); console.assert(!s.includes('const TD:') && !s.includes('const TD :'), 'removed TD'); console.assert(!s.includes('const CARD:') && !s.includes('const CARD :'), 'removed CARD'); console.assert(!s.includes('const CARD_SM:'), 'removed CARD_SM'); console.assert(!s.includes('const INP:') && !s.includes('const INP :'), 'removed INP'); console.assert(s.includes('baseThStyle'), 'uses shared TH'); console.assert(s.includes('baseTdStyle'), 'uses shared TD'); console.assert(s.includes('<Card'), 'uses Card component'); console.assert(s.includes('row-hover'), 'row-hover applied'); console.assert(s.includes('EmptyState'), 'uses EmptyState'); console.log('payroll migration OK')"</automated>
  </verify>
  <acceptance_criteria>
    - apps/payroll-dashboard/app/page.tsx does NOT contain `const TH: React.CSSProperties`
    - apps/payroll-dashboard/app/page.tsx does NOT contain `const TD: React.CSSProperties`
    - apps/payroll-dashboard/app/page.tsx does NOT contain `const CARD: React.CSSProperties`
    - apps/payroll-dashboard/app/page.tsx does NOT contain `const CARD_SM: React.CSSProperties`
    - apps/payroll-dashboard/app/page.tsx does NOT contain `const INP: React.CSSProperties`
    - apps/payroll-dashboard/app/page.tsx contains `baseThStyle` and `baseTdStyle` from @ops/ui
    - apps/payroll-dashboard/app/page.tsx contains `<Card` component usage
    - apps/payroll-dashboard/app/page.tsx contains `row-hover` class on table rows
    - apps/payroll-dashboard/app/page.tsx contains `<EmptyState` component usage (at least 1 instance)
  </acceptance_criteria>
  <done>Payroll dashboard local style constants fully replaced with shared @ops/ui components and tokens. EmptyState components added for no-data scenarios. Table rows have consistent hover states.</done>
</task>

</tasks>

<verification>
- Payroll dashboard has zero local button style constants (BTN_PRIMARY through BTN_ICON)
- Payroll dashboard has zero local table style constants (TH, TD, TH_R, TD_R, TH_C, TD_C)
- Payroll dashboard has zero local card/input constants (CARD, CARD_SM, INP)
- All alert() calls replaced with toast()
- EmptyState used for no-data scenarios
- Table rows have row-hover class
</verification>

<success_criteria>
- Zero alert() calls remain in payroll dashboard
- Zero local button/table/card/input style constants remain
- Button component used for all interactive buttons (primary, success, danger, ghost variants)
- baseThStyle/baseTdStyle tokens used for all table cells
- Card component used for card containers
- EmptyState component used for empty data sections
- row-hover class on all data table rows
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-ux-polish/09-03-SUMMARY.md`
</output>
