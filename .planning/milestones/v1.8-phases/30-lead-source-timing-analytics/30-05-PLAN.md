---
phase: 30-lead-source-timing-analytics
plan: 05
type: execute
wave: 4
depends_on: [30-02, 30-04]
files_modified:
  - apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx
  - apps/ops-dashboard/app/(dashboard)/owner/OwnerOverview.tsx
autonomous: false
requirements: [DASH-03, DASH-04]

must_haves:
  truths:
    - "Timing analytics section renders below agent performance on Performance Tracker tab"
    - "Same timing analytics section renders on Owner dashboard"
    - "Components are accessible to MANAGER, OWNER_VIEW, and SUPER_ADMIN roles (enforced by API)"
  artifacts:
    - path: "apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx"
      provides: "LeadTimingSection integrated below existing agent performance content"
      contains: "LeadTimingSection"
    - path: "apps/ops-dashboard/app/(dashboard)/owner/OwnerOverview.tsx"
      provides: "LeadTimingSection integrated into owner dashboard"
      contains: "LeadTimingSection"
  key_links:
    - from: "ManagerTracker.tsx"
      to: "LeadTimingSection.tsx"
      via: "import and render at bottom of tracker view"
      pattern: "import LeadTimingSection"
    - from: "OwnerOverview.tsx"
      to: "LeadTimingSection.tsx"
      via: "import and render below performance section"
      pattern: "import LeadTimingSection"
---

<objective>
Wire the LeadTimingSection component into the Manager Performance Tracker and Owner dashboard.

Purpose: This is the final integration step making the timing analytics visible to managers and owners.
Output: LeadTimingSection rendered on both dashboards, end-to-end feature complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-lead-source-timing-analytics/30-CONTEXT.md
@.planning/phases/30-lead-source-timing-analytics/30-04-SUMMARY.md

<interfaces>
From apps/ops-dashboard/app/(dashboard)/manager/LeadTimingSection.tsx (created in Plan 04):
```typescript
// Default export
export default function LeadTimingSection({ API }: { API: string }): JSX.Element;
```

From apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx:
```typescript
export interface ManagerTrackerProps {
  API: string;
  tracker: TrackerEntry[];
  setTracker: React.Dispatch<React.SetStateAction<TrackerEntry[]>>;
  highlightedAgentNames: Set<string>;
}
```

From apps/ops-dashboard/app/(dashboard)/owner/OwnerOverview.tsx:
```typescript
export default function OwnerOverview({ socket, API }: { socket: SocketClient | null; API: string }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate LeadTimingSection into Manager and Owner dashboards</name>
  <files>apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx, apps/ops-dashboard/app/(dashboard)/owner/OwnerOverview.tsx</files>
  <read_first>
    - apps/ops-dashboard/app/(dashboard)/manager/ManagerTracker.tsx (full file -- find end of component return JSX to know where to insert)
    - apps/ops-dashboard/app/(dashboard)/owner/OwnerOverview.tsx (full file -- find the component return JSX)
    - apps/ops-dashboard/app/(dashboard)/manager/LeadTimingSection.tsx (verify export and props)
  </read_first>
  <action>
**ManagerTracker.tsx:**

Add import at top (after existing imports):
```typescript
import LeadTimingSection from "./LeadTimingSection";
```

At the bottom of the component's return JSX (after all existing content but before the closing `</div>` or fragment), add:
```tsx
<LeadTimingSection API={API} />
```

The `API` prop is already available from `ManagerTrackerProps`. This places the timing analytics section below the agent performance table, which is the correct position per DASH-03.

**OwnerOverview.tsx:**

Add import at top (after existing imports). Note the path is relative from owner to manager directory:
```typescript
import LeadTimingSection from "../manager/LeadTimingSection";
```

At the bottom of OwnerOverview's return JSX (after the existing "Agent Performance" section), add:
```tsx
<LeadTimingSection API={API} />
```

The `API` prop is already available from OwnerOverview's props. Role enforcement happens at the API level (requireRole on lead-timing endpoints), so no additional frontend role checks are needed. SUPER_ADMIN bypasses automatically.
  </action>
  <verify>
    <automated>cd C:/Users/javer/Documents/Repositories/ai-calling-backend && npx tsc --noEmit --project apps/ops-dashboard/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ManagerTracker.tsx contains `import LeadTimingSection from "./LeadTimingSection"`
    - ManagerTracker.tsx contains `<LeadTimingSection API={API} />`
    - OwnerOverview.tsx contains `import LeadTimingSection from "../manager/LeadTimingSection"`
    - OwnerOverview.tsx contains `<LeadTimingSection API={API} />`
  </acceptance_criteria>
  <done>LeadTimingSection renders on both Manager Performance Tracker and Owner dashboard below existing content</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete timing analytics feature</name>
  <what-built>
    Complete lead source timing analytics feature:
    - Convoso timezone fixes (DST + business hours)
    - Commission fallback logic fix
    - Database indexes for aggregation
    - Three API endpoints (heatmap, sparklines, recommendation)
    - Tab renamed to "Performance Tracker"
    - Today column in agent performance table
    - Collapsible analytics section with heatmap, best source card, and sparklines
    - Independent date range filter
    - Integrated into Manager and Owner dashboards
  </what-built>
  <how-to-verify>
    1. Start dev servers: `npm run ops:dev` and `npm run dashboard:dev`
    2. Log in as a Manager role user
    3. Verify tab says "Performance Tracker" (not "Agent Tracker")
    4. On Performance Tracker tab, verify "Today" column appears in agent table
    5. Scroll down -- verify "Lead Source Timing Analytics" section appears (collapsed)
    6. Click the section header to expand
    7. Verify date range filter shows "Last 30 Days" default
    8. Verify "Best Source Right Now" card shows data or "Not enough data" message
    9. Verify heatmap grid renders with colored cells (or no-data message)
    10. Change grouping dropdown to "Week of Month" -- grid should re-render
    11. Change to "Month of Year" -- grid should re-render
    12. Hover a heatmap cell -- tooltip should show close rate, calls, sales
    13. Verify sparklines table shows below heatmap with 3 daypart columns
    14. Change date range to "Last 60 Days" -- all three components should refresh
    15. Log in as Owner role -- verify same timing analytics section appears on Owner dashboard
    16. Log in as SUPER_ADMIN -- verify access works on both dashboards
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles for ops-dashboard
- LeadTimingSection appears on both Manager and Owner dashboards
- All 25 requirements are addressed across Plans 01-05
- Feature is end-to-end functional
</verification>

<success_criteria>
- Timing analytics section visible on Manager Performance Tracker tab below agent performance
- Same section visible on Owner dashboard
- Section is collapsible (default collapsed) with independent date filter
- Heatmap, Best Source card, and sparklines all render correctly
- Tab renamed, Today column present
- Commission fix live
- Timezone fixes live
</success_criteria>

<output>
After completion, create `.planning/phases/30-lead-source-timing-analytics/30-05-SUMMARY.md`
</output>
