---
phase: 27-error-handling-robustness
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - apps/ops-api/src/routes/admin.ts
  - apps/ops-api/src/routes/agents.ts
  - apps/ops-api/src/routes/ai-budget.ts
  - apps/ops-api/src/routes/alerts.ts
  - apps/ops-api/src/routes/archive.ts
  - apps/ops-api/src/routes/call-audits.ts
  - apps/ops-api/src/routes/call-logs.ts
  - apps/ops-api/src/routes/change-requests.ts
  - apps/ops-api/src/routes/chargebacks.ts
  - apps/ops-api/src/routes/cs-reps.ts
  - apps/ops-api/src/routes/payroll.ts
  - apps/ops-api/src/routes/pending-terms.ts
  - apps/ops-api/src/routes/products.ts
  - apps/ops-api/src/routes/sales.ts
  - apps/ops-api/src/routes/service.ts
  - apps/ops-api/src/routes/users.ts
  - apps/ops-api/src/routes/webhooks.ts
autonomous: true
requirements: [EH-02]
must_haves:
  truths:
    - "Every route that reads req.query validates through Zod first -- no raw `as string` casts on query params"
    - "Every route that reads req.params.id validates through Zod first -- no raw access without schema parse"
    - "Invalid query params return 400 with { error: 'message', details: {...} } via zodErr()"
    - "Invalid route params return 400 with { error: 'message', details: {...} } via zodErr()"
    - "All existing functionality works identically for valid inputs"
  artifacts:
    - path: "apps/ops-api/src/routes/sales.ts"
      provides: "Zod-validated query params for date range, view, and route params for id"
      contains: "dateRangeQuerySchema"
    - path: "apps/ops-api/src/routes/agents.ts"
      provides: "Zod-validated query params for all, permanent, and route params for id"
      contains: "idParamSchema"
    - path: "apps/ops-api/src/routes/call-logs.ts"
      provides: "Zod-validated query params for min_call_length, max_call_length, tier"
      contains: "safeParse"
  key_links:
    - from: "all 17 route files"
      to: "apps/ops-api/src/routes/helpers.ts"
      via: "import { dateRangeQuerySchema, idParamSchema, zodErr }"
      pattern: "import.*dateRangeQuerySchema.*from.*helpers"
---

<objective>
Add Zod validation to every route handler that reads from req.query or req.params without prior validation. This is a systematic audit-and-fix across all 17 route files.

Purpose: Eliminate all raw `req.query` and `req.params` access so invalid inputs return clean 400 errors instead of silently passing through or causing unexpected behavior.
Output: All 17 route files have full Zod coverage on query params and route params.
</objective>

<execution_context>
@./.claude/skills/express/SKILL.md
@./.claude/skills/zod/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-error-handling-robustness/27-CONTEXT.md
@.planning/phases/27-error-handling-robustness/27-RESEARCH.md
@.planning/phases/27-error-handling-robustness/27-01-SUMMARY.md

<interfaces>
<!-- Schemas created in Plan 01, available from helpers.ts -->

From apps/ops-api/src/routes/helpers.ts (after Plan 01):
```typescript
export function zodErr(ze: z.ZodError): { error: string; details: any };
export const asyncHandler: (fn: ...) => ...;
export function dateRange(range?: string, from?: string, to?: string): { gte: Date; lt: Date } | undefined;
export function handlePrismaError(err: unknown, res: Response): Response;
export const dateRangeQuerySchema: z.ZodObject<{ range, from, to }>;
export const idParamSchema: z.ZodObject<{ id: z.ZodString }>;
export const booleanQueryParam: z.ZodOptional<...>;
```

Validation pattern to follow (already established in codebase for req.body):
```typescript
const parsed = schema.safeParse(req.query);  // or req.params
if (!parsed.success) return res.status(400).json(zodErr(parsed.error));
// use parsed.data.fieldName instead of req.query.fieldName
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Zod validation to routes with date range queries and simple query params (8 files)</name>
  <files>
    apps/ops-api/src/routes/admin.ts,
    apps/ops-api/src/routes/ai-budget.ts,
    apps/ops-api/src/routes/agents.ts,
    apps/ops-api/src/routes/products.ts,
    apps/ops-api/src/routes/archive.ts,
    apps/ops-api/src/routes/alerts.ts,
    apps/ops-api/src/routes/users.ts,
    apps/ops-api/src/routes/webhooks.ts
  </files>
  <read_first>
    - apps/ops-api/src/routes/helpers.ts (verify dateRangeQuerySchema, idParamSchema, booleanQueryParam exports from Plan 01)
    - apps/ops-api/src/routes/admin.ts (raw dateRange query cast on line 14)
    - apps/ops-api/src/routes/ai-budget.ts (raw dateRange query cast on line 38)
    - apps/ops-api/src/routes/agents.ts (req.query.all, req.query.permanent raw, req.params.id raw in 7+ handlers)
    - apps/ops-api/src/routes/products.ts (req.query.all, req.query.permanent raw, req.params.id raw in 10+ handlers)
    - apps/ops-api/src/routes/archive.ts (req.query.cutoffDays raw parseInt on line 32)
    - apps/ops-api/src/routes/alerts.ts (req.params.id, req.params.agentId raw)
    - apps/ops-api/src/routes/users.ts (req.params.id raw in 2 handlers)
    - apps/ops-api/src/routes/webhooks.ts (req.query.api_key raw -- auth param, minimal validation)
  </read_first>
  <action>
For each file, import the needed schemas from `./helpers` and replace raw `req.query`/`req.params` access with Zod-validated equivalents. The pattern for every fix is the same:

```typescript
// BEFORE (raw access):
const dr = dateRange(req.query.range as string, req.query.from as string, req.query.to as string);

// AFTER (Zod-validated):
const qp = dateRangeQuerySchema.safeParse(req.query);
if (!qp.success) return res.status(400).json(zodErr(qp.error));
const dr = dateRange(qp.data.range, qp.data.from, qp.data.to);
```

**File-by-file changes:**

**admin.ts** — Add `import { dateRangeQuerySchema, zodErr } from "./helpers"` (zodErr may already be imported, just add dateRangeQuerySchema). In the GET handler that uses `dateRange()` (line 14): parse `req.query` through `dateRangeQuerySchema.safeParse(req.query)`, check `!parsed.success`, use `parsed.data.range`, `parsed.data.from`, `parsed.data.to` instead of raw casts.

**ai-budget.ts** — Same pattern as admin.ts. Import `dateRangeQuerySchema`. In the GET handler with `dateRange()` call (line 38): replace raw `req.query.range as string` casts with Zod-parsed values.

**agents.ts** — Import `idParamSchema, zodErr` from helpers. For every handler that uses `req.params.id` (DELETE /:id, PUT /reactivate/:id, PUT /:id, PUT /lead-sources/:id, DELETE /lead-sources/:id): add `const pp = idParamSchema.safeParse(req.params); if (!pp.success) return res.status(400).json(zodErr(pp.error));` then use `pp.data.id`. For `req.query.all` (GET /agents): define inline schema `z.object({ all: z.enum(["true", "false"]).optional() })`, parse, use `parsed.data.all === "true"`. For `req.query.permanent` (DELETE /:id): include `permanent: z.enum(["true", "false"]).optional()` in the query schema for that handler.

**products.ts** — Same pattern as agents.ts. Import `idParamSchema, zodErr`. Add id param validation to all handlers using `req.params.id` (there are ~10 handlers). For `req.query.all` and `req.query.permanent`: define inline schemas like agents.ts.

**archive.ts** — Import `zodErr` from helpers (already done in Plan 01). For GET /archive/preview: replace the raw `parseInt(String(req.query.cutoffDays ?? "90"), 10)` with a Zod schema: `z.object({ cutoffDays: z.coerce.number().int().min(1).default(90) })`. Parse `req.query`, return 400 on failure, use `parsed.data.cutoffDays`.

**alerts.ts** — Import `idParamSchema, zodErr`. For the 3 handlers using `req.params.id` and `req.params.agentId`: add param validation. For the agentId handler, define `z.object({ agentId: z.string().min(1) })`.

**users.ts** — Import `idParamSchema, zodErr`. For PUT /:id and DELETE /:id: add `idParamSchema.safeParse(req.params)` validation.

**webhooks.ts** — The `req.query.api_key` on line 13 is used alongside `req.headers["x-webhook-secret"]` for auth. Add minimal validation: `z.object({ api_key: z.string().optional() }).safeParse(req.query)`. This just ensures the type is correct without changing auth behavior.

**CRITICAL:** Do NOT change existing behavior for valid inputs. Only add validation that rejects invalid inputs earlier. The `dateRange()` helper function still handles the actual date logic -- we just ensure the values reaching it are validated strings, not arrays or other unexpected Express query types.

**CRITICAL:** All Zod validation errors MUST use `zodErr()` wrapper per D-08.
  </action>
  <verify>
    <automated>cd C:/Users/javer/Documents/Repositories/ai-calling-backend && npx tsc --noEmit --project apps/ops-api/tsconfig.json 2>&1 | head -30 && echo "---" && grep -rn "req\.query\.\w\+ as string" apps/ops-api/src/routes/admin.ts apps/ops-api/src/routes/ai-budget.ts apps/ops-api/src/routes/agents.ts apps/ops-api/src/routes/products.ts apps/ops-api/src/routes/archive.ts apps/ops-api/src/routes/alerts.ts apps/ops-api/src/routes/users.ts apps/ops-api/src/routes/webhooks.ts 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - admin.ts contains `dateRangeQuerySchema.safeParse` and does NOT contain `req.query.range as string`
    - ai-budget.ts contains `dateRangeQuerySchema.safeParse` and does NOT contain `req.query.range as string`
    - agents.ts contains `idParamSchema.safeParse` for handlers using req.params.id
    - agents.ts does NOT contain `req.query.all === "true"` without prior Zod parse
    - products.ts contains `idParamSchema.safeParse` for handlers using req.params.id
    - products.ts does NOT contain `req.query.all === "true"` without prior Zod parse
    - archive.ts GET /preview uses Zod schema instead of raw `parseInt(String(req.query.cutoffDays`
    - alerts.ts contains `idParamSchema.safeParse` or equivalent param validation
    - users.ts contains `idParamSchema.safeParse` for PUT and DELETE handlers
    - All Zod failures use `zodErr()` wrapper (no raw `parsed.error.flatten()`)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>8 route files have Zod validation on all query params and route params. No raw `as string` casts on req.query. No raw req.params.id access without validation. Invalid inputs return 400 with zodErr-formatted response.</done>
</task>

<task type="auto">
  <name>Task 2: Add Zod validation to routes with complex queries and remaining files (9 files)</name>
  <files>
    apps/ops-api/src/routes/call-audits.ts,
    apps/ops-api/src/routes/call-logs.ts,
    apps/ops-api/src/routes/change-requests.ts,
    apps/ops-api/src/routes/chargebacks.ts,
    apps/ops-api/src/routes/cs-reps.ts,
    apps/ops-api/src/routes/payroll.ts,
    apps/ops-api/src/routes/pending-terms.ts,
    apps/ops-api/src/routes/sales.ts,
    apps/ops-api/src/routes/service.ts
  </files>
  <read_first>
    - apps/ops-api/src/routes/helpers.ts (verify all exported schemas)
    - apps/ops-api/src/routes/call-audits.ts (dateRange casts on lines 13, 32, 89; req.query.agentId on line 35; req.params.id on lines 47, 69, 78, 80)
    - apps/ops-api/src/routes/call-logs.ts (req.query.min_call_length, max_call_length, tier on lines 58-60, 110-112)
    - apps/ops-api/src/routes/change-requests.ts (req.query.status cast on lines 13, 123; req.params.id on lines 26, 106, 137, 214)
    - apps/ops-api/src/routes/chargebacks.ts (dateRange casts on lines 162, 177, 209; req.params.id on lines 118, 136, 150)
    - apps/ops-api/src/routes/cs-reps.ts (req.params.id on lines 76, 83)
    - apps/ops-api/src/routes/payroll.ts (req.params.id used in 7 handlers)
    - apps/ops-api/src/routes/pending-terms.ts (dateRange cast on line 75; req.query.groupBy on line 79; req.params.id on lines 104, 119, 133)
    - apps/ops-api/src/routes/sales.ts (dateRange casts on lines 210, 526, 582; req.query.view on line 616; req.params.id on lines 236, 280, 416, 437, 507, 517)
    - apps/ops-api/src/routes/service.ts (req.params.id on lines 30, 135, 159)
  </read_first>
  <action>
Apply the same Zod validation pattern to all remaining route files. Import needed schemas from `./helpers`.

**File-by-file changes:**

**call-audits.ts** — Import `dateRangeQuerySchema, idParamSchema, zodErr`. 4 GET handlers use `dateRange()` with raw casts: replace with `dateRangeQuerySchema.safeParse(req.query)`. For the handler that also uses `req.query.agentId` (line 35), extend the schema: `dateRangeQuerySchema.extend({ agentId: z.string().optional() })`. For 4 handlers using `req.params.id`: add `idParamSchema.safeParse(req.params)`.

**call-logs.ts** — Import `zodErr`. Define inline query schema for the two GET handlers:
```typescript
const callLogQuerySchema = z.object({
  queue_id: z.string().optional(),
  list_id: z.string().optional(),
  min_call_length: z.coerce.number().optional(),
  max_call_length: z.coerce.number().optional(),
  tier: z.enum(["short", "medium", "long"]).optional(),
});
```
Note: `queue_id` and `list_id` already have some validation in the existing body schemas. Check what already exists and only add what's missing. The two GET handlers (lines ~55 and ~107) share the same query params pattern. Parse with `callLogQuerySchema.safeParse(req.query)` and use `parsed.data.*` instead of raw `req.query.*`.

**change-requests.ts** — Import `idParamSchema, zodErr`. For the 2 GET handlers using `req.query.status` (lines 13, 123): define `z.object({ status: z.string().optional().default("PENDING") })`, parse and use. For 4 handlers using `req.params.id`: add id param validation.

**chargebacks.ts** — Import `dateRangeQuerySchema, idParamSchema, zodErr`. For 3 GET handlers using `dateRange()`: replace raw casts with schema parse. For 3 handlers using `req.params.id`: add id param validation.

**cs-reps.ts** — Import `idParamSchema, zodErr`. For 2 handlers using `req.params.id` (PUT and DELETE): add id param validation.

**payroll.ts** — Import `idParamSchema, zodErr`. For 7 handlers using `req.params.id`: add id param validation. These include PUT /:id/status, GET /:id/entries, DELETE /:id, GET /entries/:id, and PUT /entries/:id.

**pending-terms.ts** — Import `dateRangeQuerySchema, idParamSchema, zodErr`. For the GET handler using `dateRange()` + `req.query.groupBy` (line 75-79): extend schema: `dateRangeQuerySchema.extend({ groupBy: z.enum(["holdDate", "saleDate"]).optional() })`. For 3 handlers using `req.params.id`: add id param validation.

**sales.ts** — Import `dateRangeQuerySchema, idParamSchema, zodErr`. For 3 GET handlers using `dateRange()`: replace raw casts with schema parse. For the handler using `req.query.view` (line 616): define `dateRangeQuerySchema.extend({ view: z.enum(["weekly", "monthly"]).optional().default("weekly") })`. For 6+ handlers using `req.params.id`: add id param validation.

**service.ts** — Import `idParamSchema, zodErr`. For 3 handlers using `req.params.id`: add id param validation.

**CRITICAL:** Same rules as Task 1:
- Do NOT change behavior for valid inputs
- All Zod errors use `zodErr()` (D-08)
- `req.params.id` just needs non-empty string validation (Prisma P2025 handles invalid IDs per research anti-pattern guidance)
- Keep the `{ error: "message" }` response format (D-01)
  </action>
  <verify>
    <automated>cd C:/Users/javer/Documents/Repositories/ai-calling-backend && npx tsc --noEmit --project apps/ops-api/tsconfig.json 2>&1 | head -30 && echo "---" && grep -rn "req\.query\.\w\+ as string" apps/ops-api/src/routes/ 2>&1 | grep -v node_modules | grep -v ".d.ts"; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `req.query.* as string` across all route files (grep returns empty)
    - call-audits.ts contains `dateRangeQuerySchema` import and usage
    - call-audits.ts contains `idParamSchema` usage for req.params.id handlers
    - call-logs.ts defines callLogQuerySchema with min_call_length, max_call_length, tier fields
    - call-logs.ts does NOT contain `req.query.min_call_length ? Number(req.query.min_call_length)` (replaced with Zod coerce)
    - change-requests.ts contains `idParamSchema` and status query schema
    - change-requests.ts does NOT contain `req.query.status as string`
    - chargebacks.ts contains `dateRangeQuerySchema` and `idParamSchema`
    - cs-reps.ts contains `idParamSchema`
    - payroll.ts contains `idParamSchema` for all 7 id-param handlers
    - pending-terms.ts contains `dateRangeQuerySchema` with groupBy extension
    - pending-terms.ts does NOT contain `req.query.groupBy ===` without prior Zod parse
    - sales.ts contains `dateRangeQuerySchema` and `idParamSchema`
    - sales.ts does NOT contain `req.query.view ===` without prior Zod parse
    - service.ts contains `idParamSchema`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All 9 remaining route files have Zod validation on all query params and route params. Combined with Task 1, all 17 route files have zero raw req.query/req.params access. Phase-wide grep confirms no `as string` casts on query params remain.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit --project apps/ops-api/tsconfig.json` exits 0
2. No raw query casts: `grep -rn "req\.query\.\w\+ as string" apps/ops-api/src/routes/` returns empty
3. No raw params access without validation: Spot check 5 files for `idParamSchema.safeParse` before `req.params.id` usage
4. All Zod errors use zodErr: `grep -rn "parsed\.error\.flatten\b" apps/ops-api/src/routes/` returns empty (no raw flatten calls)
5. Existing Morgan tests still pass: `npm test` exits 0
</verification>

<success_criteria>
- Every route handler that reads req.query validates through Zod first
- Every route handler that reads req.params validates through Zod first
- Zero occurrences of `req.query.* as string` in any route file
- All validation errors use zodErr() wrapper
- TypeScript compiles without errors
- No behavior change for valid inputs
</success_criteria>

<output>
After completion, create `.planning/phases/27-error-handling-robustness/27-02-SUMMARY.md`
</output>
