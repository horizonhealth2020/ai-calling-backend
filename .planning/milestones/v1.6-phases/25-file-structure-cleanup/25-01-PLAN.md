---
phase: 25-file-structure-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/morgan/index.js
  - apps/morgan/voiceGateway.js
  - apps/morgan/morganToggle.js
  - apps/morgan/timeUtils.js
  - apps/morgan/rateLimitState.js
  - apps/morgan/jest.config.js
  - apps/morgan/__tests__/helpers.test.js
  - apps/morgan/__tests__/integration.test.js
  - apps/morgan/__tests__/morganToggle.test.js
  - apps/morgan/__tests__/queueProcessor.test.js
  - apps/morgan/__tests__/rateLimitState.test.js
  - apps/morgan/__tests__/timeUtils.test.js
  - apps/morgan/__tests__/voiceGateway.test.js
  - package.json
autonomous: true
requirements:
  - FS-01

must_haves:
  truths:
    - "Morgan tests pass from new location via npm test"
    - "Morgan entry point resolves at apps/morgan/index.js"
    - "No Morgan .js files remain at repository root"
    - "package.json start script points to apps/morgan/index.js"
  artifacts:
    - path: "apps/morgan/index.js"
      provides: "Morgan entry point at new location"
    - path: "apps/morgan/voiceGateway.js"
      provides: "Vapi outbound call logic"
    - path: "apps/morgan/jest.config.js"
      provides: "Jest config for Morgan tests"
    - path: "package.json"
      provides: "Updated start/test scripts"
  key_links:
    - from: "package.json"
      to: "apps/morgan/index.js"
      via: "start script and main field"
      pattern: "apps/morgan/index\\.js"
    - from: "package.json"
      to: "apps/morgan/jest.config.js"
      via: "test script --config flag"
      pattern: "--config apps/morgan/jest\\.config\\.js"
---

<objective>
Relocate all Morgan voice service files from the repository root into `apps/morgan/` using `git mv` to preserve history, and update package.json scripts to point to the new locations.

Purpose: Morgan is a working production voice service that currently clutters the repo root alongside monorepo config. Moving it into `apps/morgan/` gives the repo a professional structure while keeping Morgan fully functional (zero behavior change per user requirement).

Output: All Morgan source files, tests, and jest config live under `apps/morgan/`. `npm test` and `npm start` work from the new paths.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-file-structure-cleanup/25-CONTEXT.md
@.planning/phases/25-file-structure-cleanup/25-RESEARCH.md

@package.json
@index.js
@voiceGateway.js
@morganToggle.js
@timeUtils.js
@rateLimitState.js
@jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Morgan files to apps/morgan/ and update package.json</name>
  <files>
    apps/morgan/index.js, apps/morgan/voiceGateway.js, apps/morgan/morganToggle.js,
    apps/morgan/timeUtils.js, apps/morgan/rateLimitState.js, apps/morgan/jest.config.js,
    apps/morgan/__tests__/, package.json
  </files>
  <read_first>
    package.json, index.js, voiceGateway.js, morganToggle.js, timeUtils.js, rateLimitState.js, jest.config.js
  </read_first>
  <action>
    1. Create target directory:
       ```
       mkdir -p apps/morgan
       ```

    2. Move all Morgan source files using `git mv` (preserves git blame history):
       ```
       git mv index.js apps/morgan/index.js
       git mv voiceGateway.js apps/morgan/voiceGateway.js
       git mv morganToggle.js apps/morgan/morganToggle.js
       git mv timeUtils.js apps/morgan/timeUtils.js
       git mv rateLimitState.js apps/morgan/rateLimitState.js
       ```

    3. Move test directory and jest config:
       ```
       git mv __tests__ apps/morgan/__tests__
       git mv jest.config.js apps/morgan/jest.config.js
       ```

    4. NO changes needed to Morgan source file require paths. All files use `./` relative requires to siblings (e.g., `require("./voiceGateway")`). Since all files move together, these paths remain valid.

    5. NO changes needed to test file require paths. Tests use `../` to reach siblings from `__tests__/` (e.g., `require('../morganToggle')`). Since `__tests__/` remains a child of the Morgan directory, these paths remain valid.

    6. Update `package.json` -- change these specific fields (leave everything else untouched):
       - `"main"`: change from `"index.js"` to `"apps/morgan/index.js"`
       - `"start"`: change from `"node index.js"` to `"node apps/morgan/index.js"`
       - `"test"`: change from `"jest"` to `"jest --config apps/morgan/jest.config.js"`
       - `"test:watch"`: change from `"jest --watch"` to `"jest --config apps/morgan/jest.config.js --watch"`
       - `"test:coverage"`: change from `"jest --coverage"` to `"jest --config apps/morgan/jest.config.js --coverage"`

    CRITICAL per user decision D-04: Do NOT create `apps/morgan/package.json`. Morgan deps stay in root package.json. Do NOT modify any Morgan source code behavior.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/morgan/index.js` exists (verify: `test -f apps/morgan/index.js`)
    - `apps/morgan/voiceGateway.js` exists (verify: `test -f apps/morgan/voiceGateway.js`)
    - `apps/morgan/morganToggle.js` exists (verify: `test -f apps/morgan/morganToggle.js`)
    - `apps/morgan/timeUtils.js` exists (verify: `test -f apps/morgan/timeUtils.js`)
    - `apps/morgan/rateLimitState.js` exists (verify: `test -f apps/morgan/rateLimitState.js`)
    - `apps/morgan/jest.config.js` exists (verify: `test -f apps/morgan/jest.config.js`)
    - `apps/morgan/__tests__/voiceGateway.test.js` exists (verify: `test -f apps/morgan/__tests__/voiceGateway.test.js`)
    - No .js files remain at repo root (verify: `ls *.js 2>/dev/null` returns empty)
    - package.json `"main"` field is `"apps/morgan/index.js"` (verify: grep `"main": "apps/morgan/index.js"` package.json)
    - package.json `"start"` script is `"node apps/morgan/index.js"` (verify: grep `"node apps/morgan/index.js"` package.json)
    - package.json `"test"` script contains `--config apps/morgan/jest.config.js` (verify: grep `apps/morgan/jest.config.js` package.json)
    - `npm test` passes with all 7 test files found (verify: npm test exits 0)
    - No `apps/morgan/package.json` exists (verify: `test ! -f apps/morgan/package.json`)
  </acceptance_criteria>
  <done>All Morgan files live under apps/morgan/, no .js files at repo root, npm test passes, npm start would resolve apps/morgan/index.js</done>
</task>

</tasks>

<verification>
- `npm test` passes (all 7 Morgan test files found and green)
- `ls *.js 2>/dev/null` at repo root returns nothing
- `test -f apps/morgan/index.js && echo PASS` returns PASS
- `grep "apps/morgan/index.js" package.json` matches start script and main field
- `grep "apps/morgan/jest.config.js" package.json` matches test scripts
- `test ! -f apps/morgan/package.json && echo PASS` returns PASS (no separate package.json per D-04)
</verification>

<success_criteria>
Morgan voice service files relocated to apps/morgan/ with zero behavior change. All tests pass from the new location. Package.json scripts updated. No Morgan source code modified -- only file locations and config paths changed.
</success_criteria>

<output>
After completion, create `.planning/phases/25-file-structure-cleanup/25-01-SUMMARY.md`
</output>
