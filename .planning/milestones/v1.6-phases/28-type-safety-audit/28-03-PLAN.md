---
phase: 28-type-safety-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ops-api/src/socket.ts
  - apps/ops-api/src/routes/helpers.ts
autonomous: true
gap_closure: true
requirements: [EH-04, DC-02]

must_haves:
  truths:
    - "Every Socket.IO emit function wraps io?.emit() in try/catch so a failed emit never crashes the server"
    - "handlePrismaError is no longer exported or defined (orphaned dead code removed)"
  artifacts:
    - path: "apps/ops-api/src/socket.ts"
      provides: "Socket.IO emit functions with try/catch wrappers"
      contains: "catch"
    - path: "apps/ops-api/src/routes/helpers.ts"
      provides: "Shared route helpers without orphaned handlePrismaError"
  key_links:
    - from: "apps/ops-api/src/socket.ts"
      to: "console.error"
      via: "catch block in each emit function"
      pattern: "catch.*console\\.error"
---

<objective>
Restore Socket.IO try/catch wrappers stripped by Phase 28 worktree merge, and remove the orphaned handlePrismaError export.

Purpose: Close GAP-1 (EH-04 regression) and satisfy DC-02 (orphaned export).
Output: socket.ts with 10 try/catch-wrapped emit functions, helpers.ts without handlePrismaError.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.6-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore try/catch wrappers in all 10 Socket.IO emit functions</name>
  <files>apps/ops-api/src/socket.ts</files>
  <read_first>apps/ops-api/src/socket.ts</read_first>
  <action>
Read socket.ts FIRST to see its current state. The file has 10 emit functions that currently call `io?.emit(...)` without try/catch. Phase 28 type changes (typed payloads, Record<string, unknown>, etc.) must be PRESERVED -- only ADD try/catch wrappers around each emit call.

For each of these 10 functions, wrap the io?.emit() call in try/catch:
1. emitAuditStarted - event "processing_started"
2. emitAuditStatus - event "audit_status"
3. emitAuditComplete - event "new_audit"
4. emitAuditFailed - event "processing_failed"
5. emitSaleChanged - event "sale:changed"
6. emitCSChanged - event "cs:changed"
7. emitAlertCreated - event "alert:created"
8. emitAlertResolved - event "alert:resolved"
9. emitServicePayrollChanged - event "service-payroll:changed"
10. emitClawbackCreated - event "clawback:created"

Pattern for each function (example for emitAuditStarted):
```typescript
export function emitAuditStarted(data: { callLogId: string; agentName: string }) {
  try {
    io?.emit("processing_started", data);
  } catch (err) {
    console.error("Socket.IO emit error (processing_started):", err);
  }
}
```

CRITICAL: Do NOT change any type annotations, interfaces, or function signatures. Only add try/catch inside function bodies. Keep all existing interfaces (SaleChangedType, SaleChangedPayload, CSChangedPayload, AlertCreatedPayload, ServicePayrollChangedPayload, ClawbackCreatedPayload) exactly as they are.
  </action>
  <verify>
    <automated>cd C:/Users/javer/Documents/Repositories/ai-calling-backend && node -e "const c=require('fs').readFileSync('apps/ops-api/src/socket.ts','utf8'); const catches=(c.match(/catch/g)||[]).length; const emits=(c.match(/io\?\.emit/g)||[]).length; console.log('catches:',catches,'emits:',emits); if(catches<10||emits<10) process.exit(1); console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "catch" apps/ops-api/src/socket.ts returns 10 or more
    - grep -c "io?.emit" apps/ops-api/src/socket.ts returns 10
    - grep -c "console.error" apps/ops-api/src/socket.ts returns 10 or more
    - All Phase 28 type annotations (Record<string, unknown>, typed payloads) are preserved
    - npx tsc --noEmit passes for socket.ts
  </acceptance_criteria>
  <done>All 10 emit functions have try/catch wrappers that log errors without re-throwing. Phase 28 type safety changes are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned handlePrismaError export from helpers.ts</name>
  <files>apps/ops-api/src/routes/helpers.ts</files>
  <read_first>apps/ops-api/src/routes/helpers.ts</read_first>
  <action>
Read helpers.ts FIRST. Remove the `handlePrismaError` function (lines ~89-114) and its associated `Prisma` import from `@prisma/client` (line 3).

Keep ALL other exports intact:
- zodErr (line 6)
- asyncHandler (line 15)
- PrismaClientError interface and isPrismaError (lines 19-25)
- dateRange function (line 28)
- dateRangeQuerySchema (line 117)
- idParamSchema (line 124)
- booleanQueryParam (line 129)

The `Prisma` import on line 3 (`import { Prisma } from "@prisma/client"`) is ONLY used by handlePrismaError. Remove it. The `isPrismaError` function uses the local `PrismaClientError` interface, not the `Prisma` namespace, so it is unaffected.

VERIFY after removal: confirm no file in the codebase imports handlePrismaError.
  </action>
  <verify>
    <automated>cd C:/Users/javer/Documents/Repositories/ai-calling-backend && node -e "const c=require('fs').readFileSync('apps/ops-api/src/routes/helpers.ts','utf8'); if(c.includes('handlePrismaError')) { console.log('FAIL: handlePrismaError still present'); process.exit(1); } if(c.includes('from \"@prisma/client\"')) { console.log('FAIL: Prisma import still present'); process.exit(1); } console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - grep "handlePrismaError" apps/ops-api/src/routes/helpers.ts returns no results
    - grep "from \"@prisma/client\"" apps/ops-api/src/routes/helpers.ts returns no results
    - All other exports (zodErr, asyncHandler, isPrismaError, dateRange, dateRangeQuerySchema, idParamSchema, booleanQueryParam) still exist
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>handlePrismaError and its Prisma import are removed. All other helpers.ts exports remain intact. DC-02 orphaned export gap closed.</done>
</task>

</tasks>

<verification>
- `grep -c "catch" apps/ops-api/src/socket.ts` returns >= 10
- `grep -c "handlePrismaError" apps/ops-api/src/routes/helpers.ts` returns 0
- `npx tsc --noEmit` passes (no type errors introduced)
- `npm test` passes (no regressions)
</verification>

<success_criteria>
EH-04 restored: All 10 Socket.IO emit functions have try/catch wrappers.
DC-02 closed: handlePrismaError orphaned export removed.
Phase 28 type safety changes preserved in all modified files.
</success_criteria>

<output>
After completion, create `.planning/phases/28-type-safety-audit/28-03-SUMMARY.md`
</output>
