---
phase: 17-documentation-permission-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - apps/ops-api/src/routes/index.ts
autonomous: true
requirements:
  - TRKC-02
  - TRKT-02
  - TRKT-06
  - ROLE-04
gap_closure: true

must_haves:
  truths:
    - "TRKC-02 requirement text matches implementation (8 columns, not 15)"
    - "TRKT-02 requirement text matches UI-SPEC (only hold_date red, next_billing green — no active/first_billing blue, no hold_reason red italic)"
    - "TRKT-06 requirement text reflects flat table design (no group-by-agent)"
    - "TRKC-02, TRKT-02, TRKT-06 checkboxes are marked [x] and traceability shows Complete"
    - "POST/PATCH/DELETE /cs-rep-roster endpoints require CUSTOMER_SERVICE, SUPER_ADMIN, or OWNER_VIEW role"
    - "OWNER_VIEW landing card gap is documented as intentional known UX decision"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected requirement text and traceability entries"
      contains: "8 columns"
    - path: "apps/ops-api/src/routes/index.ts"
      provides: "Role-guarded rep roster mutation endpoints"
      contains: "requireRole"
  key_links:
    - from: "apps/ops-api/src/routes/index.ts"
      to: "middleware/auth.ts"
      via: "requireRole middleware on cs-rep-roster routes"
      pattern: "cs-rep-roster.*requireRole"
---

<objective>
Fix stale requirement text in REQUIREMENTS.md for three tracking table requirements (TRKC-02, TRKT-02, TRKT-06), add requireRole guard to rep roster mutation endpoints, and document the OWNER_VIEW landing card gap as an intentional UX decision.

Purpose: Close documentation debt and a low-severity permission gap identified in the v1.1 milestone audit.
Output: Updated REQUIREMENTS.md with correct text/traceability, role-guarded rep roster endpoints, OWNER_VIEW gap documented.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale REQUIREMENTS.md text and traceability entries</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state of all requirement text and traceability table)
    - .planning/v1.1-MILESTONE-AUDIT.md (source of truth for what needs fixing)
    - .planning/phases/14-tracking-tables/14-CONTEXT.md (locked decision: 8 chargeback columns)
    - .planning/phases/15-resolution-polish/15-CONTEXT.md (locked decision: remove agent grouping)
  </read_first>
  <action>
Edit .planning/REQUIREMENTS.md to make 6 specific changes:

**1. TRKC-02 (line 43) — change text from:**
```
- [ ] **TRKC-02**: Chargeback table displays all 15 specified columns with chargeback_amount always in red
```
**to:**
```
- [x] **TRKC-02**: Chargeback table displays 8 data columns (Date Posted, Member, Member ID, Product, Type, Total, Assigned To, Submitted) with chargeback_amount always in red
```

**2. TRKT-02 (line 52) — change text from:**
```
- [ ] **TRKT-02**: Pending terms table displays all specified columns with correct color coding (next_billing green, active/first_billing blue, hold_date red, hold_reason red italic)
```
**to:**
```
- [x] **TRKT-02**: Pending terms table displays 7 columns (Member Name, Member ID, Phone, Product, Hold Date, Next Billing, Assigned To) with hold_date in red and next_billing in green
```

**3. TRKT-06 (line 56) — change text from:**
```
- [ ] **TRKT-06**: Table supports group-by-agent with collapsible sections (agent name as group header only)
```
**to:**
```
- [x] **TRKT-06**: Table renders as flat rows (agent grouping removed per Phase 15 design decision; agent_name/agent_id stored but not displayed)
```

**4. Traceability table — change TRKC-02 entry (line 118) from:**
```
| TRKC-02 | Phase 17 | Pending |
```
**to:**
```
| TRKC-02 | Phase 14 | Complete |
```

**5. Traceability table — change TRKT-02 entry (line 124) from:**
```
| TRKT-02 | Phase 17 | Pending |
```
**to:**
```
| TRKT-02 | Phase 14 | Complete |
```

**6. Traceability table — change TRKT-06 entry (line 128) from:**
```
| TRKT-06 | Phase 17 | Pending |
```
**to:**
```
| TRKT-06 | Phase 15 | Complete |
```

**7. Coverage summary (line 144) — change from:**
```
- Pending (Phase 17 gap closure): 3 (TRKC-02, TRKT-02, TRKT-06)
```
**to:**
```
- Pending (Phase 17 gap closure): 0 (all resolved)
```

**8. Add OWNER_VIEW known UX decision — append a new section before the Traceability heading:**
```
## Known UX Decisions

| Decision | REQ-ID | Rationale |
|----------|--------|-----------|
| OWNER_VIEW has no CS dashboard card on auth-portal landing page | ROLE-04 | Intentional per Phase 16 CONTEXT.md. OWNER_VIEW can access CS dashboard via direct URL. Landing card omitted to keep owner landing page focused on KPI summary. |
```

**9. Update the "Last updated" footer line to:**
```
*Last updated: 2026-03-18 after Phase 17 gap closure*
```
  </action>
  <verify>
    <automated>grep -c "8 data columns" .planning/REQUIREMENTS.md && grep -c "flat rows" .planning/REQUIREMENTS.md && grep -c "hold_date in red and next_billing in green" .planning/REQUIREMENTS.md && grep "TRKC-02" .planning/REQUIREMENTS.md | grep -c "Complete" && grep "TRKT-02" .planning/REQUIREMENTS.md | grep -c "Complete" && grep "TRKT-06" .planning/REQUIREMENTS.md | grep -c "Complete" && grep -c "Known UX Decisions" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md line for TRKC-02 contains "[x] **TRKC-02**: Chargeback table displays 8 data columns"
    - REQUIREMENTS.md line for TRKT-02 contains "[x] **TRKT-02**: Pending terms table displays 7 columns"
    - REQUIREMENTS.md line for TRKT-06 contains "[x] **TRKT-06**: Table renders as flat rows"
    - REQUIREMENTS.md traceability row for TRKC-02 contains "Phase 14 | Complete"
    - REQUIREMENTS.md traceability row for TRKT-02 contains "Phase 14 | Complete"
    - REQUIREMENTS.md traceability row for TRKT-06 contains "Phase 15 | Complete"
    - REQUIREMENTS.md contains "## Known UX Decisions" section with OWNER_VIEW row
    - No requirement lines remain unchecked (no "- [ ]" lines in v1.1 section)
  </acceptance_criteria>
  <done>All three stale requirement texts updated to match actual implementation. Traceability table shows Complete for all 40 requirements. OWNER_VIEW landing card gap documented as intentional.</done>
</task>

<task type="auto">
  <name>Task 2: Add requireRole guard to rep roster mutation endpoints</name>
  <files>apps/ops-api/src/routes/index.ts</files>
  <read_first>
    - apps/ops-api/src/routes/index.ts (current rep roster route definitions around line 2060-2086)
    - apps/ops-api/src/middleware/auth.ts (requireRole signature and usage pattern)
  </read_first>
  <action>
In apps/ops-api/src/routes/index.ts, add requireRole middleware to the three rep roster mutation endpoints. The GET endpoint stays as requireAuth only (read access for all authenticated users is fine — the roster is needed by the submission UI for assignment dropdowns).

**Change POST (around line 2062) from:**
```typescript
router.post("/cs-rep-roster", requireAuth, asyncHandler(async (req, res) => {
```
**to:**
```typescript
router.post("/cs-rep-roster", requireAuth, requireRole("CUSTOMER_SERVICE", "SUPER_ADMIN", "OWNER_VIEW"), asyncHandler(async (req, res) => {
```

**Change PATCH (around line 2072) from:**
```typescript
router.patch("/cs-rep-roster/:id", requireAuth, asyncHandler(async (req, res) => {
```
**to:**
```typescript
router.patch("/cs-rep-roster/:id", requireAuth, requireRole("CUSTOMER_SERVICE", "SUPER_ADMIN", "OWNER_VIEW"), asyncHandler(async (req, res) => {
```

**Change DELETE (around line 2083) from:**
```typescript
router.delete("/cs-rep-roster/:id", requireAuth, asyncHandler(async (req, res) => {
```
**to:**
```typescript
router.delete("/cs-rep-roster/:id", requireAuth, requireRole("CUSTOMER_SERVICE", "SUPER_ADMIN", "OWNER_VIEW"), asyncHandler(async (req, res) => {
```

Note: requireRole is already imported at line 6 of routes/index.ts. No new import needed.
  </action>
  <verify>
    <automated>grep -n "cs-rep-roster" apps/ops-api/src/routes/index.ts | grep -c "requireRole"</automated>
  </verify>
  <acceptance_criteria>
    - routes/index.ts POST /cs-rep-roster line contains requireRole("CUSTOMER_SERVICE", "SUPER_ADMIN", "OWNER_VIEW")
    - routes/index.ts PATCH /cs-rep-roster/:id line contains requireRole("CUSTOMER_SERVICE", "SUPER_ADMIN", "OWNER_VIEW")
    - routes/index.ts DELETE /cs-rep-roster/:id line contains requireRole("CUSTOMER_SERVICE", "SUPER_ADMIN", "OWNER_VIEW")
    - routes/index.ts GET /cs-rep-roster line does NOT contain requireRole (read access unchanged)
    - grep for "cs-rep-roster" piped to grep "requireRole" returns exactly 3 matches
  </acceptance_criteria>
  <done>POST, PATCH, and DELETE /cs-rep-roster endpoints are guarded by requireRole allowing only CUSTOMER_SERVICE, SUPER_ADMIN, and OWNER_VIEW roles. GET remains open to all authenticated users.</done>
</task>

</tasks>

<verification>
1. REQUIREMENTS.md contains no unchecked requirement boxes (all `- [x]`)
2. REQUIREMENTS.md traceability table has no "Pending" entries
3. REQUIREMENTS.md has "Known UX Decisions" section documenting OWNER_VIEW gap
4. routes/index.ts has requireRole on all 3 mutation cs-rep-roster endpoints
5. GET /cs-rep-roster has no requireRole (intentional — read access for assignment UI)
</verification>

<success_criteria>
- All 6 phase success criteria from ROADMAP met:
  1. TRKC-02 says "8 columns" (not "15")
  2. TRKT-02 matches UI-SPEC (no stale color coding references)
  3. TRKT-06 reflects flat table design (no group-by-agent)
  4. ROLE-01, SCHEMA-01, SCHEMA-02 traceability already shows "Complete" (verified pre-existing)
  5. POST/PATCH/DELETE /cs-rep-roster have requireRole guard
  6. OWNER_VIEW landing card gap documented as intentional known UX decision
</success_criteria>

<output>
After completion, create `.planning/phases/17-documentation-permission-cleanup/17-01-SUMMARY.md`
</output>
