---
phase: 15-resolution-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/utils/src/index.ts
  - apps/cs-dashboard/next.config.js
  - apps/manager-dashboard/next.config.js
  - apps/payroll-dashboard/next.config.js
  - apps/owner-dashboard/next.config.js
  - apps/sales-board/next.config.js
  - apps/cs-dashboard/app/page.tsx
  - apps/manager-dashboard/app/page.tsx
  - apps/payroll-dashboard/app/page.tsx
  - apps/owner-dashboard/app/page.tsx
  - apps/sales-board/app/page.tsx
autonomous: true
requirements: [DASH-05]

must_haves:
  truths:
    - "All dollar amounts across all dashboards render with commas and 2 decimal places"
    - "All dates across all dashboards render as M/D/YYYY without leading zeros"
    - "Formatting functions come from a single shared source in @ops/utils"
  artifacts:
    - path: "packages/utils/src/index.ts"
      provides: "Shared formatDollar, formatNegDollar, and formatDate helpers"
      exports: ["formatDollar", "formatNegDollar", "formatDate"]
    - path: "apps/cs-dashboard/next.config.js"
      provides: "transpilePackages includes @ops/utils"
      contains: "@ops/utils"
  key_links:
    - from: "apps/cs-dashboard/app/page.tsx"
      to: "packages/utils/src/index.ts"
      via: "import { formatDollar, formatDate } from '@ops/utils'"
      pattern: "from ['\"]@ops/utils['\"]"
    - from: "apps/manager-dashboard/app/page.tsx"
      to: "packages/utils/src/index.ts"
      via: "import from @ops/utils"
      pattern: "from ['\"]@ops/utils['\"]"
---

<objective>
Extract formatDollar, formatNegDollar, and formatDate helpers to @ops/utils as shared functions. Update all 5 dashboard apps to import from @ops/utils and add @ops/utils to each app's transpilePackages. Remove duplicate local formatting functions from cs-dashboard.

Purpose: Consistent date and dollar formatting across the entire platform from a single source of truth.
Output: Three shared helpers in @ops/utils, all dashboards using them.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-resolution-polish/15-RESEARCH.md
@.planning/phases/15-resolution-polish/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add formatting helpers to @ops/utils and update transpilePackages</name>
  <files>packages/utils/src/index.ts, apps/cs-dashboard/next.config.js, apps/manager-dashboard/next.config.js, apps/payroll-dashboard/next.config.js, apps/owner-dashboard/next.config.js, apps/sales-board/next.config.js</files>
  <read_first>
    - packages/utils/src/index.ts (current exports: logEvent, logError)
    - apps/cs-dashboard/next.config.js (current transpilePackages: ["@ops/ui", "@ops/auth"])
    - apps/manager-dashboard/next.config.js (check current transpilePackages)
    - apps/payroll-dashboard/next.config.js (check current transpilePackages)
    - apps/owner-dashboard/next.config.js (check current transpilePackages)
    - apps/sales-board/next.config.js (check current transpilePackages)
  </read_first>
  <action>
**1. Add three formatting functions to packages/utils/src/index.ts** (append after existing logError export):

```typescript
/** Format a number as $X,XXX.XX (always positive, 2 decimal places) */
export function formatDollar(n: number): string {
  return "$" + Math.abs(n).toLocaleString("en-US", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2,
  });
}

/** Format a negative dollar as -$X,XXX.XX */
export function formatNegDollar(n: number): string {
  const abs = Math.abs(n).toLocaleString("en-US", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2,
  });
  return `-$${abs}`;
}

/** Format an ISO date string as M/D/YYYY (no leading zeros). Returns "--" for null/undefined. */
export function formatDate(d: string | null | undefined): string {
  if (!d) return "--";
  const [y, m, dd] = d.split("T")[0].split("-");
  return `${parseInt(m)}/${parseInt(dd)}/${y}`;
}
```

**2. Add "@ops/utils" to transpilePackages in each dashboard's next.config.js:**

- `apps/cs-dashboard/next.config.js`: Change `["@ops/ui", "@ops/auth"]` to `["@ops/ui", "@ops/auth", "@ops/utils"]`
- `apps/manager-dashboard/next.config.js`: Add `"@ops/utils"` to the existing array
- `apps/payroll-dashboard/next.config.js`: Add `"@ops/utils"` to the existing array
- `apps/owner-dashboard/next.config.js`: Add `"@ops/utils"` to the existing array
- `apps/sales-board/next.config.js`: Add `"@ops/utils"` to the existing array

Also check each dashboard's package.json for `@ops/utils` in dependencies. If missing, add `"@ops/utils": "workspace:*"` to the dependencies section. (It may already be there since the workspace uses @ops/utils in ops-api.)
  </action>
  <verify>
    <automated>cd "C:/Users/javer/Documents/Repositories/ai-calling-backend" && grep -c "formatDollar\|formatDate\|formatNegDollar" packages/utils/src/index.ts && grep -l "@ops/utils" apps/*/next.config.js | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - packages/utils/src/index.ts contains `export function formatDollar(n: number): string`
    - packages/utils/src/index.ts contains `export function formatNegDollar(n: number): string`
    - packages/utils/src/index.ts contains `export function formatDate(d: string | null | undefined): string`
    - formatDollar body contains `Math.abs(n).toLocaleString("en-US"`
    - formatDate body contains `parseInt(m)}/${parseInt(dd)}/${y}`
    - apps/cs-dashboard/next.config.js contains `"@ops/utils"` in transpilePackages
    - apps/manager-dashboard/next.config.js contains `"@ops/utils"` in transpilePackages
    - apps/payroll-dashboard/next.config.js contains `"@ops/utils"` in transpilePackages
    - apps/owner-dashboard/next.config.js contains `"@ops/utils"` in transpilePackages
    - apps/sales-board/next.config.js contains `"@ops/utils"` in transpilePackages
  </acceptance_criteria>
  <done>Three formatting helpers exported from @ops/utils. All 5 dashboard apps have @ops/utils in transpilePackages.</done>
</task>

<task type="auto">
  <name>Task 2: Update all dashboards to import shared formatting helpers</name>
  <files>apps/cs-dashboard/app/page.tsx, apps/manager-dashboard/app/page.tsx, apps/payroll-dashboard/app/page.tsx, apps/owner-dashboard/app/page.tsx, apps/sales-board/app/page.tsx</files>
  <read_first>
    - apps/cs-dashboard/app/page.tsx (find local formatDollar at ~line 235, fmtDate at ~line 1565, formatNegDollar if exists -- these will be replaced with imports)
    - apps/manager-dashboard/app/page.tsx (search for toFixed, toLocaleString, toLocaleDateString, any date/dollar formatting)
    - apps/payroll-dashboard/app/page.tsx (search for toFixed, toLocaleString, toLocaleDateString, any date/dollar formatting)
    - apps/owner-dashboard/app/page.tsx (search for toFixed, toLocaleString, toLocaleDateString, any date/dollar formatting)
    - apps/sales-board/app/page.tsx (search for toFixed, toLocaleString, toLocaleDateString, any date/dollar formatting)
  </read_first>
  <action>
**For each dashboard app, perform these steps:**

**cs-dashboard/app/page.tsx:**
1. Add import at top: `import { formatDollar, formatNegDollar, formatDate } from "@ops/utils";`
2. Delete the local `function formatDollar(n: number)` definition (~line 235)
3. Delete the local `const fmtDate` definition (~line 1565)
4. Replace all occurrences of `fmtDate(` with `formatDate(` throughout the file
5. If there is a local `formatNegDollar`, delete it too
6. Verify no other local dollar/date formatting functions remain

**manager-dashboard/app/page.tsx:**
1. Add import: `import { formatDollar, formatDate } from "@ops/utils";`
2. Find all inline dollar formatting patterns (e.g., `toFixed(2)`, `toLocaleString(...)`, `$${...}`) and replace with `formatDollar(value)` or `formatNegDollar(value)` as appropriate
3. Find all date formatting patterns (e.g., `toLocaleDateString()`, `new Date(...).toLocaleDateString(...)`) and replace with `formatDate(dateString)`
4. Remove any local formatting helper functions that are now redundant

**payroll-dashboard/app/page.tsx:**
1. Same pattern as manager-dashboard -- add import, find/replace inline formatting
2. Pay special attention to payroll amounts -- they should use `formatDollar` for positive amounts

**owner-dashboard/app/page.tsx:**
1. Same pattern -- add import, find/replace inline formatting

**sales-board/app/page.tsx:**
1. Same pattern -- add import, find/replace inline formatting

**IMPORTANT:** When replacing inline formatting, preserve the sign behavior:
- Positive amounts: use `formatDollar(value)` which always returns positive `$X,XXX.XX`
- Chargeback amounts (negative): use `formatNegDollar(value)` to show `-$X,XXX.XX`
- If a value needs a negative sign, use formatNegDollar. If it should always show positive regardless of input sign, use formatDollar.
- For dates, `formatDate(isoString)` handles null/undefined by returning "--"

Do NOT change any business logic, only the formatting output functions.
  </action>
  <verify>
    <automated>cd "C:/Users/javer/Documents/Repositories/ai-calling-backend" && grep -rn "from ['\"]@ops/utils['\"]" apps/*/app/page.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - apps/cs-dashboard/app/page.tsx contains `import { formatDollar` ... `from "@ops/utils"`
    - apps/cs-dashboard/app/page.tsx does NOT contain `function formatDollar(n: number)` as a local definition
    - apps/cs-dashboard/app/page.tsx does NOT contain `const fmtDate` as a local definition
    - apps/cs-dashboard/app/page.tsx uses `formatDate(` instead of `fmtDate(`
    - apps/manager-dashboard/app/page.tsx contains `from "@ops/utils"`
    - apps/payroll-dashboard/app/page.tsx contains `from "@ops/utils"`
    - apps/owner-dashboard/app/page.tsx contains `from "@ops/utils"`
    - apps/sales-board/app/page.tsx contains `from "@ops/utils"`
    - grep for `from "@ops/utils"` returns 5 matches (one per dashboard)
  </acceptance_criteria>
  <done>All 5 dashboard apps import formatDollar/formatDate from @ops/utils. No duplicate local formatting functions remain in cs-dashboard. All dollar amounts show $X,XXX.XX and all dates show M/D/YYYY.</done>
</task>

</tasks>

<verification>
- `grep -rn "from ['\"]@ops/utils['\"]" apps/*/app/page.tsx` returns 5 results
- `grep -c "export function format" packages/utils/src/index.ts` returns 3
- No local `fmtDate` or duplicate `formatDollar` in cs-dashboard
- All 5 next.config.js files include `@ops/utils` in transpilePackages
</verification>

<success_criteria>
- formatDollar, formatNegDollar, and formatDate are exported from @ops/utils
- All 5 dashboards import formatting from @ops/utils (no local duplicates)
- All transpilePackages arrays include @ops/utils
- Dollar amounts render as $X,XXX.XX, dates render as M/D/YYYY across all dashboards
</success_criteria>

<output>
After completion, create `.planning/phases/15-resolution-polish/15-02-SUMMARY.md`
</output>
