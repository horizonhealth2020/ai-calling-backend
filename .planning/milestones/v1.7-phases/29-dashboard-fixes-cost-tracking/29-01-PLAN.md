---
phase: 29-dashboard-fixes-cost-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ops-dashboard/app/(dashboard)/manager/ManagerSales.tsx
  - apps/ops-dashboard/app/(dashboard)/manager/ManagerConfig.tsx
  - apps/ops-api/src/routes/agents.ts
autonomous: true
requirements:
  - FIX-01
  - FIX-02
  - FIX-03
  - CFG-01
  - CFG-02

must_haves:
  truths:
    - "Manager Agent Sales tab shows total premium (core + addon) per sale row"
    - "Lead source create form includes a Buffer (seconds) field"
    - "Lead source POST API accepts callBufferSeconds"
    - "Manager Config Products section is read-only with no add/edit/delete controls"
    - "Read-only Products table shows product name, type, commission rates, and bundle config"
  artifacts:
    - path: "apps/ops-dashboard/app/(dashboard)/manager/ManagerSales.tsx"
      provides: "Premium column summing core + addon per row"
      contains: "addonTotal"
    - path: "apps/ops-dashboard/app/(dashboard)/manager/ManagerConfig.tsx"
      provides: "Buffer field in create form, read-only products table"
      contains: "callBufferSeconds"
    - path: "apps/ops-api/src/routes/agents.ts"
      provides: "callBufferSeconds in lead source POST Zod schema"
      contains: "callBufferSeconds"
  key_links:
    - from: "ManagerConfig.tsx create form"
      to: "POST /api/lead-sources"
      via: "authFetch POST body includes callBufferSeconds"
      pattern: "callBufferSeconds.*Number"
---

<objective>
Fix premium display bug in Manager Agent Sales, add missing buffer field to lead source create form and API, and convert Manager Config Products section to read-only.

Purpose: Resolve three bugs (premium shows core-only, create form missing buffer field, POST schema missing callBufferSeconds) and convert products section from interactive CRUD to read-only reference view for managers.
Output: Three patched files with bug fixes and products read-only conversion.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-dashboard-fixes-cost-tracking/29-CONTEXT.md
@.planning/phases/29-dashboard-fixes-cost-tracking/29-RESEARCH.md
@.planning/phases/29-dashboard-fixes-cost-tracking/29-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix premium display and lead source create form in dashboard</name>
  <files>apps/ops-dashboard/app/(dashboard)/manager/ManagerSales.tsx, apps/ops-dashboard/app/(dashboard)/manager/ManagerConfig.tsx</files>
  <read_first>
    - apps/ops-dashboard/app/(dashboard)/manager/ManagerSales.tsx (find the premium column around line 422 and the card-level addon summation pattern around lines 388-392)
    - apps/ops-dashboard/app/(dashboard)/manager/ManagerConfig.tsx (find the create form state around line 316, the edit form callBufferSeconds field around line 162/172, and the Products section around lines 447-455)
    - .planning/phases/29-dashboard-fixes-cost-tracking/29-UI-SPEC.md (component contracts for FIX-01, FIX-02, CFG-01/CFG-02)
    - packages/ui/src/tokens.ts (baseLabelStyle, baseInputStyle, baseTdStyle, baseThStyle, baseCardStyle tokens)
  </read_first>
  <action>
**FIX-01 (ManagerSales.tsx):** Find the premium column cell around line 422 that currently shows `formatDollar(Number(s.premium))`. Replace it with addon-inclusive total:

```typescript
const saleWithAddons = s as Sale & { addons?: { premium?: number | null }[] };
const addonTotal = saleWithAddons.addons?.reduce((aSum: number, a) => aSum + Number(a.premium ?? 0), 0) ?? 0;
const rowTotal = Number(s.premium ?? 0) + addonTotal;
```
Then display `formatDollar(rowTotal)` in the cell. Ensure the `addons` field is present in the Sale type or inline-cast it. The card-level logic at lines 388-392 already does the same pattern -- copy it.

**FIX-02 (ManagerConfig.tsx):**
1. Find the `newLS` state initialization (around line 316) which currently has `{ name: "", listId: "", costPerLead: "" }`. Add `callBufferSeconds: "0"` to the initial state object.
2. Find the create form JSX (the form that has name, listId, costPerLead inputs). Add a new input field after costPerLead:
   - Label: "BUFFER (S)" using baseLabelStyle (11px uppercase, weight 700)
   - Input: `type="number"`, `min="0"`, baseInputStyle, value bound to `newLS.callBufferSeconds`
   - onChange: `setNewLS(x => ({ ...x, callBufferSeconds: e.target.value }))`
   - Copy the exact pattern from the edit form's callBufferSeconds field (around line 172)
3. Find the submit handler for the create form. Add `callBufferSeconds: Number(newLS.callBufferSeconds) || 0` to the POST body JSON.

**CFG-01/CFG-02 (ManagerConfig.tsx):**
1. Find the Products section (around lines 447-455) that renders ProductRow components with edit/delete capabilities.
2. Replace the entire Products section with a read-only table inside a card:
   - Card wrapper: baseCardStyle (`--bg-surface` background, `--border-default` border, 12px radius, 24px padding)
   - Section title: "Products" in 15px, weight 700, color `--text-primary`
   - Table headers using baseThStyle: "Product" | "Type" | "Commission" | "Bundle Config"
   - Table body using baseTdStyle for each cell:
     - Product name: plain text
     - Type: Badge component with colors per type: CORE = `--primary-500` text on `--info-bg`, ADDON = `--success` text on `--success-bg`, AD_D = `--warning` text on `--warning-bg`
     - Commission: show commission rate as percentage (e.g., "10%"). For CORE show `commissionRate`, for ADDON/AD_D show addon-specific rate.
     - Bundle Config: For ADDON/AD_D types show the parent/required product name. For CORE show em-dash in `--text-muted` color.
   - Empty state: "No products configured." in 13px, color `--text-muted`
3. Remove or comment out the `saveProduct` function, any add/edit/delete button handlers, and any product form modals that are now unnecessary. Keep the `refreshProducts` prop in the interface to avoid breaking the parent component -- just do not call it.
  </action>
  <verify>
    <automated>cd C:\Users\javer\Documents\Repositories\ai-calling-backend && npx tsc --noEmit --project apps/ops-dashboard/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ManagerSales.tsx contains `addonTotal` variable computing addon sum per row
    - ManagerSales.tsx contains `rowTotal` or equivalent combining `s.premium` + addon total
    - ManagerSales.tsx no longer has a standalone `formatDollar(Number(s.premium))` in the table row (card-level is fine)
    - ManagerConfig.tsx newLS state contains `callBufferSeconds`
    - ManagerConfig.tsx create form has an input with `type="number"` and `min="0"` for callBufferSeconds
    - ManagerConfig.tsx POST body includes `callBufferSeconds: Number(`
    - ManagerConfig.tsx Products section uses `<table` with read-only display
    - ManagerConfig.tsx Products section does NOT contain any edit or delete button for products (no `onSave` calls for products, no product form modal)
    - ManagerConfig.tsx has "No products configured." empty state text
  </acceptance_criteria>
  <done>Premium column shows core + addon total per row. Create form has buffer field. Products section is read-only table with name, type, commission, bundle config columns.</done>
</task>

<task type="auto">
  <name>Task 2: Add callBufferSeconds to lead source POST API schema</name>
  <files>apps/ops-api/src/routes/agents.ts</files>
  <read_first>
    - apps/ops-api/src/routes/agents.ts (find the POST /lead-sources handler around line 75, and the PATCH handler around line 92 which already has callBufferSeconds)
  </read_first>
  <action>
In `apps/ops-api/src/routes/agents.ts`, find the POST `/lead-sources` route handler (around line 75). The current Zod schema is:

```typescript
const schema = z.object({ name: z.string().min(1), listId: z.string().optional(), costPerLead: z.number().min(0).default(0) });
```

Add `callBufferSeconds` to match the PATCH schema (line 92):

```typescript
const schema = z.object({
  name: z.string().min(1),
  listId: z.string().optional(),
  costPerLead: z.number().min(0).default(0),
  callBufferSeconds: z.number().int().min(0).optional(),
});
```

The `prisma.leadSource.create({ data: { ...parsed.data, effectiveDate: new Date() } })` on line 80 will automatically include callBufferSeconds when provided since it spreads parsed.data. No other changes needed.
  </action>
  <verify>
    <automated>cd C:\Users\javer\Documents\Repositories\ai-calling-backend && npx tsc --noEmit --project apps/ops-api/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - agents.ts POST /lead-sources schema contains `callBufferSeconds: z.number().int().min(0).optional()`
    - agents.ts POST and PATCH schemas both contain callBufferSeconds
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Lead source POST API accepts and validates callBufferSeconds via Zod schema, matching the existing PATCH schema.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors for both ops-dashboard and ops-api
2. ManagerSales.tsx premium column includes addon summation
3. ManagerConfig.tsx has buffer field in create form and read-only products table
4. agents.ts POST schema includes callBufferSeconds
</verification>

<success_criteria>
- Manager Agent Sales tab premium column sums core + addon premiums per row
- Lead source create form has Buffer (seconds) number input
- Lead source POST API accepts callBufferSeconds
- Manager Config Products section displays as read-only table with name, type, commission, bundle config columns
- No add/edit/delete controls in Products section
</success_criteria>

<output>
After completion, create `.planning/phases/29-dashboard-fixes-cost-tracking/29-01-SUMMARY.md`
</output>
