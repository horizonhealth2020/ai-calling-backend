---
phase: quick
plan: 260330-l6f
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ops-api/src/routes/lead-timing.ts
  - apps/ops-dashboard/app/(dashboard)/manager/LeadTimingHeatmap.tsx
autonomous: true
must_haves:
  truths:
    - "Heatmap hours display in Eastern Time, not UTC"
    - "Switching groupBy between DOW/WOM/MOY shows distinct 2D grids with row labels"
    - "Sparklines and recommendation endpoint also use correct ET conversion"
  artifacts:
    - path: "apps/ops-api/src/routes/lead-timing.ts"
      provides: "Correct double AT TIME ZONE conversion for all SQL queries"
      contains: "AT TIME ZONE 'UTC') AT TIME ZONE 'America/New_York'"
    - path: "apps/ops-dashboard/app/(dashboard)/manager/LeadTimingHeatmap.tsx"
      provides: "2D heatmap grid with groupVal rows and hour columns"
      contains: "groupVal"
  key_links:
    - from: "apps/ops-api/src/routes/lead-timing.ts"
      to: "PostgreSQL timestamp conversion"
      via: "double AT TIME ZONE pattern"
      pattern: "AT TIME ZONE 'UTC'\\) AT TIME ZONE 'America/New_York'"
    - from: "apps/ops-dashboard/app/(dashboard)/manager/LeadTimingHeatmap.tsx"
      to: "API response cells with groupVal"
      via: "2D grid keyed by groupVal and hour"
      pattern: "groupVal.*hour|hour.*groupVal"
---

<objective>
Fix two bugs in the lead timing heatmap feature:
1. SQL timezone conversion uses single AT TIME ZONE which misinterprets UTC timestamps as ET -- fix to double AT TIME ZONE pattern across all three endpoints (heatmap, sparklines, recommendation)
2. Frontend heatmap collapses groupBy dimensions into a flat hourly view -- restructure to a 2D grid (rows = group labels like Mon-Sun, columns = 24 hours) with a lead source selector dropdown

Purpose: Heatmap currently shows wrong hours (off by 4-5h) and switching DOW/WOM/MOY shows identical data, making the feature useless for managers.
Output: Corrected backend timezone queries and a proper 2D heatmap grid in the frontend.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/ops-api/src/routes/lead-timing.ts
@apps/ops-dashboard/app/(dashboard)/manager/LeadTimingHeatmap.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix timezone conversion in all lead-timing SQL queries</name>
  <files>apps/ops-api/src/routes/lead-timing.ts</files>
  <action>
Replace every instance of the single AT TIME ZONE pattern with the correct double conversion. The column `call_timestamp` (and `created_at`) is `timestamp without time zone` storing UTC values. PostgreSQL's single `AT TIME ZONE 'America/New_York'` interprets the stored value AS New York time, which is wrong. The correct pattern tells PG the value IS UTC first, then converts TO New York.

In the heatmap endpoint (lines 61-64, 67-70, 76-77, 87-89):
- `call_timestamp AT TIME ZONE 'America/New_York'` -> `(call_timestamp AT TIME ZONE 'UTC') AT TIME ZONE 'America/New_York'`
- `created_at AT TIME ZONE 'America/New_York'` -> `(created_at AT TIME ZONE 'UTC') AT TIME ZONE 'America/New_York'`

In the sparklines endpoint (lines 153-158, 166-170, 173-179, 187-191):
- Same pattern replacement for all `call_timestamp AT TIME ZONE` and `created_at AT TIME ZONE` occurrences, including the `::date` cast line and both CASE expressions and HAVING clauses.

In the recommendation endpoint (lines 253-254, 264-265, 307-308, 317-318):
- Same pattern replacement for all EXTRACT expressions using AT TIME ZONE.

Total: approximately 24 replacements across 3 endpoints. Every occurrence of `AT TIME ZONE 'America/New_York'` that operates directly on a column must become `(column AT TIME ZONE 'UTC') AT TIME ZONE 'America/New_York'`.

Do NOT change the Luxon `DateTime.now().setZone("America/New_York")` on line 240 -- that is correct JS-side timezone handling.
  </action>
  <verify>
    <automated>cd /c/Users/javer/Documents/Repositories/ai-calling-backend && grep -c "AT TIME ZONE 'America/New_York'" apps/ops-api/src/routes/lead-timing.ts && grep -c "AT TIME ZONE 'UTC'" apps/ops-api/src/routes/lead-timing.ts</automated>
    Verify: the count of `AT TIME ZONE 'UTC'` matches the count of `AT TIME ZONE 'America/New_York'` (every ET conversion is preceded by a UTC declaration). No bare `column AT TIME ZONE 'America/New_York'` without the UTC step should remain.
  </verify>
  <done>All SQL timezone conversions use the double AT TIME ZONE pattern. Hours extracted from timestamps will be in Eastern Time, not UTC.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure heatmap frontend to 2D grid with groupVal rows</name>
  <files>apps/ops-dashboard/app/(dashboard)/manager/LeadTimingHeatmap.tsx</files>
  <action>
Restructure the heatmap component to display a proper 2D grid where rows represent groupBy values and columns represent 24 hours, with a lead source selector above.

1. Add a lead source selector dropdown (styled with `baseInputStyle` like the groupBy dropdown). Default to the first source or "All Sources" if aggregation is preferred. When a source is selected, filter `data.sources` to that source's cells.

2. Define row label constants for each groupBy mode:
   - DOW: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"] (PG DOW: 0=Sun...6=Sat)
   - WOM: ["Week 1", "Week 2", "Week 3", "Week 4", "Week 5"]
   - MOY: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"] (PG MONTH: 1-12)

3. Fix the cell map (line 110) to key by BOTH groupVal and hour:
   ```
   const map = new Map<string, HeatmapCell>();
   for (const c of src.cells) map.set(`${c.groupVal}:${c.hour}`, c);
   ```

4. Replace the grid rendering. Instead of iterating `sourceCellMaps` as rows, iterate the row labels for the current groupBy mode. For each row label, render one row of 24 cells looked up by `groupVal:hour` key. The grid template becomes:
   ```
   gridTemplateColumns: "80px repeat(24, 1fr)"
   ```
   First column = row label (DOW name, week number, or month name). Remaining 24 columns = hour cells.

5. Update the tooltip to also show the row label context (e.g., "Monday 2pm" not just "2pm").

6. Follow existing inline CSSProperties pattern. Use `colors`, `spacing`, `radius` from `@ops/ui`. No Tailwind. The source selector row should be in the CONTROLS_ROW alongside the groupBy selector.

7. Handle the MOY case where groupVal is 1-indexed (Jan=1). The DOW case is 0-indexed (Sun=0). WOM is 1-indexed (Week 1=1).
  </action>
  <verify>
    <automated>cd /c/Users/javer/Documents/Repositories/ai-calling-backend && npx tsc --noEmit --project apps/ops-dashboard/tsconfig.json 2>&1 | head -30</automated>
    TypeScript compiles without errors. The component renders a 2D grid with row labels changing based on groupBy selection.
  </verify>
  <done>Heatmap displays a 2D grid: rows are groupBy labels (Mon-Sun for DOW, Week 1-5 for WOM, Jan-Dec for MOY), columns are 24 hours. A lead source dropdown selects which source to display. Switching groupBy shows visually distinct data layouts.</done>
</task>

</tasks>

<verification>
1. Backend: `grep -n "AT TIME ZONE" apps/ops-api/src/routes/lead-timing.ts` -- every occurrence should show the double pattern `(column AT TIME ZONE 'UTC') AT TIME ZONE 'America/New_York'`
2. Frontend: TypeScript compiles cleanly for ops-dashboard
3. Manual: Load the heatmap page, verify hours look reasonable for ET (business hours 8am-6pm should show activity, not shifted to 12pm-10pm). Switch between DOW/WOM/MOY and verify the row labels change and data differs.
</verification>

<success_criteria>
- All lead-timing SQL queries convert timestamps correctly: UTC -> ET via double AT TIME ZONE
- Heatmap grid is 2D with group labels as rows and hours as columns
- Lead source selector allows viewing one source at a time
- Switching groupBy shows distinct row structures (7 rows for DOW, 5 for WOM, 12 for MOY)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260330-l6f-fix-lead-timing-heatmap-timezone-showing/260330-l6f-SUMMARY.md`
</output>
